# Changelog

## 2026-09-15 手动摘要本地补丁提交授权

- 按负责人授权将交付清单内11文件保存为本地补丁；提交身份以Git日志为准。包含手动摘要、统计乱序保护、验收工具及累计记录。
- 版本保持1.5.2，不创建标签、不推送、不部署，所有用户数据、测试夹具及历史部署包保留在提交范围之外。

## 2026-09-15 手动摘要交付审阅（本地未提交）

- 修复并发统计加载的过期响应覆盖和提前解除loading，防止手动摘要使用尚未更新的统计；旧成功/失败均不影响最新请求。
- 新增2项回归，先复现失败再修复，前端19项及lint/build通过。交付候选11文件和排除项见LOCAL_PATCH_HANDOFF.md；版本及正式环境未变，无提交/部署。

## 2026-09-15 手动摘要本地浏览器验证

- 新增仅回环的preview-manual-summary.mjs合成测试服务，不连接真实后端或模型。
- Chrome验证首次加载无摘要请求、手动加载/失败重试、成功展示、重复点击及周期缓存通过；17项Node测试、lint/build通过。仅测试工具与文档更新，无业务代码追加、提交或部署。

## 2026-09-15 AI管理摘要改为手动生成（未部署）

- 页面初始化、凭据刷新及周期切换仅加载统计和匹配的内存摘要缓存；只有点击生成按钮才请求摘要API，数据库缓存逻辑不变。
- 增加重复点击保护、统计更新后的旧请求失效及失败重试提示；更新空状态文案。
- 新增5项实际处理函数模拟API测试，前端合计17项、lint/build通过；无真实AI调用、提交、版本变更或部署，视觉验收待办。

## 2026-09-15 正式UI冷启动边界核查

- 查明正式合成UI验收前置限制：Chrome已连接但无旧应用页，新开页自动请求Dashboard摘要，缓存未命中会调用模型并写缓存。
- 保持禁止真实AI/正式数据写入的边界，未打开应用或修改代码/服务器；同步状态与待办，UI验收未标记通过。

## 2026-09-15 终端只读验收工具

- 新增check-deployed-entry.mjs和5项合成测试：GET白名单、固定入口资源、JSON契约、超时/大小限制、禁止跳转及脱敏失败输出；不会加载页面执行AI请求。
- 正式入口检查通过（index-DtzCARX0.js、2项目）；合计12项测试、语法检查、lint/build通过。仅本地工具/文档变更，不修改版本、不部署或提交，UI验收继续待办。

## 2026-09-14 68888a8腾讯云部署成功

- 自动化助手inv-s8n00m0mqh成功，将标题栏与非法响应白屏保护补丁部署到固定68888a8；版本保持1.5.2。
- 增量bundle嵌入已核验PowerShell命令，无COS/公网分享；云端备份、SHA/Git白名单、Node7项、lint/build及切换后健康/回环门禁通过。旧dist与数据快照保留。
- 办公电脑HTTPS首页新入口index-DtzCARX0.js及历史接口200/2项目通过；无推送、标签、后端重启或真实分析调用。新增deploy/windows/deploy-68888a8.ps1固定部署模板及本地记录，未额外提交。

## 2026-09-14 补丁云端部署准备

- 本地提交68888a8已获腾讯云部署授权，未授权远程推送；核对旧部署脚本固定旧提交，禁止原样执行。
- Chrome控制台跳转微信登录页，部署暂停于登录前置条件；尚未执行云端预检、备份、上传或切换。记录本地保留，未额外提交。

## 2026-09-14 授权保存本地补丁

- 负责人授权按11文件清单保存标题栏响应式、非法响应白屏保护及验收成果；本记录随本地提交保存，版本保持1.5.2。
- 数据库、标书、测试目录、bundle及旧部署脚本不纳入；不创建标签、不推送、不部署。实际提交身份以Git日志为准。

## 2026-09-14 本地补丁交付审阅完成

- 核对标题栏和响应校验修改，确定11文件提交白名单，测试数据、bundle和旧部署脚本排除并保留；本批未修改业务代码。
- 前端7项测试、lint/build及秘密扫描通过，待明确本地提交授权；未提交、推送、部署或修改版本。

## 2026-09-14 PDF折叠组合验收与交付清单

- PDF1024四种侧栏状态及恢复均保留第2页风险框，画布未越界；本批前端7项测试、lint/build通过。
- 新增本地补丁交付范围及测试数据排除说明；未修改应用代码、提交或部署，保留隔离夹具。

## 2026-09-14 隔离合成Word/PDF响应式回归

- 复用现有合成脚本及虚拟环境，完成Word/PDF在1024/1280/1440/1920宽度下第2页定位、预览边界和风险展示检查；Word1024侧栏组合通过。新增本批验收记录，未修改业务代码或正式环境。
- pytest14项、Node7项通过；记录尺寸切换TextLayer取消警告及模型隔离引起的预期摘要502。合成目录保留，临时服务停止；不代表真实Word转换或正式内容矩阵验收。

## 2026-09-14 非JSON历史接口响应白屏修复

- 项目列表写入状态前校验数组及行对象，Dashboard写入前校验对象；静态托管回退200 HTML时显示既有错误提示，不再因files.filter崩溃白屏。
- 新增4项响应格式回归，连同API基址3项共7/7；lint/build通过。Chrome静态预览已验证错误降级及1024/1280标题栏布局，本地补丁未部署，无业务数据变更。

## 2026-09-14 标题栏响应式本地补丁（待视觉复验）

- 针对正式页面双侧栏挤压标题，新增中央容器宽度查询、标题栏自动增高和换行保护；修正主副标题CSS类名，紧凑导航保留中文可访问名称及悬停提示。
- lint/build通过；静态预览HTTP检查通过但两个浏览器仍空白，未宣称视觉验收或正式修复完成。无部署、提交、推送或业务数据变更。

## 2026-09-14 正式合成Word报告页码验收

- 从正式Chrome页面导出并核查实际落地DOCX：风险表第2页、原文引用PAGE 2、评分92分与页面一致，关闭既有报告页码不一致缺陷；文件大小与SHA-256记录于合成回归文档。
- 仅更新验收记录和任务状态，未修改应用代码、发布或网络；合成样本及新旧报告全部保留，不调用外部分析服务。

## 2026-09-13 同源API修复部署与失败保护

- 正式环境固定为 `8f130f9`；腾讯云 `inv-r8kp2q08r0` ExitCode 0，数据备份、bundle校验、独立目录构建、首页/历史接口和回环监听验证通过。
- 新增固定PowerShell部署脚本，预检失败不改线上文件，失败保留构建现场并核实回滚。修复Windows混合分隔符比较；控制台编辑必须读回保存结果。两次失败已回滚，第三次部署成功。
- 办公电脑HTTPS首页200、资源更新为 `index-oM_1rAB-.js`；历史接口200、2个项目。Chrome经历较长白屏后正常加载历史项目与Dashboard，未见读取失败提示；首次加载慢和报告内容复验仍保留为待办。
- 临时COS对象及空桶均已删除；负责人完成微信验证后，2026-09-13控制台复核存储桶列表共0项。本地bundle、云端备份和合成夹具保留；未新建提交/标签、推送或改网络，未调用外部分析服务。

## 2026-09-12 问题记录（同源修复次日完成部署）

- 实际下载报告（6）已找到，36,051字节，正文风险表仍为第1页而引用为第2页；上次仅点击导出并不能证明页码修复端到端通过。
- 新开正式入口显示项目/Dashboard读取失败；同源历史接口独立探测HTTP 200。线上入口资源 `index-BkCPqOEh.js` 的实际API常量为 `http://127.0.0.1:8000`，同时包含 `report_page`。确认生产构建遗漏API配置后回退到访问者电脑地址；旧标签页数据不能代表新部署状态。
- 本地 `frontend/src/api.js` 已改为生产默认同源、开发保留回环地址、显式配置优先；该修复后续建立为 `8f130f9` 并于2026-09-13部署。
- 当前最高优先为修复正式前端构建配置并核对浏览器实际接口请求，随后重新导出合成Word报告验证第2页。P6-31C保持进行中；本批未改生产配置、数据或网络。
- 2026-09-12：生产前端同源 API 修复建立本地提交 `8f130f9`，bundle 完整性校验通过；腾讯云上传待解决浏览器文件选择器阻塞。

- 2026-09-12正式报告补验：受控入口 `Auth Word Project` 点击“导出报告”后页面无错误；浏览器扩展不捕获程序化 Blob 下载，因此不将点击单独视为落地证据。后端报告契约测试 `4 passed、1 warning`。

- 2026-09-12正式部署完成：固定提交 `ad5acb5` 已通过腾讯云 bundle SHA-256/Git 校验、前端生产构建、健康检查和回环监听门禁；部署前备份已生成，临时 COS 对象及空桶已删除。未开放新的公网端口，未调用真实 DeepSeek、百度 OCR 或 WPS 业务分析。

- 2026-09-12浏览器连接恢复：只读打开合成 `Auth PDF Project` 分析页，确认PDF两页预览、评分80分、1项风险及第2页风险定位；未上传文件、提交表单、调用AI或修改数据。P6-31C仍保留响应式矩阵、报告内容/版式和真实Word转换限制。

- 2026-09-12风险定位补验：点击合成PDF现有风险后跳转第2页，风险详情页码同步，截图确认红色半透明高亮覆盖风险文字；未修改业务数据或调用外部服务。

- 2026-09-12 Word预览补验：合成Word项目显示2页版式预览，风险定位跳转第2页且详情页码同步；明确该预生成夹具不替代真实Word/WPS转换验收，未创建新WPS任务。

- 2026-09-12侧栏布局抽查：收起并恢复项目中心侧栏后，Word预览、顶部导航和风险检查器保持可见；仅为单一桌面视口抽查，未宣称完整响应式矩阵通过。

- 2026-09-12风险检查器布局抽查：收起并恢复右侧风险检查器后，中央Word预览扩展，项目中心和顶部导航保持可见；仅为单一桌面视口抽查。

- 2026-09-12报告页码一致性修复部署：Word报告导出携带渲染页码映射，后端优先使用映射页码并兼容原始页码；报告契约4项、前端lint/build和本机LibreOffice视觉检查通过。固定提交 `ad5acb5` 已部署到腾讯云正式环境，云端报告内容待合成回归复验。

- 2026-09-12项目对比报告视觉复核：本机LibreOffice逐页检查5页合成对比报告，关键内容无裁切或重叠；第5页留白较多，登记为非阻塞版式优化项。

- 浏览器验收阻塞记录：受控入口新标签页无法被自动化扩展接管，响应式/报告矩阵暂缓；未上传文件、提交表单或修改数据。

- 智能编标浏览器只读回归：Auth PDF Project工作台显示修订、目录审核状态、评分点来源/缺口和响应材料空状态；未执行接受/拒绝、新增或AI建议生成。

- 受控入口浏览器复核：智能编标工作台正常加载，空项目状态下目录建议/评分点提取按钮正确禁用，人工审核边界提示可见；未上传文件或调用AI。

- 测试流程文档补充：增加Windows pytest临时目录锁定时的独立 `--basetemp` 重跑示例及路径安全限制，未修改运行代码。

- 任务状态校准：将唯一未完成的P6-31C标记为进行中，补充已完成本地门禁与剩余正式环境/网络阻塞说明；未修改代码或部署。

- AI隔离与响应复核：提示词、AI异常和Dashboard缓存测试20项通过（1条既有警告），未调用真实DeepSeek或修改密钥配置。

- OCR与扫描页降级复核：provider选择、低文字页降级及预览边界测试10项通过（1条既有警告），未启用百度OCR或上传真实文件。

- 项目中心与版本元数据复核：工作区 API、配置和版本一致性测试14项通过（1条既有警告），未修改版本号、标签或业务代码。

- 核心分析契约复核：API、风险评分、风险定位和回归模板测试33项通过（1条既有警告），未调用真实AI或修改分析逻辑。

- 生产启动与部署资源复核：相关测试10项通过（1条既有警告），未启动云端服务、未调用真实AI或修改部署配置。

- 完整发布门禁复跑通过：Python编译、后端177 passed/1 skipped、秘密扫描、Python依赖审计、前端 lint/build、npm生产依赖审计均通过；未发布、推送或部署。

- 鉴权与安全边界复核：鉴权、速率限制、请求日志脱敏测试17项通过（1条既有警告），未读取真实密钥或操作云端。

- 上传输入安全复核：类型/MIME/大小、事务清理和存储路径测试28项通过（1条既有警告），未接触真实文件或外部服务。

- 数据保护回归：备份、恢复和存储路径安全测试13项通过、1项因Windows符号链接权限限制跳过（1条既有警告），未触碰真实数据。

- 稳定性边界复核：Dashboard缓存、AI异常结构化响应、上传事务测试26项通过（1条既有警告），未调用真实AI或修改业务代码。

- V1.5工作台闭环复核：目录、评分点、映射、响应材料、迁移和闭环测试17项通过（1条既有警告），未调用真实AI或修改业务代码。

- 前端边界契约复核：预览、品牌、鉴权测试13项通过（1条既有警告），确认PDF worker及核心入口未回归；未修改运行代码。

- 报告导出契约复核：实际 `test_report_contract.py` 3项通过（1条既有警告）；未修改业务代码、未调用外部分析服务。

- DOC/DOCX预览回归复跑：Word/WPS转换回退、缓存PDF和预览接口边界共7项测试通过（1条既有警告）；未调用外部分析服务，未修改业务代码。

- 后端回归门禁复跑：独立 basetemp 下 `pytest` 结果为177 passed、1 skipped、1 warning；默认临时目录Windows权限阻塞已绕开并记录，未修改业务代码。

- 后端门禁复跑：默认 pytest 临时目录因Windows权限占用导致首跑setup错误；切换到隔离临时目录后完整测试通过（177 passed、1 skipped），未修改业务代码或数据。

- 前端门禁复跑：`frontend` 目录 `npm run lint` 与 `npm run build` 均通过；构建保留既有大 chunk 非阻塞警告，未修改业务代码或继续扩大拆包范围。

- P6-31C腾讯云只读安全核查：确认实例仅绑定 `sg-hftoto2k` 且该安全组仅关联当前实例；入站仅有既有 TCP 3389/80/443 规则，没有 UDP 41641。发现一条已有 IPv6 全来源 TCP 3389，登记为待单独决策的安全风险。未修改安全组、端口、正式代码或数据。

- P6-31C单目标TUN排除回滚复核通过：用户撤销后4份配置SHA-256全部匹配试验前备份，公网目标路由恢复Mihomo，tailnet路由保持Tailscale；中继pong 946毫秒，ChatGPT HTTPS 200/证书验证通过。试验已恢复结束，原加载问题未修复；未改云端网络、正式代码或数据。

- P6-31C单目标TUN排除实测：用户手动保存，配置仅预期项变化；目标路由WLAN、tailnet路由Tailscale、ChatGPT HTTPS 200且证书验证通过。三次ping仍DERP(lax)，worker下载40.014899秒超时（246272/1369825字节）。已请求用户删除本条排除恢复，回滚待确认；未改云端网络、正式代码或数据。

- P6-31C单目标TUN排除预检：已获授权，核实Mihomo 1.19.29及auto-route/空排除列表，4份相关配置备份与原件哈希一致；窗口控制兼容性失败、恢复期间检测到用户输入，停在应用前。没有写入排除项，没有网络变更需要回滚；尚未完成改后联合验证，未改正式代码/数据/云端安全组。

- P6-31C本机只读路由调查：目标公网地址经Mihomo TUN，tailnet地址经Tailscale，无活动出口节点；系统HTTP代理关闭、Clash Verge/Mihomo运行。明确--noproxy不排除TUN接管，未确认最终UDP来源，不误判Clash为唯一根因。未读取订阅或秘密配置，未修改任何网络设置；目标/32的TUN排除仅作为需另行授权的候选方案。

- P6-31C受限直连授权后预检：本机Tailscale UDP探测与绕过HTTP代理的HTTPS公网出口IPv4不一致，无法确认目标UDP来源，按用户明确停止条件停止。未新增安全组/防火墙规则，无需回滚；未改代理、正式代码或数据，未调用外部分析服务。等待是否只读调查本机分流路径的指示。

- P6-31C两端网络诊断汇总：云端 `cmd-4cvbhrkn` 首次被PowerShell原生命令警告打断，修正诊断脚本后 `inv-r8hn60g17h` ExitCode 0；云端UDP可用、映射稳定，办公电脑映射变化。形成仅办公电脑公网IPv4/32的受限Tailscale UDP直连试验及失败回退方案，因涉及新增入站许可等待授权。未改正式代码、网络、数据或代理，未调用外部分析服务，端到端故障未解决。

- P6-31C自身HTTPS对照完成：`cmd-8a94fnrr` / `inv-r8hn000u9d` ExitCode 0，三次完整worker下载1709～1999毫秒；办公电脑同期20秒仅收到115,200字节并超时。办公电脑netcheck已获取脱敏能力摘要，云端netcheck待补。范围收窄至跨节点访问差异，未认定Clash为根因，未改正式代码、配置、数据或网络，端到端门禁仍未通过。

- P6-31C云端回环对照通过：自动化助手独立只读命令 `cmd-bda7elnd` / `inv-r8hd5g0stv` ExitCode 0，同一worker三次完整1,369,825字节，耗时1539/1462/1406毫秒。与办公电脑40秒超时形成对照，下一步区分Serve HTTPS与跨节点路径，尚不认定Clash/DERP为唯一原因。未修改正式代码、配置、网络或用户数据；端到端问题保持未解决。

- P6-31C冷加载只读排查：3次Tailscale探测均经DERP(lax)，延迟398～906毫秒，未建立直连；worker HEAD类型/长度正常，普通及支持压缩的GET均40秒超时。保留为未解决问题，下一步先做服务器回环对照，不将中继判定为唯一根因。本批仅更新证据/状态文档，未修改正式代码、网络或数据，未调用外部分析服务。

- P6-31C合成回归进展：负责人授权后备份原空数据库，独立合成数据目录已激活并修正数据库/上传/报告路径一致性；云端最终门禁 `inv-s8gt2jg5ie` ExitCode 0。Chrome验证PDF两页正文、风险跳页与可见高亮，Word预生成PDF风险页映射、Dashboard查看分析穿透、基础项目对比及两类报告实际下载。本批未修改正式业务代码；保留原库、备份、夹具和报告，不调用外部分析服务。冷加载传输延迟、真实Word转换和报告内容/版式未整体验收，P6-31C保持未完成。

- P6-31C第七小批完成办公电脑首轮正式入口回归：负责人本人认证后，Tailscale客户端Running/Online且云服务器节点在线；Chrome通过正式HTTPS加载应用，同源历史接口200，Dashboard、标书分析、智能编标导航和空状态正常。生产项目列表为空，故文档预览、风险跳页、项目对比和报告导出尚无测试对象；未上传文件、写入数据库或调用DeepSeek/OCR。
- P6-31C第六小批完成办公电脑Tailscale客户端安装：winget启动器不可访问后改用官方稳定源，安装包Authenticode签名有效且主体为Tailscale Inc.；静默安装后CLI版本为 `1.102.3`、Windows服务RUNNING、BackendState为 `NeedsLogin`，本机8000/4173无监听。未启动用户认证、未修改腾讯云公网安全组或服务器入口。
- P6-31C第五小批完成办公电脑接入前只读审计：当前电脑未发现Tailscale可执行文件、标准安装目录或Windows服务，本机8000/4173也无监听。已将过时的内部访问方案同步为服务器端HTTPS入口已完成；下一步需明确授权安装官方客户端，并由负责人本人完成tailnet认证后执行真实业务回归。
- P6-31C第四小批完成Tailscale Serve HTTPS入口：负责人启用tailnet HTTPS后，首次短探测未就绪并按门禁自动重置；延长等待窗口复跑 `cmd-njz119nd`，执行记录 `inv-s8grjp0qjc` 用时2秒、ExitCode 0，通过正式版本、应用任务、共享鉴权、HTTPS 200及8000/4173仅回环检查。公网安全组未修改，下一批仅做获准办公电脑业务回归。
- P6-31C第三小批尝试启用Tailscale Serve HTTPS：经动作时确认后，执行 `cmd-68k0g98b` 与加入非交互 `--yes` 的 `cmd-njz119nd`，均在腾讯云自动化助手上限处超时；每次后续只读检查均明确返回 `No serve config`，未留下半配置入口。结合Tailscale官方文档确认，当前tailnet尚未完成MagicDNS/HTTPS Certificates管理员确认；首次开启还需确认设备名与tailnet DNS名进入公开证书透明度记录。P6-31C暂时阻塞，正式后端/Caddy继续仅回环运行，公网安全组未修改。
- 完成P6-31C第二小批回环应用入口：云端固定正式提交 `4f01024 / 1.5.2`，备份生产 `.env` 与前端构建后启用 `AUTH_MODE=shared_token`，令牌仅保存在服务器秘密配置；前端按tailnet HTTPS同源地址重建，Caddy作为计划任务仅绑定 `127.0.0.1:4173`，为 `/api` 与 `/uploads` 注入服务器端令牌并代理到 `127.0.0.1:8000`。最终执行 `cmd-mgkro89h` 以ExitCode 0通过匿名后端401、带令牌后端200、代理接口/首页200及8000/4173无公网监听门禁。Tailscale Serve与办公电脑访问尚未启用，腾讯云公网安全组未修改。
- P6-31C第一小批完成官方Caddy安装：经动作时确认后，从Caddy官方GitHub发布接口解析唯一Windows amd64发布包及校验清单，SHA-512一致后安装并运行版本自检；腾讯云执行记录 `inv-r8g2sh00j2` / `cmd-8qsizihl` 以ExitCode 0完成。耗时814秒但未超出受控超时；本批未修改后端鉴权、前端构建、Tailscale Serve或公网安全组。
- 启动P6-31C受控内网应用入口：云端预检确认正式 `4f01024 / 1.5.2`、计划任务、健康探针和前端dist正常，业务接口仍为匿名200；Tailscale Serve当前无配置并支持目录/本机HTTP目标。代理清单确认Caddy、Winget和IIS均不存在，计划采用仅回环Caddy完成前端托管、同源代理和服务器端令牌注入，等待新软件安装的动作时确认；尚未修改生产配置、鉴权、Serve或公网安全组。
- 完成P6-31B腾讯云Tailscale节点入网：负责人亲自完成一次性设备认证后，只读复核命令 `cmd-3ot05ejj` 在不足1秒内以ExitCode 0通过，强制条件确认Windows服务与Tailscale后端Running、节点Online且已分配Tailscale IPv4；具体内网地址不写入仓库。未开放应用入口、未修改公网安全组或后端回环监听。
- 启动P6-31B腾讯云受控组网节点安装：复核Tailscale官方Windows下载与CLI文档后，自动化助手执行 `inv-r8fijg0w25` 从官方稳定源下载并通过Authenticode签名验证，安装客户端 `1.102.3`，Windows服务为Running。设备认证链接已生成，等待负责人亲自登录后复核节点在线；未开放应用入口、未修改公网安全组或后端回环监听。
- 项目负责人为正式部署入口选择B（受控组网/VPN）；完成P6-31A实施边界，首选候选为Tailscale，并将云服务器节点入网、前端长期托管/同源代理、共享鉴权和办公电脑接入拆为独立门禁。本批未安装第三方软件，未修改生产环境或腾讯云公网安全组；实际安装前仍需从官方来源复核版本、命令和签名。
- 完成P6-30正式部署访问边界审计：确认腾讯云当前仅有后端 `127.0.0.1:8000` 回环服务，前端尚未长期静态托管；形成服务器本机、受控组网/VPN和域名HTTPS三种路径及鉴权、CORS、最小端口、回滚和受保护功能回归门禁。同步关闭已被1.5.2维护发布取代的P6-16进行中状态；未修改云服务器、网络、安全组或秘密配置。
- 完成正式 `v1.5.2` 腾讯云部署：通过临时 COS 单对象传输固定 Git bundle，云端复核 SHA-256 `8723EDC9...16D47D0`、annotated 标签对象 `34650bc...` 及解引用提交 `4f01024...`；系统 PATH 修复与普通重启后，自动化助手执行 `inv-r8fgsw05ra` 以 ExitCode 0 完成。后端生产检查、健康/就绪/文件接口及前端 lint/build 通过，服务仅监听 `127.0.0.1:8000`，未开放80/443或其他新公网端口，DeepSeek/OCR保持禁用。临时 COS 对象及空桶均已由项目负责人确认删除。
- 启动正式 `v1.5.2` 腾讯云部署：预检确认旧正式检出 `b542d01 / 1.5.1`、无既有数据目录/应用进程/80、443、8000监听，生成并验证 v1.5.1 回滚 bundle（SHA-256 `5306A804...E3572`）。两次从 GitHub 获取固定提交 `4f01024` 分别因连接重置和443超时失败，自动回滚均确认正式 HEAD 未变，未创建数据目录、启动任务或公网入口；继续等待临时 COS 单对象中转授权。
- 经项目负责人授权，将 `v1.5.2` GitHub远程 `main`、标签对象及解引用提交的哈希复核结果建立为独立本地文档提交；未再次推送或部署。
- 经项目负责人授权，将 `main` 与 `v1.5.2` 推送到 GitHub；首次 SSH 连接因代理映射地址关闭而安全失败，改用 HTTPS/OpenSSL/HTTP/1.1 后成功。远程 `main=a9125eb`、标签对象 `34650bc` 和解引用正式提交 `4f01024` 与本地一致，未部署。
- 经项目负责人授权，将本地 `v1.5.2` annotated 标签状态及发布边界建立为独立文档提交；标签仍固定解引用到正式提交 `4f01024`，未推送或部署。
- 经项目负责人授权，为正式 `1.5.2` 发布提交 `4f01024` 创建本地 annotated 标签 `v1.5.2`；标签对象 `34650bc` 解引用到完整正式提交，未推送或部署。
- 经项目负责人授权，将已签署且腾讯云候选门禁通过的 `1.5.2-rc.1` 统一提升为正式 `1.5.2`；根版本、FastAPI 动态版本来源和前端包/锁文件一致，完整正式发布门禁通过并创建本地发布提交，未创建标签、推送或部署。
- 完成正式 `1.5.2` 发布前只读审计与执行清单：确认已签署候选 `e4cf9dd` 位于当前 HEAD 祖先链、版本仍为 `1.5.2-rc.1` 且不存在 `v1.5.2*` 标签；固化正式版本提升、标签、推送、腾讯云部署的分段授权、完整门禁和回滚边界，未执行发布写操作。
- 完成 `1.5.2-rc.1` 腾讯云隔离候选门禁：候选身份、System 符号链接回归、完整后端 178 项、秘密扫描、Python/npm 依赖审计及前端 lint/build 均通过；隔离虚拟环境 pip 升至 26.2 消除 `PYSEC-2026-3721`，最终执行 `inv-s8fe6wg7vv` 确认候选工作区干净。临时 COS 对象和空桶已删除，正式 `1.5.1`、数据目录及服务未改动，未推送、打标签、提升正式版或部署。
- 完成 `1.5.2-rc.1` 腾讯云候选门禁安全交付方案：使用 Git bundle、固定 SHA-256 和独立云端目录检出精确提交 `e4cf9dd`，优先验证 System 账户符号链接修复后再运行完整门禁；本地命令链已验证，候选包尚未上传，云端正式 `v1.5.1` 未改动。
- 项目负责人于2026-09-09确认签署 `1.5.2-rc.1` 最终候选提交 `e4cf9dd`；签署仅授权后续腾讯云候选门禁，明确不授权创建标签、推送、提升正式版本或部署。经另行授权，签署与状态文档建立为独立本地提交。
- 完成 `1.5.2-rc.1` 候选提交 `e4cf9dd` 的签署前只读复核：候选身份、父提交、11个文件、版本、标签和数据排除边界一致，版本/备份聚焦回归 2 passed、1 skipped；已建立待项目负责人确认的验收签署单，未自动签署、提升、打标签、推送或部署。
- 经项目负责人授权，将云端门禁发现的备份原子性修复统一提升为 `1.5.2-rc.1`；完整候选门禁为 177 passed、1 skipped，Python/npm 依赖审计均为 0 个已知漏洞，秘密扫描和前端 lint/build 通过。本批只建立本地候选提交，不创建标签、不推送、不部署。
- 云端依赖门禁发现并修复 Windows System 账户下的备份原子性问题：符号链接安全拒绝改为在创建快照目录前完成所有源目录校验，避免异常清理受 Windows 占用影响时留下空目标。本地完整回归 177 passed、1 skipped；云端正式 `v1.5.1` 未热改，等待新维护版本决策。
- 执行腾讯云项目依赖门禁：原始文件 SHA-256 因 Git CRLF 换行转换安全失败，已换为固定 HEAD + Git 工作树/依赖清单无差异校验。修正后成功建立 `.venv`、安装后端依赖并通过秘密扫描；完整 pytest 为 177 passed、1 failed，因门禁规则未继续前端安装或服务配置。
- 经项目负责人动作时确认，通过临时 COS 在腾讯云 Windows 主机安装 Git：固定 SHA-256、Authenticode 和签名证书指纹校验全部通过，自动化助手执行 `inv-k8e34g0636` 以 ExitCode 0 完成，并返回 `git version 2.55.0.windows.5` 与 `GIT_INSTALL_OK`；未重启服务器。经后续单独确认，公有读安装包对象及空桶已删除，控制台复核对象数和桶数均为 0。
- 完成腾讯云固定版本部署只读预检：首次 GitHub 连接重置后为命令增加 OpenSSL、HTTP/1.1 和有限重试，复跑 `inv-k8e3mtg1f9` 以 ExitCode 0 完成；目标应用/数据目录均不存在，C 盘可用 76.68 GB，远程 `v1.5.1` 标签对象及解引用提交与正式发布记录一致，未创建目录或检出代码。
- 完成腾讯云固定版本检出：自动化助手执行 `inv-k8e3t3gkdd` 以 ExitCode 0 将单标签 `v1.5.1` 克隆到全新 `C:\AI-Bid-Assistant`；HEAD、根版本和工作区干净状态通过核验，detached HEAD 为发布标签预期状态，未跟随 `main`、未生成数据目录或启动服务。
- 完成腾讯云依赖安装命令准备：正式 HEAD、后端依赖清单和前端锁文件均以固定哈希保护，命令将建立应用专属 `.venv`、执行 pip/npm 安装及后端完整回归、前端 lint/build；当前已选中唯一实例并停在最终执行确认点，尚未安装或启动服务。
- 完成正式 `1.5.1` 腾讯云部署的 Git 安装网络诊断：本地从 Git for Windows 官方发布地址取得并验证安装包（SHA-256 与 Authenticode 有效），云主机侧依次复现证书吊销离线、连接重置及官方 CDN 约 15–20 KB/s 限速；三次命令均在安装阶段前安全失败或超时，Git 未安装，Python/Node 复核未受影响。
- 启动正式 `1.5.1` 腾讯云 Windows Server 真实部署准备：自动化助手确认 Python 3.14.6、Node.js 24.18.0 和 npm 11.16.0 已安装；首次整批命令因云主机直连 GitHub 超时，Python 自定义目录参数被空格拆分后实际安装到 `C:\Program`，后续部署暂用 `py -3.14` 启动器并将 Git/WPS 分批处理。
- 完成正式 `1.5.1` 本地部署预检：部署模板、生产启动、探针和备份恢复聚焦回归17 passed、1 skipped，前端正式构建与秘密扫描通过；未连接服务器或部署。
- 经项目负责人授权，将 `main` 和 `v1.5.1` 推送到已配置的GitHub仓库；远程哈希与本地复核一致，未部署。
- 经项目负责人授权，将 `v1.5.1` 标签及发布状态同步建立为独立本地文档提交；未移动标签、推送或部署。
- 经项目负责人授权，为正式 `1.5.1` 发布提交 `b542d01` 创建本地annotated标签 `v1.5.1`；未推送、未部署。
- 经项目负责人授权，将已签署候选统一提升为正式 `1.5.1`，复跑正式发布门禁并创建本地发布提交；未创建标签、推送或部署。
- 经项目负责人授权，将 `1.5.1-rc.1` 签署记录及状态同步建立为独立本地文档提交；未提升正式版本、创建标签、推送或部署。
- 项目负责人已签署 `1.5.1-rc.1` 最终候选提交 `814c4bf`；签署记录和项目状态已同步，签署明确不创建标签、不推送、不提升正式版本、不部署。
- 完成 `1.5.1-rc.1` 候选提交 `814c4bf` 的签署前只读复核：核对22个文件、版本、标签和数据排除边界，聚焦回归12 passed，并建立待负责人确认的验收签署单；未提升正式版本、创建标签、推送或部署。
- 经项目负责人授权，将P6维护成果统一提升为 `1.5.1-rc.1`，复跑完整候选门禁并建立一个本地维护候选提交；未创建标签、推送或部署。
- 完成P6维护候选边界审计：统一发布门禁177 passed、1 skipped，Python与前端依赖审计均为0个已知漏洞；固化18个候选文件、真实配置/用户数据排除、版本标签和 `git revert` 回滚边界，本批不提交、不推送、不部署。
- Dashboard改用ECharts模块化入口，只注册现有折线、柱状、饼图及必要组件/Canvas渲染；图表块由1136.66 kB降至585.56 kB，gzip由377.43 kB降至198.59 kB，四图、周期切换、响应式和风险穿透通过隔离浏览器回归。
- 完成前端拆包收尾审计：manifest确认工作台、Dashboard图表和文档预览三个动态入口有效；量化默认Dashboard首次JS约2.06 MB（gzip约673.61 kB），并确认剩余1.14 MB大块来自 `echarts-for-react` 默认完整ECharts入口，后续不再拆页面，转入模块化图表优化。
- 完成文档预览拆包浏览器回归：两页PDF/Word的翻页、风险跳页、文字高亮、Word页映射和合成OCR框通过；修复懒加载后宽度观察器挂载过早导致窄容器仍按700px渲染的问题，完整门禁177 passed、1 skipped。
- 文档预览拆包第二步：抽离并懒加载 `DocumentPreview`，将react-pdf与worker初始化移出主入口，同时保留Blob生命周期、Word协调和风险点击状态；主入口降至约346.23 kB，预览形成约347.44 kB异步chunk，浏览器回归留待下一小批。
- 完成文档预览异步边界审计：新增6组机器契约保护PDF worker与Blob生命周期、响应式宽度、翻页、Word渲染页映射、文字高亮和OCR叠框；形成纯展示组件优先的最小拆分顺序，本批未搬迁运行代码。
- 完成Dashboard/ECharts隔离浏览器回归：4个图表、7/30天切换及重点关注项目到PDF风险检查器的穿透通过；修复Dashboard多项目时只聚合最后一条的缩进错误，并新增双项目数据一致性测试。
- Dashboard拆包第一步：将4处ECharts图表封装为懒加载展示组件，保持图表option、周期状态、预警穿透和AI缓存数据流不变；主入口降至约1269.76 kB，Dashboard/ECharts形成约1136.65 kB异步chunk，浏览器回归留待下一小批。
- 完成工作台懒加载的隔离合成浏览器回归：异步入口、空状态、项目切换、AI确认取消及章节/映射/材料入口正常，控制台无错误或警告；模型请求保持不可达本机隔离。
- 前端拆包第一步：将 `WorkbenchPage` 抽离为独立模块并使用 `React.lazy`/`Suspense` 按需加载，保持既有操作props契约；构建生成独立工作台chunk，浏览器回归留待下一小批。
- 完成前端拆包边界审计：量化单文件入口与ECharts/react-pdf重依赖，确定先工作台、再Dashboard/ECharts、最后文档预览的渐进拆分顺序，并明确不以提高警告阈值代替优化。
- 正式版后维护：项目中心移除Ant Design已弃用的 `List` 组件，改用带 `list`/`listitem` 语义的原生卡片映射并保留空状态；新增契约测试防止弃用组件回归。
- 经项目负责人授权，将已签署候选统一提升为正式 `1.5.0`，复跑正式发布门禁并创建本地发布提交和 `v1.5.0` 标签；不推送、不部署。
- 经项目负责人授权，将 `1.5.0-rc.1` 签署记录及状态同步建立为独立本地文档提交；不创建标签、不推送、不提升正式版本、不部署。
- 项目负责人已签署 `1.5.0-rc.1` 最终候选提交 `33ad2bc`；验收单和项目状态已同步，签署明确不创建标签、不推送、不提升正式版本，相关动作均未执行。
- 完成 `1.5.0-rc.1` 候选提交 `33ad2bc` 的签署前只读复核，核对35个提交文件、版本与数据排除边界，修正审计中的提交前旧HEAD并新增待负责人确认的验收签署单；未提升正式版本、创建标签、推送或部署。
- 经用户授权将版本统一提升为 `1.5.0-rc.1`，复跑完整候选门禁并建立一个本地原子候选基线；Python依赖及npm缓存离线审计均为0个已知漏洞，未创建标签、推送或部署。
- 完成V1.5候选第一轮门禁与只读审计：完整后端167项通过、迁移5项通过、前端lint/build及秘密扫描通过；固化交付范围、迁移安全、已知限制和Git数据边界，未修改版本或执行Git写操作。
- 完成V1.5工作台纯合成浏览器闭环回归：项目隔离、建议审核、人工映射、材料新增/编辑、修订与汇总、AI确认取消及1024px无横向溢出通过；夹具补充合成工作台数据且不连接真实模型。
- 智能编标工作台补齐响应材料人工新增入口：只展示当前工作台已确认评分点和章节，至少关联一项后才能保存，并支持人工填写名称、责任人、说明及四态状态；不执行自动分派或正文生成。
- 智能编标工作台新增目录建议与评分点提取的显式AI入口；调用前明确提示会发送当前招标文件并消耗模型额度，结果保持待人工审核，写入沿用修订冲突保护。
- 智能编标工作台接入已确认评分点到已确认章节的人工映射，展示已有章节标签并支持更新人工说明；保存不删除其他关系，使用修订冲突保护且不执行AI自动映射。
- 智能编标工作台新增人工顶级目录章节入口，前端拦截空标题并限制200字；保存携带当前修订，成功更新完整工作台，409时丢弃过期输入并提示刷新。
- 智能编标工作台新增响应材料编辑弹窗，支持人工维护责任人、说明及待准备/进行中/已完成/阻塞状态；无变化不写入，409时丢弃过期编辑上下文并提示刷新。
- 智能编标工作台接入AI目录和评分点建议的人工接受/拒绝操作；仅待确认项显示按钮，拒绝需二次确认，写入携带当前修订并对409冲突提示刷新。
- 新增智能编标工作台前端只读入口，复用项目中心和统一认证客户端，以三列总览展示目录、评分点来源/覆盖和响应材料责任人/状态；工作台页面隐藏风险检查器且不提供正文生成。
- 新增V1.5智能编标工作台最小闭环回归，串联人工目录、带来源评分点、审核、章节映射和响应材料，验证连续修订快照及过期写入保护。
- 完成V1.5响应材料后端闭环：新增材料创建、列表和修改API，支持人工责任人、说明及四种状态汇总；仅允许关联当前工作台已确认评分点/章节，写操作受修订冲突保护并纳入快照。

本项目采用 Keep a Changelog 风格；正式版本遵循语义化版本。

## [1.5.1] - 2026-09-05

- 修复Dashboard多项目聚合的数据一致性问题。
- 工作台、Dashboard图表和文档预览改为按需加载，ECharts使用模块化入口。
- 增加拆包、PDF/Word预览及风险定位边界回归，保持V1/V1.5既有业务能力。
- 本地正式版本未创建标签、推送或部署。

## [1.5.0] - 2026-09-04

- 新增可审计的智能编标工作台最小闭环：目录建议、评分点提取、人工审核、章节映射、响应材料和修订快照。
- 增加独立前端工作台入口、显式AI调用确认、项目隔离、覆盖缺口与材料状态展示。
- 使用两版SQLite增量迁移保护既有V1数据，并通过完整后端、前端、迁移、浏览器和安全门禁。
- 正式版明确不包含一键整本标书、自动正文、自动分派或自动投标。

## [Unreleased]

- 新增第二版工作台安全迁移和响应材料模型：材料至少关联评分点或章节，支持人工责任人、说明及待准备/进行中/已完成/阻塞状态；迁移幂等、保留旧数据并拒绝残缺同名表。
- 完成评分点—章节人工映射：仅允许同工作台已确认对象，支持幂等更新说明、修订冲突保护、映射快照以及已覆盖/缺口/待确认统计。P5-03后端闭环完成。
- 完成评分点建议人工审核：支持接受和拒绝，保留原文来源与最终状态快照，并拒绝重复审核、跨工作台引用和过期修订。
- 新增带来源校验的评分点提取接口：结构化保存标题、要求、可空最高分值、页码和连续原文引用；无效项过滤，模型失败或全部无效时零污染，并纳入AI限流与修订快照。
- 完成AI目录建议人工审核：支持接受和拒绝待确认建议，保留AI来源、原文引用与历史快照，并拒绝重复审核、人工章节审核和过期修订。
- 新增带来源校验的AI目录建议接口：仅保存可在对应页验证的引用，建议保持待确认，模型失败或全部引用无效时不写入数据或修订，并纳入AI请求限流。
- 完成人工目录章节后端编辑闭环：支持新增、改名和同级排序，以工作台修订号阻止并发覆盖，并为每次成功修改保存快照。
- 完成V1.5智能编标工作台设计基线：目录树、评分点、映射、来源引用、修订模型、API草案和三栏交互原型。
- 新增机器可读工作台契约及测试，锁定AI建议需人工确认、评分点必须有来源且禁止一键整本标书和自动投标。
- 新增版本化SQLite工作台迁移、6张基础模型、幂等空工作台创建/读取接口和初始修订；旧V1数据保留，残缺同名表拒绝登记成功。

## [1.0.0] - 2026-09-03

- 经用户授权将已签署候选提升为正式 `1.0.0` 并创建本地 `v1.0.0` 标签；未推送、未部署。
- 统一发布门禁的成功提示同时适用于候选版和正式版，避免正式发布时仍显示候选状态。
- 经用户授权建立 `1.0.0-rc.1` 本地原子候选基线；不包含推送、正式标签或发布。
- 发布检查的pytest临时目录和pip-audit缓存改为每次唯一生成，避免Windows残留目录权限导致后续门禁失败或降级警告。
- 新增 `1.0.0-rc.1` 候选验收签署单，固化候选提交、验证证据、已知限制、部署待办和授权边界。
- 修复从仓库根目录调用生产脚本时默认数据库和存储目录随当前工作目录漂移的问题。
- 项目负责人已签署 `1.0.0-rc.1` 候选版本验收，最终候选提交为 `6844019`；未创建标签或执行推送。

### Added

- 自动开发总指令、固定工作流、项目状态、路线图、活动任务和优先级任务清单。
- 前后端启动、最低验证和 V1 核心回归清单。
- `.env.example`、安全说明和后端依赖清单。
- pytest 临时 SQLite 应用夹具，以及根接口、健康检查、历史文件列表和数据库隔离测试。
- 统一 AI 错误响应契约及三条 AI 调用链的缺少配置、超时和非法 JSON 替身测试。
- 上传文件与数据库事务规则，以及部分写入、解析、AI 和提交失败的隔离回归测试。
- Dashboard AI 规范化指纹、命中、数据变化失效、损坏缓存重建和失败不落库测试。
- 风险报告和项目对比报告的 Word 重开、字段一致性、空值降级及唯一文件名测试。
- 不含真实数据的回归样本台账、预期结果 JSON、脱敏结果模板及其结构测试。
- 授权真实回归运行器及3份 PDF 的脱敏格式回归记录。
- pytest 使用已忽略的项目本地临时根，避免 Windows 公共 Temp 权限异常阻断隔离测试。
- 授权回归结果审计脚本，验证风险页码、原文高亮和 Word 报告关键统计一致性。
- DOCX 文本预览接口及其隔离测试，返回明确标记的模拟分页文本而非伪装成 PDF。
- 授权 DOCX、纯扫描 PDF 和合并单元格表格 PDF 的脱敏样本登记，以及可按样本编号运行真实回归的选择参数。
- Word 97–2003 DOC 二进制文本段解析及模拟分页预览，新增 `olefile` 轻量依赖和错误格式隔离测试。
- 百度通用文字识别（标准含位置版）扫描页回退、令牌缓存、调用上限、坐标归一化、风险框叠加及 HTTP 替身测试。
- Microsoft Word 主引擎、WPS Office 备用的 DOC/DOCX 转 PDF 脚本，以及转换校验、引擎回退和历史风险页码映射测试。
- 运行配置默认值、进程环境和 `.env` 优先级测试。
- DeepSeek端点/模型/超时接线与上传大小限制、超限零残留测试。
- PDF/DOCX/DOC标准MIME、文件签名及伪装文件前置拒绝测试。
- 上传根目录边界、异常历史路径公开/预览/删除隔离及穿越文件名测试。
- 生产启动配置、数据库/存储就绪探针和启动前失败测试。
- Nginx、systemd、Windows启动/服务模板的HTTPS、预检、守护、轮转和无秘密契约测试。
- 请求ID、结构化访问日志以及嵌套敏感字段、令牌和安全路径归一化测试。
- 滑动窗口限流、稳定429/Retry-After、共享AI预算与可信代理身份测试。
- 鉴权保护矩阵、全部业务/静态/文档路由覆盖和秘密规则文档测试。
- 方案A共享Bearer密钥中间件、公开路径白名单、鉴权启动检查及401/503安全契约测试。
- 前端统一认证请求客户端、仅内存共享凭据输入、401重新认证提示和认证源码契约测试。
- 纯合成共享鉴权回归夹具脚本及生产入口关闭原始Uvicorn访问日志的自动契约测试。
- 非覆盖式SQLite、上传和报告备份快照脚本，包含逐文件SHA-256清单和安全边界测试。
- 只向全新目录写入的安全恢复脚本，包含清单路径、文件集合、复制前后SHA-256、SQLite完整性及失败清理验证。
- 独立依赖审计工具清单和无内容泄露的高置信度秘密扫描脚本及测试。
- `1.0.0-rc.1`统一候选版本、跨后端/前端版本一致性测试、一键发布检查入口、发布清单及发布说明草案。
- V1候选版本纯合成隔离生产、鉴权和核心浏览器冻结回归记录。
- V1候选版本Git基线审计、用户数据排除清单、原子提交方案及回滚边界文档。
- 中文页面元数据、AI标书助手SVG favicon、实际前端README和品牌回归契约。

### Changed

- 移除未引用的Vite模板图像和图标资源，保留PDF预览必需的worker文件。
- DeepSeek 客户端改为调用 AI 功能时按需创建；未配置 Key 不再阻止非 AI 服务启动。
- 清理前端未使用导入、变量和无效赋值，修正异常空白；ESLint 从 13 个错误、1 个警告降为零，业务执行路径保持不变。
- 后端验证命令改为使用项目原有 `backend/.venv`，根接口与健康接口已通过。
- 移除 Vite 示例全局样式，统一应用背景、Content 响应式间距、内容最大宽度和基础字体。
- PDF 页面宽度改为监听中央容器动态调整，左右侧栏和视口变化时不再依赖固定 700px 裁切。
- 统一 Dashboard、标书分析和项目对比的顶层标题层级、Ant Design 图标、12px 卡片圆角与 20px 垂直间距。
- 项目对比报告导出按钮改用标准导出图标，保持原导出行为不变。
- 统一项目中心、Dashboard、风险检查器和项目对比的加载与空状态，复用 Ant Design Spin/Empty 表达。
- 项目列表和 Dashboard 请求失败增加页面内错误 Alert，同时保留原有 toast 提示。
- 修复 1024px 双侧栏展开时 Header、Dashboard 图表和项目行的横向溢出。
- 窄桌面展开侧栏自适应为左 220px、右 300px；折叠仍保持 56px，PDF 继续按中央容器缩放。
- Dashboard 图表采用稳定单列布局，避免 ECharts 在窗口单双列切换时保留旧画布宽度。
- 完成 V1 UI 受保护功能回归，覆盖 Dashboard 周期与预警穿透、项目中心搜索/筛选、风险定位高亮、项目对比选择及两类报告生成。
- 将风险等级、扣分、评分和空值处理提取为统一后端契约，供上传、项目列表/详情与 Dashboard 复用。
- 上传、项目对比与 Dashboard AI 接口改用可识别的 502/503/504 结构化错误；上传页展示安全错误说明。
- 上传改用 UUID 临时文件和唯一正式路径；项目列表/详情根据数据库存储路径生成预览 URL。
- Dashboard AI 缓存指纹忽略无关请求字段并规范统计周期；损坏缓存自动失效后重新生成。
- 风险报告复用统一评分契约并展示评级、总风险和总扣分；前端导出提交当前页面风险状态。
- 两类服务端 Word 报告改用微秒级唯一文件名，避免覆盖已有报告。
- 风险高亮在模型词组无法定位时，从指定页引用与原文的连续重合片段生成保守回退词，不跨页猜测。
- 前端按文件扩展名选择 PDF 或 Word 预览链路；早期 DOCX 模拟分页现已升级为办公软件转换后的 PDF 真实页面，风险点击继续支持跳页和高亮。
- 前端上传选择器与预览分流扩展至 `.doc`；后端在分析前拒绝非 PDF/DOCX/DOC，并对无法解析的 DOC 返回 422。
- PDF 解析改为优先使用本地文字层，仅对文字不足页面调用显式启用的百度 OCR，并保留原页码。
- OCR 命中行坐标随风险结果持久化；前端在响应式 PDF 上按风险等级叠加高/中/低风险框，电子 PDF 文字层高亮保持不变。
- DOC/DOCX 分析和预览统一使用办公软件转换后的 PDF 真实页面；前端显示每页页码、总页数和实际渲染引擎，历史模拟页码按原文重新映射。
- 用户无 Microsoft Word 授权时由本机 WPS Office x64 作为实际渲染引擎；已完成合成 DOCX、本地 Word 报告及历史 DOC/DOCX 的真实页数和界面回归。
- 数据库 URL、上传目录、报告目录和 CORS 白名单改为统一配置模块驱动，同时保留原本地开发默认值。
- DeepSeek客户端支持统一模型与超时环境配置；上传改为分块累计真实字节并在超过默认50 MiB时前置拒绝。
- 上传入口增加扩展名、声明MIME和文件签名一致性校验，同时兼容空MIME及通用二进制MIME。
- 历史文件公开地址、Word预览与物理删除统一使用解析后的上传根目录边界；上传文件名仅保留basename。
- 新增标准生产启动脚本与host、port、worker、日志级别环境配置；原本地reload命令保持可用。
- 增加Nginx反向代理、systemd及Windows守护参考模板，并明确标准输出、journal、WinSW及Nginx日志轮转边界。
- 每个HTTP响应增加安全请求ID；访问日志改为无查询、无正文、无文件名的单行JSON元数据。
- 上传和AI高成本路由增加可配置单进程限流；Nginx转发地址改为覆盖客户端输入，默认仍不信任代理头。
- 完成共享密钥、账号角色和企业网关鉴权方案决策文档；用户确认前保持当前开发环境无鉴权。
- 用户确认方案A后完成后端共享密钥保护；本地默认关闭，生产共享模式关闭FastAPI文档并保护业务API、原件和就绪探针。
- 普通API、上传、AI、报告和预览请求收口到统一客户端；PDF及Word转换PDF改为带认证Header读取Blob后交给PDF.js。
- Nginx部署模板关闭默认访问日志，请求审计统一由应用层安全归一化JSON日志承担。
- 保存共享访问凭据后自动重试项目列表与Dashboard，避免正确认证后仍保留旧401错误状态。

### Fixed

- 移除前端完整业务响应和Axios错误对象的控制台输出，避免客户分析数据或Authorization Header进入浏览器日志。
- 修复Uvicorn与Nginx默认访问日志仍记录 `/uploads/` 原始客户文件名的问题。
- 修复浏览器输入正确共享凭据后项目列表与Dashboard不自动恢复的问题。
- 修复凭据保存事件中项目列表可能漏掉重试的问题，改为由凭据启用状态统一恢复项目与Dashboard请求。
- 移除AI原始响应、DOCX正文、上传路径、项目名和风险关键词调试输出；删除异常响应不再回传原始异常文本。

- 修复 DeepSeek 客户端按需初始化改动中 `analyze_dashboard_management` 调用处的缩进错误。
- 修复“中低风险”同时计入中风险和低风险，以及损坏风险条目可能导致接口失败的问题。
- 修复上传解析、AI 或数据库提交失败后残留无主文件，以及同名上传覆盖旧文件的问题。
- 修复风险报告继续使用旧评分公式，导致报告评分、风险计数和页面不一致的问题。
- 修复 DOCX 历史记录被 PDF.js 加载并显示 `Failed to load PDF file` 的问题。
- 完成16页纯扫描 PDF 的真实 OCR/DeepSeek 隔离回归与坐标叠框视觉审计；无可靠原文匹配的风险保持不画框。
- 完成授权 DOCX 的真实 WPS/DeepSeek 隔离回归；61页版式转换、6项风险页码/高亮及 Word 报告关键字段通过审计。
- 修复模型忽略 PDF 表格字段的问题：新增结构化采购清单契约、分析页展示和报告摘要；真实样本4项采购清单及20个字段均可回溯原文。
- 增加损坏PDF、损坏DOCX和不支持扩展名的真实字节级异常回归；在WPS/OCR/DeepSeek前拒绝并验证文件、数据库零残留。
- PDF解析改用内存字节流，避免PyMuPDF处理结构损坏文件时在Windows残留上传文件句柄。
- 完成V1真实标书回归总报告，汇总6份授权业务文档、216页、35项风险、异常输入、修复记录和P3前已知限制。
- 修复旧版 `.doc` 被当作不支持内容继续分析、无法提取正文的问题。
- 扫描 PDF 不再以空正文继续分析；OCR 未配置、云服务失败及页面超限返回可识别错误并清理临时上传。
- 修复历史 `backend/.env` 为空时遮蔽仓库根目录百度 OCR 配置、导致错误返回“未配置”的问题。
- 修复 DOC/DOCX 原按字符模拟分页导致预览页数与 Microsoft Word/WPS 原文件不一致、页面下方缺少页码的问题。

### Security

- 修复当前Python环境审计发现的9条依赖漏洞记录；Python复扫和前端116个生产依赖审计均为0个已知漏洞。
- 移除 DeepSeek API Key 片段日志，明确用户数据库、上传件和报告的保护规则。
- AI 错误响应和日志不再返回原始上游异常文本，避免潜在密钥或供应商详情泄露。
- 忽略本地真实回归样本和本地预期覆盖文件，禁止自动把现有上传件视为已授权测试集。
- `.env.example` 与安全说明补充持久化目录和生产 CORS 白名单配置，禁止生产环境使用通配来源。
- 超限上传在文档解析、Office转换、OCR和AI调用前返回413并清理临时状态；安全说明要求反向代理上限不高于应用上限。
- 明确MIME或文件签名不一致的上传在任何外部处理前返回415并保持文件、数据库零残留。
- 越界历史路径不再公开、转换或删除；删除异常记录时只清理数据库状态并保留目录外文件。
- 新增数据库、上传目录和报告目录就绪检查；依赖异常时就绪接口返回503且生产入口拒绝启动。
- 完成隔离类生产两次启动、四个HTTP接口、端口关闭和SQLite重启持久化验证；模板未自动安装到系统。

### Known issues

- 前端生产构建存在大 chunk 警告。
- Ant Design `List` 组件存在下一主版本移除警告。
- 系统 Python 启动器不可用；项目虚拟环境可用，后续必须显式使用其解释器。
- FastAPI TestClient 当前提示 Starlette/httpx 兼容层弃用警告。
- 真实回归尚无 DOCX、纯扫描 PDF、复杂表格和异常文件样本；AI 引用不保证与原文逐字一致。
- Microsoft Word 主引擎因本机无授权未做真实验证；WPS 已验证可用。缺失字体、加密文件、Word 6/95 和复杂嵌入对象仍未验证。
- 百度 OCR 真实识别已由用户确认恢复；含位置风险框需对重新分析后的扫描件执行视觉对齐验收，旧记录不含坐标。
