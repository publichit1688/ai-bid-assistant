# 项目状态

## 2026-09-22 当前12文件本地补丁提交授权

- 负责人明确授权按LOCAL_PATCH_HANDOFF.md当前12文件建立本地补丁提交。提交前29项Node复验、秘密扫描及差异格式检查通过；业务代码不再修改，沿用已记录的lint/build、预览契约及合成浏览器验证。
- 基线4cc85ed、版本1.5.2保持；本记录随补丁提交，最终提交身份以Git回执为准。完成后停止，不创建标签、不推送、不部署。数据库、用户文件、夹具、dist、bundle和旧部署脚本全部排除并保留；下方等待授权为历史记录。

## 2026-09-22 12文件交付审阅完成

- 审阅入口固定阶段/错误枚举、GET与重定向边界及高亮接线。发现reader.cancel拒绝会覆盖原读取超时；新增测试先失败（HOME:BODY_FAILED），最小修复后保留HOME:TIMEOUT，清理仍尝试执行，不输出私密异常。
- 当前29项Node测试全部通过（入口8项），前端lint通过。只修改诊断脚本/测试及文档，未改变前端业务构建，本批不重复build、后端或云端验收；既有高亮浏览器证据仍按历史范围适用。
- 候选为10个已跟踪修改文件及2个新增高亮文件，共12文件，暂存区为空，HEAD仍4cc85ed。数据库、上传件、报告、夹具、dist、bundle和旧部署草稿排除并保留。下一步等待本次12文件本地提交授权，不再重复审阅或擅自部署。

## 2026-09-22 云端当前提交与入口一致性通过

- 腾讯云指定实例ins-goda9pyq一次性只读PowerShell检查于01:08:16–01:08:19完成，ExitCode 0，READ_ONLY_AUDIT_OK。未改已有命令、服务、配置或数据。
- 当前HEAD为4cc85ed5be1e583977fc4fbbedcc2391c6ae9dce；git diff --quiet HEAD通过，已跟踪文件与提交一致（不代表未跟踪/忽略文件或运行中后端进程身份核验）。
- frontend/dist/index.html引用index-CDm_oRGG.js；127.0.0.1:4173返回的首页与入口JS内容分别等于磁盘对应文件。入口JS SHA-256为8304F70479A77AD6DA25A9EE1A5837AF8C6E7B0F0D229D1BEB1524D87883F2B2。
- 本批覆盖已跟踪源码和首页/入口JS，不宣称所有拆分资源、依赖、代理配置或进程均完成审计。没有发现已跟踪代码漏部署；最新高亮补丁仍仅本地未提交。下一步审阅新增入口诊断及12文件交付清单，后续提交/部署仍需相应授权。

## 2026-09-21 入口诊断增强与实时复验通过

- 只读入口检查新增INPUT/HOME/ASSET/FILES固定阶段和脱敏错误码；保持GET白名单、超时、大小限制、重定向拒绝和证书验证，不输出原始异常、URL、响应或项目名称。未知错误只输出固定枚举。
- 7项入口脚本测试、前端lint通过；沙箱外实测退出0，首页、固定 `/assets/index-CDm_oRGG.js` 和项目列表契约通过，projectCount=2。本次入口失败阻塞解除，但前次失败根因未查明，不声称网络已永久恢复。
- 服务器源码/Git差异/托管目录逐文件核对仍未完成。当前累计候选增至12文件，需新增诊断审阅及对应提交授权；未提交、部署或修改正式数据。本批不重复既有业务全量测试。

## 2026-09-21 部署一致性审计与实时入口复核

- 历史执行 `inv-r8vt4c0rnc` 日志确认部署提交 `4cc85ed5be1e583977fc4fbbedcc2391c6ae9dce`，lint/build完成，构建入口 `index-CDm_oRGG.js` 与此前实际线上读取一致；不是当前服务器逐文件校验。日志开头存在 `Continue=Stop` 错误但继续运行，后续部署必须复核错误终止策略，不能仅凭ExitCode 0认定无错误。
- 本地HEAD仍4cc85ed；最新高亮修复未提交、未部署，本地dist入口为 `index-BA7-yI06.js`。不能把本地已修复等同于线上已修复，也没有证据证明上次部署漏文件。
- 本批沙箱外只读Tailscale查询：BackendState=Running、SelfOnline=true，按正式DNS精确匹配唯一目标，Online/Active均true。只表示客户端报告节点在线，不代表应用健康。
- 沙箱外完整入口GET检查退出码1，未取得成功结果；脚本统一错误信息不能区分网络、资源或API契约失败，不推断根因。此前一次完整检查成功属于历史证据，不能覆盖本次失败。服务器当前HEAD、源码差异和托管目录逐文件核对仍待完成。
- 本批仅同步审计文档，不改业务代码、版本、提交、部署、服务、网络或数据；不调用外部分析服务。

## 2026-09-21 高亮修复交互补验及交付审阅

- 本地Chrome只读合成1280×800：PDF四侧栏组合高亮保持，2→1→2页高亮数量1→0→1；切换Word清除旧高亮，Word映射第2页、翻页清除/恢复、右折叠和双折叠均通过。Word左折叠沿用上一批实测；视口恢复、临时页关闭、服务停止。
- 审阅代码仅改变文本层高亮生命周期，保留OCR框/风险原数据和逐span匹配边界。LOCAL_PATCH_HANDOFF.md列明当前10文件，含累计文档和既有未提交测试修正；数据/夹具/bundle/旧部署脚本排除保留。
- 业务代码本批未再修改，沿用上一批26项Node、lint/build、6项后端通过结果。当前等待新补丁本地提交授权；版本仍1.5.2，HEAD为4cc85ed，线上修复尚未部署。

## 2026-09-21 缩放高亮本地修复通过

- 根因：App双重定时器仅在点击后修改旧文本节点，PDF缩放重建文本层后未恢复。移除定时器与全局DOM查询，在DocumentPreview文本层渲染完成及活动风险变化时，对当前容器重施高亮，使用Word渲染页码限定目标页。
- 新增3项Node回归（接线修复前失败），合计26项通过；lint/build通过，既有大chunk警告保留；后端预览契约6 passed、1 warning。
- 本地只读合成Chrome 1280×800：Word定位第2页，折叠左栏缩放至700px后高亮元素仍1个、原文一致，截图橙色高亮保持。视口恢复、临时页关闭。未部署，线上问题仍待交付；下一批补PDF/切页/其他侧栏组合回归并审阅补丁。

## 2026-09-21 正式Word 1280布局及高亮问题

- Auth Word Project在1280×800完成双展开、左折叠、双折叠、右折叠截图与DOM验证：页面宽/scrollWidth均1280，画布/父容器双展开495/495、其余700/700；始终保持第2页/共2页，恢复右栏后详情仍第2页。
- 点击风险后双展开截图有橙色文字高亮；折叠左栏导致画布缩放后，后续三个组合截图文字仍在但橙色高亮消失。仅确认现象，尚未定位是否与文本层重建或高亮生命周期有关；不标记整体验收通过。
- 下一批优先本地复现并定位缩放后高亮丢失，再恢复其他尺寸验收。仅使用预生成Word合成夹具，不代表真实Office转换通过。视口已恢复、临时页已关闭，无AI调用或业务代码/部署变更。

## 2026-09-21 正式PDF 1280响应式小批验收

- Chrome旧页控制返回Debugger unattached，新建临时正式入口标签页恢复；历史2项目、Dashboard风险2/平均86正常加载。
- Auth PDF Project在1280×800完成双展开、左折叠、双折叠、右折叠四组合截图检查，标题导航与第一页可见；DOM页面宽1280、scrollWidth1280，无页面横向溢出。PDF画布/父容器双展开495/495，其余三组合700/700。
- 本批仅覆盖PDF第一页此尺寸布局，不替代风险高亮、Word、其他尺寸及冷加载耗时验收。视口已reset，临时标签已关闭，原用户标签保留；未点击AI或数据写操作。


## 2026-09-21 全量回归收尾及剩余验收边界

- 补录上一批已完成的全量 pytest：178 passed、1 skipped、1 warning，耗时93.56秒；使用 backend 虚拟环境及独立目录 `.pytest-auto-stability-full-20260920`。本批没有重复执行全量测试。
- 已核对跳过项为 `test_backup_refuses_overwrite_and_symlinks` 中创建符号链接受 Windows 权限限制；警告为 Starlette/httpx 弃用提示。
- 负责人首页 HEAD 200 是该次入口可达证据，不能证明当前持续可达，也不能替代首页正文、指定 JS 资源与 `/api/files` 的完整脚本验证；此前“终端检查解除”仅限首页探测，完整自动检查仍未补验。
- P6-31C仍未完成。下一批优先检查Chrome连接并补验正式合成PDF/Word响应式矩阵；随后按证据缺口处理冷加载测量、云端报告内容/版式和真实Word转换。没有代码变化或新失败时，不重复全量测试来替代这些验收。


## 2026-09-20 预览边界回归复跑

- 首次从仓库根目录运行测试时因测试夹具需要 `backend` 工作目录而出现 `ModuleNotFoundError: app`；未修改测试或业务代码。
- 按项目约定切换到 `backend` 并使用 `.venv\Scripts\python.exe` 后，`tests/test_frontend_preview_boundary.py` 通过：6 passed、1 warning。
- 本批仅验证本地预览契约，不改线上服务、数据库、版本或部署。

## 2026-09-20 稳定观察复核结果

- 本轮只读 `curl.exe -I` 自动复核在当前执行环境被 Windows Schannel 返回 `SEC_E_NO_CREDENTIALS (0x8009030E)` 阻塞；未关闭证书校验、未调整代理或网络设置。
- 该环境错误不代表线上入口返回 5xx。负责人此前在办公电脑 PowerShell 已取得正式入口 `HTTP/1.1 200 OK` 和 `Server: Caddy`，该成功证据仍为最新有效线上验证。
- 本批未触发登录、AI、上传、导出、写接口、代码、数据、版本、标签、推送或部署变更。

## 2026-09-20 终端 HTTPS 检查解除

- 负责人在办公电脑 PowerShell 中执行 `curl.exe -I https://172-30-0-5.tailb5cf3d.ts.net/`，返回 `HTTP/1.1 200 OK`、`Content-Type: text/html; charset=utf-8` 和 `Server: Caddy`。
- 该结果证明正式 Tailscale HTTPS 入口可从终端访问；此前 Node 脚本的 PowerShell TLS/代理链阻塞不再作为当前线上入口阻塞。
- 未关闭证书校验、未修改代理、服务器配置、代码、数据库、版本、标签或部署状态；本批仅同步验证记录。

## 2026-09-20 正式 Word 风险页码复验

- Chrome 只读打开 `Auth Word Project`，预览从第1页定位到第2页；预览页码、风险详情页码和原文 `AUTH WORD RISK PAGE 2` 一致。
- 未触发重新分析、AI摘要、上传、导出或删除；正式合成夹具和线上数据保持不变。

## 2026-09-20 线上入口稳定观察

- Chrome 只读复核正式 Tailscale HTTPS 入口成功，当前入口资源为 `assets/index-CDm_oRGG.js`，历史项目2个、Dashboard数据正常。
- 本机 Node 入口检查因 PowerShell TLS/代理链认证失败而未完成；未关闭证书校验、未修改代理或服务器配置，不能将该终端失败解释为线上故障。

## 2026-09-20 前端生产构建复跑

- `npm run build` 成功，3771 个模块转换完成；产物可生成，保留既有大 chunk 警告。
- 本批仅复跑构建门禁，未修改代码、版本、数据库或线上服务。

## 2026-09-20 全量后端回归通过

- 使用独立 pytest 临时目录完成全量后端回归：178 passed、1 skipped、1 warning（Windows 备份恢复测试按环境跳过）。
- 秘密扫描与 `git diff --check` 通过；未触碰正式数据库、上传目录或线上服务。

## 2026-09-20 后端回归契约修复

- 发现 `test_frontend_preview_boundary.py` 仍断言旧的 App 内联页码边界表达式；现已改为校验统一 `riskPreviewPage.js` 解析器及其页数上限逻辑。
- 相关后端回归20项通过，秘密扫描和 `git diff --check` 通过；未修改业务运行逻辑或线上配置。

## 2026-09-20 部署后本地门禁复跑

- 前端关键回归 23 项全部通过，`npm run lint` 通过；未修改业务代码、数据库或线上配置。
- 本批仅确认 `4cc85ed` 部署后的本地工作区仍可复验，线上页面回归证据保持不变。

## 2026-09-20 线上页码修复部署完成

- 固定提交 `4cc85ed5be1e583977fc4fbbedcc2391c6ae9dce` 已通过腾讯云自动化助手部署到 `ins-goda9pyq`（执行 ID `inv-r8vt4c0rnc`，ExitCode 0，耗时101秒）。部署前完成运行时备份，未改数据库、上传文件、端口或版本标签。
- 线上 Tailscale HTTPS 页面已复核：Word 预览显示第2页/共2页，风险列表与详情均显示第2页，合成夹具数据正常。原有 `6427d83` 命令内容已恢复。
- 本地版本仍为1.5.2；本批不推送、不创建标签、不部署其他提交。后续进入线上回归观察与文档收尾。

## 2026-09-20 页码补丁本地提交授权

负责人已明确授权LOCAL_PATCH_HANDOFF.md当前清单10文件的本地补丁提交，版本保持1.5.2。本记录随补丁保存，最终提交身份及成功状态以Git日志和执行回执为准；下方等待授权为历史记录。提交后停止，不创建标签、不推送、不部署。保留全部数据、夹具、旧部署脚本及bundle，不纳入本次提交；线上6427d83尚未包含本修复，后续同步或部署需对应明确授权。

## 2026-09-20 页码补丁审阅完成，等待本地提交授权

- 审阅页码解析、App展示/导航接线、预览映射清空与测试边界，本补丁范围未发现新增阻断项。23项Node测试、lint/build复跑通过，既有大chunk警告保留。本批未复跑浏览器，沿用上一批隔离UI证据。
- docs/LOCAL_PATCH_HANDOFF.md明确当前10文件候选，含累计部署/入口恢复/UI验收记录；数据库、上传/报告、合成夹具、dist、bundle和固定旧提交部署脚本排除并保留。
- HEAD6427d83、版本1.5.2、暂存区为空；未创建提交、标签、推送或部署。线上页码问题尚未修复。下一步等待负责人明确授权这10文件本地补丁提交，不重复审阅，不沿用旧11文件授权；后续部署需另行授权。

## 2026-09-20 页码修复隔离浏览器验收通过

- 新增frontend/scripts/preview-risk-pages.mjs，仅回环5176、合成API、读取既有合成PDF；所有非GET请求拒绝，无后端/数据库/AI或外部代理。
- Chrome实测Word原页1映射2：点击前列表第2页，点击后预览/详情第2页；切换PDF无Word映射仍定位原始第2页。使用已有预生成文件，不代表真实Office转换。
- 23项Node测试、lint/build通过（既有大chunk警告保留）。临时服务停止，5176无监听，临时标签页关闭，夹具保留。
- 下一批审阅页码修复及累计记录，整理逐文件交付清单；未授权创建本批新提交、推送或部署，线上6427d83不变。

## 2026-09-20 Word风险列表页码本地修复

- 确认右侧风险列表直接显示item.page，而点击定位使用wordPreviewRiskPages。新增riskPreviewPage.js统一渲染页码解析，列表和点击导航共用；原始risk.page及报告逻辑不改动。
- 新增4项回归：UI接线、Word原页1映射2且数据不变、PDF/缺映射回退、非法值与页数边界。接线回归修复前失败，修复后合计23项Node测试通过；lint/build通过，既有大chunk警告保留。
- 本批仅本地代码和文档，未提交/部署/推送，不改版本或正式数据。线上6427d83仍存在列表页码问题，不能声称线上已修复。
- 下一批使用隔离合成环境补验列表第2页、点击详情第2页和PDF回归，再审阅交付；本批未做修复后的浏览器验收，真实Word转换仍未验收。

## 2026-09-20 正式合成Word预览部分通过，页码显示待修复

- Auth Word Project已有版式预览显示2页；评分92分、中风险1项、扣8分。点击风险定位至第2页，截图确认AUTH WORD RISK PAGE 2文字高亮、详情页码第2页及原文一致。
- 新发现一致性问题：风险列表仍显示第1页，而定位后的详情/渲染页为第2页。本批不标记全部通过。初步源码检索显示定位使用wordPreviewRiskPages映射，另有直接risk.page展示，尚需定位具体组件和最小回归修复。
- 下一批优先本地复现并修复Word风险列表与渲染页码不一致，测试通过后再整理交付；不直接修改或重新部署正式环境。暂停推进其他验收任务。
- 本地19项既有Node测试通过，但不覆盖本次页码问题；本批仅浏览器验证和文档，无AI调用、重新分析、删除、提交、推送或部署。预生成夹具不等于真实Office转换通过。页面及数据保留。

## 2026-09-20 正式合成PDF预览与风险定位通过

- 复用Chrome正式页面，打开Auth PDF Project历史分析，预览显示2页；评分80分、高风险1项、扣20分，与历史列表一致。
- 点击风险定位从第1页跳至第2页；截图确认红色风险框覆盖AUTH REGRESSION PDF RISK PAGE 2，右侧风险详情页码与原文一致。手动上一页回第1页、下一页回第2页均成功，边界按钮禁用正确。
- 本地19项Node测试通过；本批未重新分析、调用AI、上传/导出/删除、部署或修改服务。只做当前窗口PDF小阶段，不替代正式响应式全矩阵、真实Word转换和报告版式验收。
- 下一批验收正式合成Word已有预览与风险定位（预生成PDF不能视为真实Office转换）；保留当前浏览器页面、全部数据与备份。本次只更新文档，不创建提交或标签、不推送。

## 2026-09-20 正式Dashboard冷启动小批验收

- Chrome正式新页最终正常渲染：Auth PDF Project 80分/1风险、Auth Word Project 92分/1风险；Dashboard项目2、风险2、平均86，历史读取未报错。
- 手动摘要按钮加载阶段禁用，统计完成后可用；摘要保持空状态并显示显式触发提示。本批未点击生成，未上传/删除/导出或修改服务。未采集正式网络请求计数，不以空摘要证明零POST；本地实际处理函数测试覆盖自动加载零POST。
- 新页导航等待曾超时，随后经历空页面与统计加载才完成；未取得可靠加载耗时指标，冷启动性能仍待专项测量。一次控制台错误/警告采样为空，不代表全部会话无错误。
- 本地19项Node回归通过；本批无业务代码改动，仅更新状态记录。下一批继续正式合成PDF风险定位小阶段，真实Word转换、报告版式及完整矩阵仍待验收。版本/提交/部署保持不变。

## 2026-09-20 负责人恢复后入口复验通过

- 负责人反馈已经恢复后，本机实时检查BackendState=Running，目标云端节点Online=true、Active=true。
- 办公电脑只读check-deployed-entry.mjs退出码0：正式HTTPS首页、固定/assets/index-wLcyXqAL.js和/api/files均通过状态/类型/内容校验，projectCount=2。入口连通性阻塞解除；未读取业务正文或调用AI。
- 本批未进行云端配置修改、服务重启或重复部署；具体离线根因与负责人恢复操作未核实，不猜测。6427d83部署记录保持，下一批继续正式页面安全冷启动与合成UI验收，不能将HTTP验证替代完整交互/真实Word/报告版式验收。
- 本次只更新本地状态文档，不提交、不推送、不打标签，保留全部数据与备份。下方离线记录为历史状态。

## 2026-09-20 受控链路定位：目标Tailscale节点离线

- 本机BackendState=Running；按正式域名精确筛选目标节点100.83.20.96，Online=false、Active=false、CurAddr为空，最后在线2026-09-19 13:26:44 UTC（北京时间21:26:44）。这是办公客户端报告，尚未由云端核实原因。
- 三次Tailscale ping（每次5秒）均超时，退出码1。当前证据指向受控节点连通性，不能据此断言云主机停机、应用损坏或代理是根因；不重复部署。
- 浏览器实时列表仅有空的内置浏览器，无Chrome连接。下一步需恢复Chrome扩展连接并打开腾讯云，再只读检查ins-goda9pyq运行状态、云端Tailscale服务/认证/出口及本机4173、8000健康。不要仅反复重试办公HTTPS。
- 本批仅诊断与文档，无网络设置、服务重启、代码、数据、版本、提交、标签或推送变更。6427d83部署成功记录保持，当前入口验收仍阻塞；所有备份/夹具保留。

## 2026-09-20 核实6427d83部署成功，办公入口补验超时

- 已获负责人部署6427d83授权；本次只读取既有执行inv-s8p002g7tq，未重复执行。目标ins-goda9pyq，命令cmd-bkf9vmwh；2026-09-15 16:23:33至16:24:55，82秒，命令成功/ExitCode 0。
- 云端日志确认固定提交6427d83b0660279303e888cff68638c644482597；bundle/Git及运行数据备份通过，19项Node测试、lint/build、部署后健康与回环监听检查通过。保留既有大chunk等构建提示。
- 备份路径C:/AI-Bid-Backups/pre-6427d83-02e56e5610f04692b14b30a2861bd3c6；入口资源/assets/index-wLcyXqAL.js。以上为部署时回执，不等于今日实时服务器检查。
- 今日办公电脑只读HTTPS检查在沙箱内外均失败；独立首页请求10秒超时。Tailscale服务及BackendState均为Running，不能据此认定服务器离线或部署失败。未关闭证书验证、未修改代理/安全组/网络设置。
- 下一小批先只读定位受控入口连通性，再验证固定资源和历史JSON，随后安全冷启动UI。正式完整UI、真实Word转换和报告版式仍待验收；不点击AI生成、不删除夹具，不重复部署。
- 版本仍1.5.2；本次仅更新本地记录，未提交、推送、创建标签或调用外部分析服务。下方为历史状态。

## 2026-09-15 手动摘要补丁本地提交授权

负责人明确授权LOCAL_PATCH_HANDOFF.md清单内11文件本地补丁提交。范围为手动摘要、统计乱序保护、测试工具及累计状态文档；版本保持1.5.2。本授权记录随补丁保存，最终提交身份和完成结果以Git日志及执行回执为准。数据库、上传、报告、测试数据、bundle和旧部署脚本排除并保留。不创建标签、不推送、不部署；后续云端仍需对应新提交的独立授权，下方等待授权文字为历史状态。

## 2026-09-15 补丁审阅及统计请求乱序修复

- 审阅复现快速切换周期时旧统计请求提前解除loading并可能覆盖新统计的问题；新增回归先失败，App增加dashboardRequestRef，只有最新请求可写数据、错误和loading。旧摘要失效机制保持。
- 新增2项乱序测试覆盖旧请求先完成、晚成功及晚失败；合计19项Node测试、lint/build通过，既有大chunk提示保留。无外部AI/服务器操作，本批未复跑浏览器，前批UI证据与新增模拟时序验证分别记录。
- LOCAL_PATCH_HANDOFF.md列明11文件交付候选和排除项，包含此前未提交文档/终端工具。HEAD68888a8、版本1.5.2、暂存区为空。审阅批次完成，下一步等待明确本地补丁提交授权，不重复审阅或自动部署。

## 2026-09-15 手动摘要隔离浏览器验收通过

- 新增frontend/scripts/preview-manual-summary.mjs：Vite仅127.0.0.1:5175提供合成统计/摘要，首个摘要请求延迟后502、第二次成功；无真实后端、数据库、凭据或外部AI。
- Chrome实测首次加载summaryRequests=0；点击显示loading及分析提示；合成失败后显示重试消息且按钮恢复；重试显示四段合成摘要。再次点击、切换30天（清空摘要）、返回7天（缓存恢复）后请求仍为2。截图确认按钮/失败提示未裁切。
- 17项Node测试、lint/build通过（既有大chunk提示）；本批不修改业务代码、不提交或部署。正式68888a8尚未改变。下一步审阅手动摘要补丁与交付范围，真实Word和正式完整UI矩阵仍待验收。

## 2026-09-15 AI管理摘要手动触发（本地）

- 负责人同意本地修改和测试、不部署。App.jsx统计加载只复用匹配的页面内缓存，不再自动POST摘要；生成按钮明确手动触发，后端数据库缓存保持不变（按钮请求时使用）。
- 增加同步重复点击保护，统计刷新使旧摘要请求失效；加载/失败统计禁止生成，摘要网络错误提示可重试，旧响应不覆盖当前展示。
- 新增test-manual-summary.mjs，使用实际App处理函数与模拟API验证5条路径：自动加载零POST、手动及缓存、重复点击/迟到响应、失败重试、不可用统计。连同既有测试共17 passed，lint/build通过，既有大chunk提示保留；未做浏览器视觉验收或真实AI请求。
- 本批无提交、版本变更、标签、推送或部署。正式环境仍是68888a8，自动摘要行为尚未改变；下一批先本地隔离合成UI验证按钮/冷启动，再审阅补丁，云端变更另行授权。

## 2026-09-15 正式UI冷启动验收前置限制

- 当前Chrome扩展已连接，但仅有腾讯云主页，旧标书助手标签页不存在；并非浏览器连接失败。
- 已核查App.jsx：trendDays初始化触发loadDashboard，随后自动loadAiManagementSummary并POST /api/dashboard/ai-summary。后端api/dashboard.py缓存未命中时调用analyze_dashboard_management并写缓存；不能假设已有缓存保证零外部调用。
- 因当前验收禁止真实AI及正式数据写入，本批未新开正式应用页，也未更改缓存、令牌、服务或浏览器规则。需要先明确安全冷启动方案；建议本地开发AI摘要手动触发并保留缓存，再经独立授权部署，或另行授权受限的合成统计摘要调用。UI任务仍未完成。

## 2026-09-15 终端只读部署验收

- 新增frontend/scripts/check-deployed-entry.mjs及5项测试：仅GET首页、指定同源JS和/api/files，限制HTTPS、禁止重定向、30秒请求超时、响应大小上限及类型/列表校验；只输出资源名和项目数量，不输出正文或凭据。
- 正式HTTPS实测通过，入口/assets/index-DtzCARX0.js及JS内容有效，历史JSON保留2项目；不访问Dashboard摘要、预览生成或外部AI接口。
- Node合计12项通过，脚本语法检查、前端lint/build通过；构建保留既有大chunk提示。本批仅本地验收工具和文档，无提交、推送、标签、部署或正式数据写入。正式合成UI/真实Word转换及完整版式仍待验收。

## 2026-09-14 68888a8腾讯云部署完成

- 固定提交68888a825fff708133d323936faecdb2fa136284已部署；自动化助手cmd-hmvm6xbl、执行inv-s8n00m0mqh，命令成功/ExitCode 0，22:11:55至22:13:33，共98秒，目标ins-goda9pyq。
- 使用18,475字节增量Git bundle嵌入31,301字节PowerShell命令直接传输，未创建COS或公开文件。SHA-256为30E281F5B9A9DB1D5205266AB43A0C1614DEC37D6D7A98C33A2030E114F00EAA；保存后读回内容一致，云端SHA/Git/11文件白名单检查通过。
- 旧HEAD/健康检查通过，运行快照校验9文件，备份C:/AI-Bid-Backups/pre-68888a8-44d1583d07e34232b02b4d4090805e42保留运行数据与旧dist。云端Node7项、lint/build、切换后首页及JSON历史接口、8000/4173仅回环监听通过；未重启后端或改秘密配置。
- 办公电脑HTTPS首页200，入口/assets/index-DtzCARX0.js；历史接口200/application/json、2个项目。沙箱内TLS认证失败，沙箱外只读重试成功，未关闭证书验证。未打开Dashboard触发真实AI。
- 版本保持1.5.2，无远程推送、新标签、部署后额外提交或新公网端口；正式UI完整回归、真实Word转换及报告版式仍待验收。部署脚本和本批记录留在本地未提交，下方为历史状态。

## 2026-09-14 补丁部署已授权，等待腾讯云登录

- 本地补丁提交为68888a825fff708133d323936faecdb2fa136284；负责人明确选择部署腾讯云，未选择远程推送。目标固定此提交，版本保持1.5.2、不创建标签、不开放新端口；先备份预检，失败回滚，保护现有数据，不调用真实分析服务。
- 实际点击Chrome腾讯云控制台后跳转微信登录页，当前会话失效；尚未核验云端HEAD、备份或执行部署。旧deploy-8f130f9.ps1绑定旧提交和已删除COS地址，不能原样复用。
- 下一步由负责人完成登录后继续云端只读预检，再准备新提交传输与部署。未创建COS对象、未公开文件、未改服务器。本记录留在本地，未额外提交。

## 2026-09-14 本地补丁提交授权记录

负责人已明确授权将LOCAL_PATCH_HANDOFF.md清单内11个文件创建本地补丁提交；本记录随该提交保存，提交身份以Git日志为准。范围为标题栏响应式、非法响应白屏保护及测试/验收文档，版本保持1.5.2。数据库、样本、测试目录、bundle及旧部署脚本排除并保留。不创建标签、不推送、不部署；正式环境仍使用此前基线。后续远程同步或部署需要对应明确授权，下方等待授权文字为历史记录。

## 2026-09-14 补丁审阅完成，等待本地提交授权

- 审阅标题栏与响应校验补丁及测试覆盖，范围内无新增阻断项；明确4个前端代码/测试文件与7个文档文件的逐文件交付清单，见LOCAL_PATCH_HANDOFF.md。
- 本批前端7项测试、lint/build、秘密扫描及diff检查通过；未改应用代码，未复跑后端或浏览器矩阵。此前回归证据保持有效，但不替代正式环境验证。
- HEAD仍8f130f9、暂存区为空、版本1.5.2；数据库/样本/测试目录/bundle/旧部署脚本排除且保留。本地提交、标签、推送及部署均未执行。下一步等待明确本地提交授权，不继续重复审阅。

## 2026-09-14 PDF侧栏组合补齐及交付范围整理

- 复用隔离合成目录，PDF在1024宽度展开/左折叠/双折叠/右折叠/恢复的画布宽度依次407/571/700/651/407px；容器407/571/815/651/407px。全程第2页、1个风险框、预览未越界，无页面横向溢出。恢复截图确认风险详情第2页及80分保持。
- 本批Node7项、lint/build通过；大chunk及切换时TextLayer取消警告保持已知非阻塞项。浏览器尺寸已恢复，临时前后端停止，夹具保留。
- 新增LOCAL_PATCH_HANDOFF.md，明确应用补丁、历史部署脚本及测试数据边界；HEAD仍8f130f9，无提交/推送/部署。下一步只读补丁交付审阅，不扩大功能。

## 2026-09-14 隔离合成预览响应式补验完成

- 使用现有虚拟环境和prepare_auth_regression脚本，生成独立 `.browser-layout-20260914` 数据库/上传/报告目录；本机8014后端和5174前端仅回环运行，未访问正式数据。测试Key及本机9端口覆盖模型配置，OCR禁用。
- Chrome下Word/PDF双侧栏展开，1024/1280/1440/1920宽度均显示第2页、预览在容器内、无页面横向溢出；Word在1024宽度的左折叠/右折叠/双折叠也通过。Word黄色原文高亮、PDF红色风险框截图可见，风险详情页码一致。
- pytest预览边界/文档预览/报告契约14 passed、1条既有弃用警告；Node回归7/7。尺寸切换有TextLayer取消警告，稳定后正常；Dashboard自动摘要因测试模型地址不可达返回预期502，未调用真实服务。
- 本批只更新验收文档，没有新增业务代码、部署或提交。浏览器尺寸恢复，临时服务已停止，合成夹具保留。正式环境内容矩阵、真实Word转换和完整版式验收仍未完成。

## 2026-09-14 本机白屏已修复，标题栏本地复验通过

- 根因已复现：无后端的Vite静态预览将 `/api/files` 返回为200/text/html首页，前端直接写入files状态，触发 `TypeError: d.filter is not a function`。不是仅凭200判定资源正常，也未归因于代理。
- 新增项目数组/行对象和Dashboard对象校验，异常内容进入既有失败提示，不污染状态；同一静态环境刷新后页面正常渲染，项目及Dashboard失败提示可见，导航可操作。
- Node回归7/7、lint/build、秘密扫描及diff检查通过。Chrome实测1024宽侧栏组合和1280宽双侧栏展开：品牌/导航按钮在标题栏内、无页面横向溢出；1280中央600px，标题单行正常。视口已恢复。
- 上批标题栏补丁本地复验完成，仍未部署。仅验证无后端布局与错误降级，Word/PDF内容及正式完整尺寸矩阵待隔离合成回归。正式数据、服务、网络未改，无提交/推送/外部分析调用。

## 2026-09-14 标题栏响应式修复待视觉复验

- 正式Chrome截图（1330像素宽）复现双侧栏展开时品牌文字多行挤压、超出标题栏；不能视为响应式验收通过。
- 本地补丁按中央容器宽度切换紧凑导航，标题栏允许增高换行；纠正副标题类名误标在主标题，并补充导航中文可访问名称。未部署、提交或推送。
- lint/build通过（构建仍有既有大chunk提示），静态预览首页及入口JS均HTTP 200；Chrome和内置浏览器均停留空白，修复后的视觉复验未完成。下一步先诊断该预览阻塞，不进入其他任务；正式代码和数据未改。

## 2026-09-14 最新验收

- 正式Chrome页面合成Word报告页码补验通过：预览/详情第2页，实际下载DOCX风险表第2页、引用 `AUTH WORD RISK PAGE 2`、评分92分一致。新文件36,052字节，已只读检查正文并记录SHA-256，详见 `P6_31C_SYNTHETIC_REGRESSION.md`。此前报告页码不一致问题已关闭。
- 本批未修改应用代码、部署或数据配置；保留合成样本及全部报告。P6-31C剩余首次加载慢、正式入口响应式矩阵和真实Word转换/报告版式限制。

## 2026-09-13 当前状态：同源 API 修复已部署

- 腾讯云执行 `inv-r8kp2q08r0` 成功（ExitCode 0），正式目录为 `8f130f90505e6b53259e73937f021a8a0a8bf258`，版本仍为1.5.2；bundle SHA-256/Git身份、数据备份、前端构建、首页/历史接口及8000/4173回环监听检查通过。
- 备份：`C:/AI-Bid-Backups/pre-8f130f9-10fc74c970a848e7919f41952ced7ec5`；包含运行数据快照和旧dist。仅切换前端资源和既有提交，后端进程、秘密配置、数据库原件及合成夹具保持保留。
- 办公电脑HTTPS读取首页200，入口为 `assets/index-oM_1rAB-.js`；`/api/files` 200、2个项目。Chrome新标签经历较长白屏后加载完成：两个历史项目、Dashboard项目数2/风险数2/平均分86正常，无项目读取失败提示。同源API页面读取补验通过；首次加载慢和报告页码复验仍待完成。
- 临时 `release-8f130f9.bundle` COS对象及空桶 `ai-bid-deploy-20260912-1482509524` 均已删除；负责人完成微信验证后，2026-09-13控制台复核存储桶列表“共0项 / 暂无数据”，本批中转清理完成。其他本地bundle与云端备份均保留。
- 新增固定部署脚本 `deploy/windows/deploy-8f130f9.ps1`：预检失败不改运行资源，构建完成再切换，失败保留现场并验证回滚。两次路径检查失败均出现 `ROLLBACK_VERIFIED`；第二次发现控制台未保存更新，改用编辑器粘贴并读回已保存内容后第三次成功。错误SHELL命令已改名DO-NOT-RUN并替换为退出脚本。
- 本批本机API基址3项测试、PowerShell语法/混合分隔符检查和秘密扫描通过；云端构建保留既有大chunk提醒。未创建提交、标签或推送，未调用外部分析服务、未改网络设置。

更新时间：2026-09-13

## 2026-09-12 问题记录（同源修复已于次日部署）

- 实际下载报告（6）已找到，36,051字节，正文风险表仍为第1页而引用为第2页；上次仅点击导出并不能证明页码修复端到端通过。
- 新开正式入口显示项目/Dashboard读取失败；同源历史接口独立探测HTTP 200。线上入口资源 `index-BkCPqOEh.js` 的实际API常量为 `http://127.0.0.1:8000`，同时包含 `report_page`。确认生产构建遗漏API配置后回退到访问者电脑地址；旧标签页数据不能代表新部署状态。
- 本地 `frontend/src/api.js` 改为生产默认同源、开发保留回环地址、显式配置优先；修复提交 `8f130f9` 已于2026-09-13部署。
- 当前最高优先为修复正式前端构建配置并核对浏览器实际接口请求，随后重新导出合成Word报告验证第2页。P6-31C保持进行中；本批未改生产配置、数据或网络。
- 修复bundle上传和云端校验已完成；SHA-256为 `1934164396B1C789A7C2A572FB3F9D3AD6F83F0FA02A2CEA22A3B7B943F80EFA`。旧上传阻塞已解除。

更新时间：2026-09-12

## 先前正式部署记录

2026-09-12：正式环境已固定部署提交 `ad5acb5`（报告 Word 页码映射修复）。部署前备份已生成于 `C:\AI-Bid-Backups\pre-ad5acb5-r3-20260912-015103`；COS 中转 bundle 已完成 SHA-256 与 Git 完整性校验，云端前端构建、健康检查和回环监听门禁全部通过。后端 `8000`、前端 `4173` 仍仅监听回环地址，未新增公网端口。部署完成后临时 COS 对象及空桶已由负责人确认删除。

2026-09-12正式入口报告补验：在 `Auth Word Project` 页面点击“导出报告”后页面保持正常、未出现错误提示；浏览器扩展不捕获程序化 Blob 下载，因此本批不把浏览器点击单独视为文件落地证据。后端报告契约测试在正确工作目录下 `4 passed、1 warning`，页码映射优先级和缺失字段降级保持通过。

## 当前阶段

浏览器验收连接已恢复：受控入口可接管并已只读打开 `Auth PDF Project` 分析页；PDF两页预览、评分80分、1项风险和第2页风险定位可见。响应式矩阵、报告版式和真实Word转换验收仍未完成，不把本次只读检查扩大为完整验收。

2026-09-12风险定位补验：点击现有风险入口后预览跳至第2页，风险详情页码同步，红色半透明高亮框覆盖合成风险文字；未上传文件、调用AI或修改业务数据。

2026-09-12 Word预览补验：`Auth Word Project` 显示2页Word版式预览及本机办公软件转换标识，风险定位跳转第2页并同步详情页码；仅验证预生成夹具，真实Word/WPS转换仍待独立验收。

2026-09-12侧栏布局抽查：收起并恢复项目中心侧栏后，中央预览、顶部导航和右侧风险检查器均保持可见；只覆盖单一桌面视口，不替代完整响应式矩阵。

2026-09-12风险检查器布局抽查：收起并恢复右侧风险检查器后，中央Word预览扩展，项目中心和顶部导航保持可见；只覆盖单一桌面视口，不替代完整响应式矩阵。

2026-09-12报告页码一致性修复已部署：导出请求携带Word渲染页码映射，后端报告优先使用 `report_page` 并兼容回退 `page`；报告契约4 passed、前端lint/build和本机LibreOffice 2页视觉渲染通过。固定提交 `ad5acb5` 已部署到腾讯云正式环境，云端报告内容待合成回归复验。

2026-09-12项目对比报告视觉复核：本机LibreOffice逐页检查5页合成对比报告，标题、表格、风险和最终建议均无裁切/重叠；第5页留白较多，作为非阻塞版式优化项保留。

智能编标浏览器只读回归：`Auth PDF Project` 工作台可加载目录、评分点覆盖、来源页码、审核状态和响应材料空状态；当前修订1，未改变数据。

浏览器工作台入口复核：受控HTTPS入口可打开智能编标工作台，空项目选择状态、禁用操作和“不会自动生成整本标书”提示均正常。

测试流程文档已补充Windows临时目录权限故障的安全重跑方式；不改变生产启动、数据目录或门禁逻辑。

状态标记已校准：P6-31C处于进行中而非未开始；本地功能和门禁证据已齐，剩余事项依赖正式环境或跨节点网络验证。

AI调用安全复核：提示词、AI错误降级和Dashboard缓存测试20 passed、1 warning；真实DeepSeek保持未调用，异常响应契约通过。

OCR/扫描文档稳定性复核：相关测试10 passed、1 warning；OCR仍保持显式配置才启用，低文字页降级和预览页码边界保持通过。

项目中心/版本稳定性复核：工作区 API、配置和版本测试14 passed、1 warning；历史文件入口和正式版本元数据契约保持正常。

核心分析稳定性复核：核心 API、风险评分、风险定位和回归模板测试33 passed、1 warning；风险计数、评分和页码定位契约保持通过。

部署稳定性复核：生产启动、部署资源和运行时检查10 passed、1 warning；现有回环监听、健康检查与部署模板契约保持通过。

正式发布门禁复核：`scripts/verify_release.ps1` 全部通过，后端177 passed、1 skipped，Python依赖无已知漏洞，前端 lint/build 与 npm生产依赖审计通过；未推送、未打标签、未部署。

鉴权与运行安全复核：鉴权、速率限制、请求 ID/日志脱敏测试17 passed、1 warning；安全边界保持通过。

上传与文档输入稳定性复核：相关测试28 passed、1 warning，扩展名/MIME/大小/路径和失败清理边界保持通过；未调用外部OCR或AI。

数据保护复核：备份/恢复及存储路径安全测试13 passed、1 skipped、1 warning；跳过项为Windows符号链接权限能力限制，未影响其他数据保护断言。

稳定性复核：Dashboard缓存、AI异常降级和上传事务测试26 passed、1 warning；失败路径未发现新的半成品数据问题。

V1.5智能编标工作台稳定性复核：设计、评分点、映射、响应材料、迁移及闭环测试17 passed、1 warning，既有可审计闭环保持通过。

前端关键契约复核：预览、品牌和鉴权测试13 passed、1 warning；拆包后的入口与核心资源契约保持正常。

报告导出稳定性复核：`test_report_contract.py` 3 passed、1 warning，报告字段和 DOCX 生成契约保持通过。未调用外部分析服务；真实报告版式仍需正式环境人工抽查。

Word预览稳定性复核：真实转换链路相关单元/接口边界测试7 passed、1 warning；覆盖Word失败回退WPS、缓存预览PDF和DOCX/PDF预览分流。真实办公文件跨环境版式差异仍属于部署回归限制。

后端稳定性复核：使用独立临时目录运行完整 pytest，177 passed、1 skipped、1 warning；默认 pytest 临时目录曾遇到Windows权限阻塞，已通过显式 basetemp 绕开。跳过项为符号链接权限能力限制。

后端稳定性复核：使用现有 `backend/.venv` 和独立临时目录运行完整 pytest，结果 177 passed、1 skipped；默认临时目录的权限占用已识别为测试环境问题，不改变应用代码或用户数据。

前端稳定性复核：`npm run lint` 和 `npm run build` 通过；构建仅保留既有大 chunk 提示，未发现错误。当前继续优先处理受控入口网络/真实回归限制，不新增Dashboard拆包或业务功能。

2026-09-11云端只读核查：当前实例仅绑定一个安全组，且安全组只关联该实例；入站预览没有 UDP 规则，现有允许项为 TCP 3389、80、443（含一条已有 IPv6 全来源3389）。本批未新增规则、未开放端口、未修改云端设置。由于办公电脑 UDP 来源此前无法确认，Tailscale直连试验仍暂停。

最新回滚结论：单目标TUN排除已由用户撤销，4份配置哈希全部恢复至试验前备份；公网目标路由回到Mihomo，tailnet路由保持Tailscale，ChatGPT HTTPS 200/证书验证通过。Tailscale仍经DERP(lax)，946毫秒pong。本次试验安全收尾，原端到端加载问题未解决。

最新覆盖：单目标TUN排除经用户保存并已生效，配置对比确认仅预期项变化。目标走WLAN、tailnet走Tailscale、ChatGPT HTTPS 200/证书通过；但仍未直连且worker40秒下载超时。已按失败恢复边界请求用户删除该条排除；回滚尚未确认。正式代码、数据、云端安全组及端口未改。

2026-09-11补充：单目标TUN排除已获授权，完成Mihomo 1.19.29支持核对及办公电脑4份相关配置备份，备份与现配置哈希均一致。窗口操作兼容性及用户输入保护导致本批停在修改前，排除项尚未应用，目标路由仍为Mihomo。改动后的路由/Tailscale/ChatGPT联合验证未执行，不能标记修复完成；未改云端安全组、正式代码或数据。

本机路由只读检查已确认：目标公网IPv4走Mihomo TUN，tailnet地址走Tailscale，无活动Tailscale出口节点。系统HTTP代理关闭不能排除TUN接管，此前--noproxy回显不是物理直连证明。未修改任何网络设置；源地址仍未确认，UDP放行试验保持暂停。目标/32的TUN排除仅为待授权候选。

最新冷加载诊断：受限UDP直连试验已获授权；但本机UDP探测与HTTPS公网出口IPv4不同，未能确认指定目标UDP来源，按授权停止条件停在来源预检。未新增云端/本机规则，无需回滚，未改代理、正式代码或数据。故障未修复；下一步需确认是否只读调查本机分流路径。详见合成回归记录。

V1.0.0已完成本地功能冻结；V1.5智能编标工作台最小闭环已发布。备份原子性修复已提升为正式 `1.5.2`，`main` 与 annotated 标签 `v1.5.2` 已推送到 GitHub并完成远程哈希复核；固定正式提交 `4f01024` 已部署到腾讯云 Windows Server，并通过回环健康验收。

正式tailnet HTTPS入口已启用：后端只监听 `127.0.0.1:8000`，Caddy长期托管前端并仅监听 `127.0.0.1:4173`；共享令牌只保存在服务器秘密配置，由同源代理注入。Tailscale Serve HTTPS门禁已确认入口200，腾讯云公网安全组未修改。负责人已授权纯合成生产回归：备份原空库后，运行配置切到 `C:/AI-Bid-Data/p6-31c-synthetic` 独立数据目录（不是向原库插入记录），现有2条合成项目，原数据及夹具均保留。Chrome核心预览、风险定位、基础对比与报告下载通过；首次资源传输慢待查，真实Word转换未覆盖。证据见 `docs/P6_31C_SYNTHETIC_REGRESSION.md`。

## 已确认实现

受控入口冷加载排查：办公电脑经DERP(lax)，两次worker下载40秒超时；云端回环对照三次均完整1,369,825字节、1406～1539毫秒，执行 `inv-r8hd5g0stv` ExitCode 0。下一步检查服务器自身HTTPS及两端连通能力，区分Serve与跨节点路径；问题未修复，不将Clash或中继认定为唯一原因。

- 后端为 FastAPI，使用 SQLite/SQLAlchemy 保存标书、分析结果和 Dashboard AI 摘要缓存。
- 前端为 React/Vite，主要 UI 使用 Ant Design，图表使用 ECharts。
- 已存在上传分析、历史文件/项目中心、风险检查器、风险定位高亮、响应式 PDF 区域、Dashboard 预警穿透、项目对比、风险报告及对比报告导出。
- DOCX/DOC 不再使用按字符模拟分页：默认由 Microsoft Word 转换为 PDF，Word 不可用或转换失败时自动回退 WPS Office；分析、预览和风险定位共用转换后的真实页面。
- Word 预览接口返回转换 PDF 的真实页数、实际渲染引擎和历史风险页码映射；前端每页下方显示“第 X 页 / 共 Y 页”，历史模拟页码会按原文命中重新映射到渲染页。
- 本机已安装并自动发现 WPS Office x64 12.1.0.28488；两页合成 DOCX、三页本地 Word 报告、61页历史 DOCX 和64页历史 DOC 均已完成真实转换验证，界面页码、下一页、风险跳页和文字层高亮通过本地浏览器检查。
- 扫描 PDF 默认接入百度通用文字识别（标准含位置版）：先本地检测文字层，仅将缺少有效文字的页面提交 OCR，保留原 PDF 页码，并将命中文字坐标叠加为响应式风险高亮框。
- Dashboard AI 管理摘要具备前端缓存逻辑，后端存在 `dashboard_ai_cache` 持久化模型及接口逻辑。
- 前端生产构建可完成。
- 前端 ESLint 已清零，Dashboard 与标书分析入口已完成本地浏览器渲染抽查。
- 已移除 Vite 示例全局样式，统一页面背景、Content 间距与内容最大宽度；PDF 会根据中央容器实际宽度在 700px 上限内动态缩放。
- Dashboard、标书分析和项目对比的顶层标题已统一层级、Ant Design 图标、卡片圆角与垂直间距。
- 项目中心、Dashboard、风险检查器和项目对比已统一加载、错误和空状态；项目列表与 Dashboard 请求失败会保留 toast 并显示页面内 Alert。
- 已完成 1024/1280/1440/1920px 桌面响应式矩阵：窄桌面侧栏收窄、Header 压缩、Dashboard 图表单列稳定显示，Content、PDF 和项目对比入口无横向溢出。
- 已完成 V1 受保护功能 UI 回归：Dashboard 周期与预警穿透、项目中心搜索/筛选、风险定位高亮、项目选择/取消对比和报告生成均通过；详细证据见 `docs/V1_UI_REGRESSION.md`。

## 本轮建立的工程基线

- 建立 AGENTS、ROADMAP、TASKS、TESTING、ACTIVE_TASK、自动开发工作流、环境变量示例、安全说明和 CHANGELOG。
- DeepSeek 客户端改为按需初始化；服务启动不再因缺少 Key 在模块导入阶段失败，也不再打印 Key 片段。
- 增加后端依赖清单，便于重建开发环境。
- 建立后端 pytest 最小回归基线：测试应用在临时目录创建独立 SQLite/上传目录，已覆盖根接口、健康检查和历史文件列表，避免触碰用户数据库。
- 建立统一风险评分契约：上传、历史列表/详情和 Dashboard 共享风险分类、扣分、评分及空值规则，并通过跨接口测试锁定一致性。
- 建立统一 AI 错误契约：上传、项目对比与 Dashboard AI 接口对未配置、超时、非法 JSON 和其他上游错误返回安全、可识别的 HTTP 错误。
- 上传文件采用“唯一临时文件 → 分析 → 原子提升 → 数据库提交”事务流程；失败仅清理本请求文件，同名上传不再覆盖既有文件。
- Dashboard AI 缓存已具备规范化指纹、稳定命中、业务数据变化失效、损坏缓存重建和模型失败不落库的测试基线。
- 风险报告与项目对比报告已建立字段契约和 Word 重开测试；风险报告评分与统一页面评分一致，两类报告使用唯一服务端文件名。
- 已建立不含真实数据的回归样本台账、预期 JSON 和脱敏结果模板；本地原件目录及本地覆盖文件均被 Git 忽略。
- 已完成3份授权 PDF 的真实 DeepSeek 隔离回归与定位审计：16项风险页码全部有效，高亮词全部可在对应页连续命中，3份 Word 报告的评分、风险数和扣分与分析结果一致。
- 新增授权 DOCX、16页纯扫描 PDF 和4页合并单元格表格 PDF 已按脱敏编号登记；原件仅复制到 Git 忽略目录。
- 纯扫描 PDF 已完成真实百度 OCR 与 DeepSeek 隔离回归：HTTP 200、7项风险页码全部有效，6项生成30个规范化坐标框；第12–16页叠框视觉检查均与文字行对齐。
- 授权 DOCX 已完成真实 WPS/DeepSeek 隔离回归：WPS 转换61页，6项风险页码及高亮全部命中，Word 报告关键字段与分析结果一致。
- 合并单元格表格 PDF 首次回归暴露模型忽略表格字段；修复提示词与前端/报告字段后复验通过，4个采购项、20个非空字段全部可回溯原文，6项风险定位和报告一致性通过。
- 人工异常样本覆盖损坏PDF、损坏DOCX和不支持扩展名：均在外部调用前返回明确4xx，临时上传与数据库零残留；PDF改为内存字节流解析，避免PyMuPDF在Windows锁住损坏上传件。
- P2真实标书回归总报告已完成：6份授权业务文档共216页，35/35风险页码有效，34/35具备可验证高亮或OCR坐标，限制与首次失败修复记录已固化。
- P3-01第一小批已完成：数据库 URL、上传目录、报告目录和 CORS 白名单统一支持进程环境、`backend/.env` 或项目根 `.env` 配置，并保持原本地默认值兼容。
- P3-01第二小批已完成：DeepSeek 统一模型覆盖和60秒默认超时支持环境配置；上传默认限制50 MiB并在解析、Office、OCR、AI之前分块拒绝超限文件。P3-01已完成。
- P3-02第一小批已完成：上传入口校验扩展名、声明MIME及PDF/DOCX/DOC文件签名；明确类型或签名不一致均在外部处理前返回415并保持零残留。
- P3-02第二小批已完成：公开地址、Word预览和物理删除统一限制在配置上传根目录；穿越文件名降为basename，异常历史路径不读取、不转换、不删除。P3-02已完成。
- P3-03第一小批已完成：新增无reload的标准生产入口、数据库/上传/报告启动检查，以及区分存活与就绪的健康探针；本地reload启动保持兼容。
- P3-03第二小批已完成：新增Nginx、systemd和Windows守护/轮转参考模板；隔离类生产环境两次启动、HTTP探针、停止及SQLite重启持久化验证通过。P3-03已完成。
- P3-04第一小批已完成：新增安全请求ID、单行JSON访问日志与递归敏感字段脱敏；移除AI原文、DOCX正文、项目名、上传路径和风险关键词调试输出。
- P3-04第二小批已完成：上传与AI高成本入口增加可配置内存滑动窗口、稳定429/Retry-After及可信代理边界；健康、历史读取和静态资源保持不受限。
- P3-04第三小批已完成设计，用户已选择方案A；第四小批已完成后端共享Bearer密钥边界、公开路径白名单、文档入口关闭、启动配置检查及稳定401/503契约。本地默认关闭。
- P3-04第五小批已完成前端统一Axios客户端、仅内存凭据输入与401提示；PDF及Word转换预览通过带Header的Blob请求加载，两类报告和全部业务请求共享认证客户端，前端不再输出完整业务响应或Axios错误对象。
- P3-04第六小批已完成隔离共享模式HTTP回归：匿名/错误/正确凭据、项目、Dashboard、PDF、Word预览、报告及文档关闭契约通过；发现并修复Uvicorn与Nginx默认访问日志保留原始文件名的问题。当批浏览器实例不可用，已在第七小批补验。
- P3-04第七小批已完成Edge扩展浏览器回归并修复凭据保存后不自动重试的问题；401弹窗、项目/Dashboard恢复、PDF/Word Blob渲染、报告授权请求、刷新清除凭据及控制台秘密检查通过。P3-04完成。
- P3-05第一小批已建立非覆盖式SQLite/上传/报告快照：SQLite在线backup、逐文件SHA-256清单、已有目标拒绝和符号链接拒绝已实现；安全恢复、依赖漏洞及秘密扫描待后续批次。
- P3-05第二小批已建立安全恢复脚本：只写入不存在的新目录，复制前验证清单路径、文件集合、逐文件哈希与SQLite完整性，经同级临时目录校验后原子落位；恢复失败不留下目标或半成品。依赖漏洞及秘密扫描待后续批次。
- P3-05第三小批已完成：Python当前环境和前端生产依赖漏洞复扫均为0；秘密扫描覆盖受控源码、测试、部署模板及文档，命中只报告位置和规则、不输出内容。P3部署与安全阶段完成，下一阶段进入P4 V1冻结。
- P4-01第一小批已建立 `1.0.0-rc.1` 候选版本、跨后端/前端版本一致性契约、一键自动发布检查、人工发布/回滚清单及发布说明草案；候选版本尚未签署或发布。
- P4-01第二小批已完成纯合成隔离生产与浏览器冻结回归，并修复凭据启用后项目中心可能漏重试的问题；Dashboard周期、PDF/Word预览、风险空状态、项目对比及两类报告通过，AI被不可达本机地址隔离。
- P4-01第三小批已完成Git候选基线只读审计：确认旧单路由入口应删除，127个未跟踪文件已分类，用户数据/密钥忽略边界有效，并形成原子候选提交与数据回滚方案；未执行Git写操作。
- P4-01第四小批已完成发布品牌收尾：中文HTML元数据、自有favicon和真实前端README已落地，5个未引用模板资源已清理并由契约测试锁定。P4-01代码、回归和文档工作完成。
- P4-02第一小批经用户授权建立 `1.0.0-rc.1` 本地原子候选基线；授权范围不包含推送、正式标签或发布。
- 候选提交前门禁已修复Windows固定pytest临时目录残留导致的重复运行失败和pip-audit缓存权限降级，现改为每次生成唯一目录并由自动测试锁定。
- P4-02第二小批已完成候选验收签署前复核并建立签署单；生产预检、回归证据、限制和回滚边界通过复核。
- 签署前复核发现直接从仓库根目录运行生产脚本会让相对默认存储路径漂移；入口现统一锚定 `backend/`，不再在调用者当前目录创建 `bid.db`、`uploads` 或 `reports`。
- P4-02第三小批经用户授权将生产路径修复、契约测试和验收材料纳入更新后的本地候选提交；不包含标签、推送或正式发布。
- P4-02第四小批已由项目负责人签署 `1.0.0-rc.1` 候选验收，最终候选提交为 `6844019`；签署不包含正式版本提升、标签或推送。
- P4-02第五小批经用户授权将版本统一提升为正式 `1.0.0` 并创建本地 `v1.0.0` 标签；明确不推送、不部署。
- P5-01已完成智能编标工作台设计：独立关联现有 `BidFile`，新增目录、评分点、映射、来源和修订实体，AI结果必须先作为建议并经人工确认；机器可读契约和三栏交互原型已落地。
- P5-02第一小批已完成工作台基础设施：显式版本化SQLite迁移创建6张新增表，保留旧V1数据并拒绝残缺同名表；空工作台创建/读取、幂等创建和初始修订API通过。
- P5-02第二小批已完成人工目录编辑后端闭环：人工章节可新增、改名和同级排序，每次成功写入都会递增工作台修订号并保存完整快照；过期修订返回稳定409，跨工作台章节和父级引用被拒绝。
- P5-02第三小批已完成带来源引用的AI目录建议：模型输出必须提供可在对应页连续命中的原文，验证通过项仅以待确认状态追加且不覆盖人工章节；无有效来源或模型失败时修订与数据均保持不变，入口纳入AI限流。
- P5-02第四小批已完成目录建议人工审核：待确认AI章节可接受为已确认或拒绝为已拒绝，AI来源、原文引用和历史快照均保留；人工章节、已处理建议、跨工作台章节和过期修订均被拒绝。P5-02后端闭环完成。
- P5-03第一小批已完成评分点结构化提取：标题、响应要求、最高分值、页码和连续原文引用经过后端校验后以待确认状态保存；无明确分值保留为空，无效来源被过滤，模型失败或全部无效时数据与修订零污染。
- P5-03第二小批已完成评分点人工审核：待确认评分点可接受为已确认或拒绝为已拒绝，来源记录和每次审核快照均保留；过期修订、重复审核和跨工作台评分点被拒绝且不产生额外修订。
- P5-03第三小批已完成评分点—章节人工映射：仅允许已确认评分点与已确认章节建立关系，同一关系重复提交更新说明而不重复建行，跨工作台引用、待确认对象、重复请求项和过期修订均被拒绝；工作台响应和快照返回映射，并计算已覆盖、缺口与待确认评分点数量。P5-03完成。
- P5-04第一小批已建立响应材料最小模型和第二版增量迁移：材料至少关联评分点或章节，记录稳定键、名称、责任人、说明及待准备/进行中/已完成/阻塞状态；迁移保留既有工作台数据、重复执行幂等，并拒绝残缺同名表且不登记成功。
- P5-04第二小批已完成响应材料新增、列表和修改API：只允许关联当前工作台已确认评分点/章节，责任人和说明由人工维护，四种状态生成材料汇总；写操作使用修订号防并发覆盖，材料随工作台响应和修订快照保存。
- P5-05第一小批已完成后端最小闭环回归：纯合成项目依次建立目录、提取并确认带来源评分点、保存章节映射、创建和完成人工响应材料；来源、覆盖统计、材料状态及连续7个修订快照保持一致，旧修订无法覆盖新状态。
- P5-05第二小批已建立前端智能编标工作台只读入口：顶部导航与左侧项目中心可打开/切换项目工作台，统一展示修订、评分点覆盖与缺口、目录审核状态、原文来源以及响应材料责任人和四态状态；该页面不展示风险检查器，也不提供正文生成。
- P5-05第三小批已接入前端建议审核：仅待确认AI目录/评分点可接受或拒绝，拒绝带二次确认；写入携带当前修订，成功后使用后端完整响应更新，409时提示刷新且不覆盖当前界面。
- P5-05第四小批已接入响应材料人工维护：弹窗支持责任人、说明和四种状态，空责任人保存为未指定，无变化不发请求；写入使用当前修订，409时丢弃过期编辑上下文并要求刷新，不包含自动分派。
- P5-05第五小批已接入前端人工新增顶级目录章节：空标题在前端拦截，最长200字，保存使用当前修订；成功更新完整工作台，409时丢弃过期输入并提示刷新，复杂层级和拖拽排序保持后续范围。
- P5-05第六小批已接入前端评分点—章节人工映射：仅已确认评分点可映射到已确认章节，已有关系以标签展示并可更新人工说明；保存携带修订且不会删除其他关系，409时提示刷新，不包含自动映射。
- P5-05第七小批已接入显式AI目录建议与评分点提取入口：调用前明确确认当前招标文件将发送给已配置模型并消耗额度，两个入口互斥防重复点击；结果仅以待审核建议返回，409时要求刷新，不自动接受或生成正文。
- P5-05第八小批已接入响应材料人工新增：候选关联仅含当前工作台已确认评分点和章节，前端要求至少关联一项；人工填写名称、责任人、说明和四态状态，保存沿用修订冲突保护，不自动分派或生成正文。
- P5-05第九小批已完成纯合成浏览器闭环：建议审核、人工映射、材料新增/编辑、修订与覆盖/阻塞汇总、项目隔离及1024px布局通过；AI确认框只验证后取消，模型请求被不可达本机地址隔离。P5-05完成。
- P5-06第一小批已完成完整门禁、迁移复核和候选边界审计：167项后端通过、1项Windows符号链接跳过，迁移5项通过，前端lint/build、秘密扫描和格式检查通过；候选范围及限制已固化，未修改版本或执行Git写操作。
- P5-06第二小批经用户授权将版本统一提升为 `1.5.0-rc.1`，复跑候选门禁并创建单一原子本地候选提交；没有创建标签、推送或部署。P5-06完成。
- P5-07第一小批已只读复核最终候选提交 `33ad2bc`、35个提交文件和数据排除边界，修正候选审计中的提交前旧HEAD，并形成待负责人确认的验收签署单；未提升正式版本、创建标签、推送或部署。
- P5-07第二小批已记录项目负责人对 `1.5.0-rc.1` 最终候选提交 `33ad2bc` 的验收签署；签署明确不创建标签、不推送、不提升正式版本。P5候选验收完成。
- P5-08经项目负责人授权，将签署记录和状态同步建立为独立本地文档提交；不创建标签、不推送、不提升正式版本、不部署。
- P5-09经项目负责人授权，将已签署候选统一提升为正式 `1.5.0`，复跑完整发布门禁并建立本地正式发布提交和 `v1.5.0` 标签；不推送、不部署。P5正式版本冻结完成。
- P6-01已完成正式版后第一项维护：项目中心移除Ant Design已弃用 `List`，使用带列表语义的原生映射并保留筛选、空状态和项目卡片操作；`v1.5.0` 标签保持不变。
- P6-02已完成前端拆包边界审计：当前 `App.jsx` 9733行、约232 kB，生产主包约2421 kB（gzip约775 kB）；确定先抽离工作台验证页面懒加载，再拆Dashboard/ECharts，最后在完整预览回归保护下处理react-pdf。
- P6-03第一小批已将 `WorkbenchPage` 原样抽离到独立页面模块，通过 `React.lazy`/`Suspense` 按需加载；构建生成9.25 kB工作台chunk，主包降至约2412.47 kB，现有props及操作回调契约保持不变。
- P6-03第二小批已完成隔离合成浏览器回归：异步工作台入口、未选项目空状态、PDF/Word项目切换、目录/评分点数据、AI确认取消、章节/映射/材料弹窗均正常，控制台0错误0警告。P6-03完成。
- P6-04第一小批已建立Dashboard图表异步边界：4处图表配置与Dashboard状态继续留在 `App`，仅将 `echarts-for-react` 移入懒加载展示组件；主包由约2412.47 kB降至1269.76 kB，ECharts形成1136.65 kB独立异步chunk。
- P6-04第二小批已完成隔离浏览器回归：4个ECharts实例正常渲染，7/30天切换同步更新周期数据，重点关注项目可穿透到PDF分析页和风险检查器；回归发现并修复Dashboard多项目时只聚合最后一条的缩进错误。P6-04完成。
- P6-05第一小批已完成文档预览异步边界审计：确认PDF和Word渲染PDF共用受保护Blob/`react-pdf`链路，新增6组契约锁定worker、对象URL回收、响应式宽度、翻页、Word页映射、文字高亮及OCR叠框；未搬迁运行代码。
- P6-05第二小批已将PDF/Word预览纯展示层抽离为懒加载 `DocumentPreview`：`react-pdf`与worker初始化移出主入口，Blob请求/回收、Word协调、页码状态、风险点击和高亮调度继续由 `App` 管理；主入口降至346.23 kB，预览形成347.44 kB异步chunk。
- P6-05第三小批已完成隔离两页PDF/Word浏览器回归：异步加载、翻页、PDF第2页文字/OCR高亮、Word原始页1到渲染页2映射均通过；发现并修复懒加载组件挂载后响应式观察器未启动的问题，495px容器内画布已由700px恢复为495px。完整后端177 passed、1 skipped，前端lint/build通过，P6-05完成。
- P6-06已完成拆包收尾审计：manifest确认工作台、Dashboard图表和文档预览三个动态入口有效；默认Dashboard仍立即加载1136.66 kB完整ECharts块，工作台增量仅9.32 kB，文档预览按打开文件增加约347.78 kB。依赖证据确认大块来自 `echarts-for-react` 默认完整ECharts入口，后续不再拆页面，优先做模块化图表引入。
- P6-07已完成ECharts模块化引入：只注册Bar/Line/Pie、Grid/Legend/Tooltip、LabelLayout和CanvasRenderer，图表块由1136.66 kB降至585.56 kB；隔离浏览器四图、最近30天切换、1280px布局及重点项目到PDF风险页穿透通过。
- P6-08已完成P6累积成果完整门禁与Git边界审计：177 passed、1 skipped，Python/前端依赖0个已知漏洞，秘密扫描、lint/build通过；18个未来候选文件、用户数据排除和可审计回滚边界已固化，详见 `docs/P6_MAINTENANCE_CANDIDATE_AUDIT.md`。
- P6-09经项目负责人授权，将P6维护成果统一提升为 `1.5.1-rc.1`，复跑完整候选门禁并建立一个本地维护候选提交；未创建标签、推送或部署。
- P6-10第一小批已完成候选提交 `814c4bf` 的签署前只读复核：22个文件、版本、标签、数据排除和回滚边界通过核对，聚焦回归12 passed；验收签署单已建立，等待项目负责人确认。
- P6-10第二小批已记录项目负责人对 `1.5.1-rc.1` 候选提交 `814c4bf` 的验收签署；签署明确不创建标签、不推送、不提升正式版本、不部署。P6维护候选验收完成。
- P6-11经项目负责人授权，将 `1.5.1-rc.1` 签署记录、候选审计和状态同步建立为独立本地文档提交；候选提交 `814c4bf` 保持在父提交历史中，未提升正式版本、创建标签、推送或部署。
- P6-12经项目负责人授权，将已签署候选统一提升为正式 `1.5.1`，复跑完整正式发布门禁并建立本地发布提交；177 passed、1 skipped，秘密扫描、Python/npm依赖审计和前端lint/build通过，未创建标签、推送或部署。
- P6-13经项目负责人授权，为正式 `1.5.1` 发布提交 `b542d01` 创建本地annotated标签 `v1.5.1`；标签目标复核通过，未推送、未部署。P6补丁发布流程完成。
- P6-14经项目负责人授权，将 `v1.5.1` 标签及发布状态同步建立为独立本地文档提交；正式发布提交和标签均保持不变，未推送、未部署。
- P6-15经项目负责人授权，将 `main` 和 `v1.5.1` 推送到已配置的GitHub仓库；远程 `main=f30d618`，标签解引用到正式发布提交 `b542d01`，与本地复核一致，未部署。
- P6-16第一小批已完成正式 `1.5.1` 本地部署预检：部署/生产启动/运行检查/备份恢复契约17 passed、1 skipped，前端正式构建和秘密扫描通过。
- P6-16第二小批已在已授权腾讯云 Windows Server 2022 上完成基础运行环境分步安装：Python 3.14.6、Node.js 24.18.0 和 npm 11.16.0 可用；Git官方GitHub发布包下载超时，WPS和应用部署尚未开始。
- P6-16第三小批已完成Git安装网络诊断：本机官方安装包的固定SHA-256和Authenticode有效；云主机三次直连官方GitHub/CDN均在安装前因吊销检查、连接重置或约15–20 KB/s限速失败/超时。超时后只读复核确认Git未安装，既有Python启动器与Node/npm未受影响。
- P6-16第五小批经动作时确认完成 Git 云端安装：临时 COS 对象下载成功，固定 SHA-256、Authenticode 和签名证书指纹全部通过；自动化助手执行 `inv-k8e34g0636` 于 33 秒内以 ExitCode 0 完成，云主机返回 `git version 2.55.0.windows.5` 和 `GIT_INSTALL_OK`。经后续单独确认，公有读临时对象及空桶均已删除，COS 控制台复核文件数和桶数均为 0。
- P6-16第六小批完成云端只读部署预检：首次 GitHub 请求因连接重置失败，加入 HTTP/1.1 和有限重试后，执行 `inv-k8e3mtg1f9` 于 3 秒内以 ExitCode 0 完成。`C:\AI-Bid-Assistant` 与 `C:\AI-Bid-Data` 均不存在，C 盘可用 76.68 GB；远程 `v1.5.1` 标签对象 `cf3aa454...` 解引用到正式提交 `b542d010...`，预检未创建目录或检出代码。
- P6-16第七小批完成固定版本检出：自动化助手执行 `inv-k8e3t3gkdd` 用时 24 秒、ExitCode 0，将 `v1.5.1` 单标签检出到新目录 `C:\AI-Bid-Assistant`。HEAD 为正式提交 `b542d010...`、根版本为 `1.5.1`、工作区干净；按发布标签处于预期 detached HEAD，未跟随远程 `main`，`C:\AI-Bid-Data` 未生成。
- P6-16第八小批已完成依赖安装命令准备：核验正式 HEAD 和工作树后，建立应用专属 `.venv`、执行 pip/npm 安装及后端编译/秘密扫描/完整 pytest、前端 lint/build。
- P6-16第九小批已完成云端后端依赖安装并发现真实 Windows System 账户回归缺陷：首次原始字节哈希门禁因 CRLF 安全退出，改为固定 HEAD + Git 无差异后，依赖和秘密扫描通过，完整 pytest 为 177 passed、1 failed。失败为符号链接拒绝后目标目录可残留；本地已将所有文件源校验前置到快照目标创建前，聚焦和完整回归通过。云端 `v1.5.1` 未热改，前端安装/门禁及生产配置、数据目录、服务均未继续。
- P6-17经项目负责人授权，将备份原子性修复统一为 `1.5.2-rc.1`；根版本、FastAPI 元数据及前端包/锁文件版本契约通过，完整候选门禁 177 passed、1 skipped，秘密扫描、Python/npm 依赖审计和前端 lint/build 通过。本批只建立本地候选提交，不创建标签、推送或部署。
- P6-18已完成最终候选 `e4cf9dd` 的签署前只读复核：父提交为 `5cca7ad`，候选只包含11个预期文件、123行新增和13行删除；版本、已发布标签和用户数据排除边界均正确，聚焦回归 2 passed、1 skipped。
- P6-19项目负责人已于2026-09-09确认签署 `1.5.2-rc.1` 候选提交 `e4cf9dd`；签署仅授权后续腾讯云候选门禁，明确排除标签、推送、正式版本提升和部署。
- P6-20经项目负责人授权，将 `1.5.2-rc.1` 签署记录、候选审计和状态同步建立为独立本地文档提交；候选代码提交保持为父提交，未创建标签、推送、提升正式版本或部署。
- P6-21已形成腾讯云候选门禁安全方案：以 Git bundle 传输可校验仓库历史，在独立目录精确检出 `e4cf9dd`，先跑 System 账户符号链接聚焦回归，再运行完整后端和前端门禁；正式 `C:\AI-Bid-Assistant`、生产数据和服务保持不变。传输通道尚未授权，候选包未上传。
- P6-22经授权使用临时 COS 单对象中转候选 bundle；云端 SHA-256、Git bundle 与候选身份校验通过。System 账户符号链接聚焦回归 1 passed，完整后端回归 178 项通过；隔离虚拟环境将 pip 26.1.2 升至 26.2 后 Python 审计为0，秘密扫描、前端 lint/build 和 npm 审计均通过。最终执行 `inv-s8fe6wg7vv` 清理3个门禁缓存目录并确认候选工作区干净；正式提交 `b542d01`、版本 `1.5.1` 和 `C:\AI-Bid-Data` 不存在状态保持不变。临时 COS 对象与空桶已删除，候选隔离目录保留，未部署。
- P6-23已完成正式 `1.5.2` 发布前只读审计：候选 `e4cf9dd` 位于当前 HEAD `3636cd4` 的祖先链，版本仍为 `1.5.2-rc.1`，不存在 `v1.5.2*` 标签；已形成版本提升、标签、推送、腾讯云部署四段授权清单及回滚边界，未执行任何发布写操作。
- P6-24经项目负责人授权，将根版本、FastAPI 动态版本来源和前端包/锁文件统一提升为正式 `1.5.2`；完整正式发布门禁通过并建立本地发布提交，未创建标签、推送或部署。
- P6-25经项目负责人授权，为正式发布提交 `4f0102481d16ffa52dfa8bc9b2f99937897e4e50` 创建本地 annotated 标签 `v1.5.2`；标签对象为 `34650bc97707bbad11b9f3146c88daf1524d1c71`，解引用提交与正式发布提交一致，未推送或部署。
- P6-26经项目负责人授权，将 `v1.5.2` 标签对象、解引用提交和当前发布边界同步建立为独立本地文档提交；标签未移动，未推送或部署。
- P6-27经项目负责人授权推送 `main` 与 `v1.5.2`：首次 SSH 连接被代理映射地址关闭，未改变远程；改用 HTTPS、OpenSSL 和 HTTP/1.1 后成功。只读复核确认远程 `main=a9125eb7edcdaf754b4858c0b9591bbf7b46701d`、标签对象 `34650bc97707bbad11b9f3146c88daf1524d1c71`、解引用正式提交 `4f0102481d16ffa52dfa8bc9b2f99937897e4e50` 与本地一致，未部署。
- P6-28经项目负责人授权，将 `v1.5.2` 远程分支、标签对象和解引用提交的复核结果建立为独立本地文档提交；未再次推送或部署，腾讯云正式环境仍保持 `1.5.1`。
- P6-29已获正式 `v1.5.2` 腾讯云部署授权并完成预检与备份：旧正式检出固定为 `b542d01 / 1.5.1`，工作区干净，既无生产数据目录、应用进程，也无80、443、8000监听；v1.5.1 Git bundle 已通过 Git 校验并记录 SHA-256。两次从 GitHub 获取固定 `4f01024` 均因云主机网络连接重置或443超时失败，自动回滚确认正式 HEAD 未变，未创建数据目录、启动任务或公网入口。继续执行需临时 COS 单对象中转授权。
- P6-29经授权使用临时 COS 单对象中转正式 bundle：云端 SHA-256 `8723EDC91C3D09668FAF60DAA5DF9AE8759AD436C5DCB433DC869E26916D47D0`、Git bundle、标签对象 `34650bc97707bbad11b9f3146c88daf1524d1c71` 和正式提交 `4f0102481d16ffa52dfa8bc9b2f99937897e4e50` 校验通过。修正 System 的 Node.js PATH 并普通重启后，自动化助手执行 `inv-r8fgsw05ra` 用时241秒、ExitCode 0；前端 lint/build、后端生产检查、健康/就绪/文件接口、工作区干净及仅回环监听验收通过，DeepSeek/OCR禁用，80/443未监听。临时 COS 对象及空桶均已由项目负责人确认删除。
- P6-31C第二小批完成仅回环同源入口与共享鉴权：生产 `.env` 与前端dist已先备份，随机生成的共享令牌只留在服务器秘密配置；前端按Tailscale DNS HTTPS地址重建，Caddy计划任务只绑定 `127.0.0.1:4173` 并代理 `/api`、`/uploads` 到 `127.0.0.1:8000`。最终 `cmd-mgkro89h` 以ExitCode 0通过匿名业务接口401、授权直连200、代理接口/首页200以及8000/4173无公网监听检查；未启用Serve或修改公网安全组。
- P6-31C第三小批经动作时确认执行Serve：初版与 `--yes` 修正版都在等待tailnet首次HTTPS授权时达到自动化助手超时；执行记录分别为 `inv-s8gqkpgrwm` 与 `inv-s8gqtw0m1q`。每次后续只读状态命令均在1秒内成功并返回 `No serve config`，证明没有半配置入口。官方资料确认管理员需先开启MagicDNS与HTTPS Certificates，并确认相关DNS名称进入公开证书透明度记录。
- P6-31C第四小批在负责人启用tailnet HTTPS后复跑：首次短探测因入口尚未就绪而按门禁自动执行 `tailscale serve reset`；延长等待窗口后，执行记录 `inv-s8grjp0qjc` 用时2秒、ExitCode 0，输出 `FORMAL_HEAD_OK`、`APP_TASKS_OK`、`SHARED_AUTH_OK`、`LOOPBACK_SCOPE_OK`、`TAILSCALE_SERVE_HTTPS_OK`、`NO_PUBLIC_LISTENER_OK` 和 `P6_31C_SERVE_STAGE_OK`。正式HTTPS入口已生效，8000/4173继续仅回环监听，公网安全组未修改。
- P6-31C第五小批完成办公电脑接入前只读审计：`Get-Command tailscale.exe`、Tailscale Windows服务和两个标准安装路径均未发现客户端；本机8000/4173也无监听。服务器端入口不需改动，但安装第三方客户端和加入tailnet属于新的系统变更，等待负责人明确授权，认证必须由负责人本人完成。
- P6-31C第六小批经负责人明确授权安装办公电脑客户端：winget启动器不可访问后停止使用，改从Tailscale官方稳定源下载 `1.102.3` 通用Windows安装程序；安装前Authenticode为Valid、签名主体为Tailscale Inc.。静默安装后确认CLI版本 `1.102.3`、Windows服务RUNNING、BackendState为 `NeedsLogin`，本机8000/4173无监听；未启动登录、不修改腾讯云安全组或应用入口。
- P6-31C第七小批在负责人本人完成认证后执行办公电脑回归：Tailscale BackendState为Running、SelfOnline为True，唯一在线云服务器节点同tailnet在线。Chrome正式HTTPS入口加载成功，`/api/files` 返回200及空数组；Dashboard、标书分析与智能编标页面、导航和空状态均正常。生产数据库没有项目记录，因此PDF/Word预览、风险定位、项目对比和报告导出没有测试对象；本批未上传文件、写数据库或调用DeepSeek/OCR。

## 已知问题与风险

- 腾讯云主机直连 GitHub/GitHub官方CDN下载 Git for Windows 发布包存在吊销检查失败、连接重置和约15–20 KB/s限速；后续必须使用仍可审计的传输路径并在云端再次校验固定SHA-256与Authenticode，不降低校验要求。临时COS中转会产生外部资源和潜在少量费用，须经项目负责人明确授权。
- COS 对象保持私有读写，控制台临时下载链接默认仅一小时有效；不得将带签名的临时链接写入源码、文档、日志或提交。安装成功后须再次确认并删除临时对象，避免遗留外部资源。
- Python安装器的 `TargetDir=C:\Program Files\Python314` 参数在自动化调用中被空格拆分，解释器实际安装到 `C:\Program`；`py -3.14` 可正常定位。应用虚拟环境建立前需决定保留显式启动器路径还是经确认修复安装位置，不直接修改系统PATH。
- Vite构建在P6-04第一小批后仍有两个超过500 kB的chunk：主入口约1269.76 kB、Dashboard/ECharts异步块约1136.65 kB；Dashboard是默认页，浏览器回归后再判断是否继续拆展示页面，不能以分块本身等同于首屏下载已减少。
- Ant Design `List` 弃用提醒已在P6-01通过原生列表映射消除；后续浏览器回归继续观察其他组件警告。
- 项目已有 `backend/.venv`（Python 3.14.6）；使用其中解释器完成后端编译、FastAPI 导入、根接口和健康接口验证。系统级 Windows Store `python.exe` 不可用，后续命令必须优先使用项目虚拟环境。
- Git基线已确认旧 `backend/main.py` 是171字节的单路由示例并完成删除；当前启动、测试和生产链路统一使用已跟踪的 `backend/app/main.py`。
- SQLite 数据库、上传的真实 PDF 和已生成报告位于工作区，属于用户数据；发布前必须迁移到受控持久化目录并继续保持 Git 忽略。
- 后端自动化测试目前有171项通过、1项因Windows无符号链接权限跳过；新增多项目Dashboard聚合回归，继续覆盖版本一致性、秘密扫描、备份恢复、共享密钥、限流、日志脱敏、部署、数据契约、文档预览、OCR、报告与异常隔离。
- 完整 AI 项目对比未在 UI 回归中触发，以避免产生真实模型费用；Dashboard 缓存与 AI 异常现已使用可控替身完成接口级测试。
- P2-02 已完成：DOCX、合并单元格表格PDF和人工异常文件通过；纯扫描样本OCR/DeepSeek、页码及30个坐标框通过，1项无可靠原文匹配时保守不画框及低风险未覆盖作为已知限制。
- P2-03 的16项风险页码与高亮定位均通过，3份报告一致性通过；AI 返回的引用仅9/16能与对应页逐字匹配，关键词0/16逐字匹配（其内容是风险概念标签），因此引用与关键词不能单独作为可靠定位依据。
- 用户确认本机没有 Microsoft Word 授权，因此当前实际渲染引擎为 WPS Office；代码仍保留未来安装正版 Word 后自动优先使用 Word 的顺序。加密文件及不受 Word/WPS 支持的旧格式会返回明确错误。
- 真实纯扫描回归中高风险2/3、中风险4/4生成坐标框并视觉对齐；另1项高风险因模型证据与 OCR 行无可靠匹配而保守不画框。本样本没有低风险项，低风险框仍只有代码与替身测试覆盖。旧分析记录不含坐标，不能补显高亮。
- V1.5最小闭环、浏览器回归、完整门禁、候选签署及正式 `1.5.0` 提升均已完成。详细证据见 `docs/V1_5_WORKBENCH_REGRESSION.md`、`docs/V1_5_CANDIDATE_AUDIT.md`、`docs/V1_5_CANDIDATE_CHECKLIST.md` 与 `docs/V1_5_ACCEPTANCE_SIGNOFF.md`。本地 `v1.5.0` 标签按本批Git结果建立，当前未推送或部署。
- 正式 `1.5.0` 发布门禁结果：后端167 passed、1 skipped，Python和前端生产依赖审计均为0个已知漏洞，前端lint/build及秘密扫描通过；保留既有弃用警告和约2.46 MB主包提醒。
- P6-01后主包约2.42 MB；P6-02确认不能通过单纯提高阈值或同步vendor分块解决，后续以初始加载依赖实际减少和受保护功能回归为验收标准。
- P6-03代码、静态门禁和合成浏览器回归均已通过；Dashboard初始化AI仅命中不可达本机测试地址并安全返回502，未连接真实DeepSeek。
- P6-04隔离回归使用两条合成项目和不可达本机AI地址；修复后Dashboard累计风险、周期趋势、重点关注列表与项目中心一致，浏览器控制台0错误0警告，未连接真实DeepSeek。
- P6-05保持了预览高风险边界：Blob请求与回收、Word接口协调、风险点击状态仍留在入口；纯展示层及响应式观察器位于懒加载组件，详细证据见 `docs/DOCUMENT_PREVIEW_BOUNDARY_AUDIT.md`。
- `react-pdf`在快速切换PDF/Word时会记录一次文字层任务取消型 `AbortException`；依赖会在业务回调前自行输出，最终页码和高亮均正常。本阶段不修改第三方依赖、不关闭文字层，后续仅在依赖升级或真实功能失败时处理。
- P6-06构建图显示首屏静态JS约926.12 kB（gzip296.18 kB），默认Dashboard加图表后约2062.78 kB（gzip673.61 kB）；Card/typography共享块已被入口使用，当前不采用脆弱的Ant Design深层导入或人为提高chunk警告阈值。
- P6-07后默认Dashboard首次JS约1511.68 kB（gzip494.77 kB），较P6-06基线减少约551.10 kB（gzip178.84 kB）。构建仍提示585.56 kB图表块超过500 kB，但已是当前三类图表和Canvas渲染的明确模块集合，不提高阈值掩盖提醒。
- P6-09确认根版本、FastAPI和前端包/锁文件统一为 `1.5.1-rc.1`；P6维护候选已在本地提交，annotated tag `v1.5.0` 仍解引用到正式发布提交 `a4295e60...`，未创建候选标签、推送或部署。
- P6-13确认根版本、FastAPI和前端包/锁文件统一为正式 `1.5.1`；`v1.5.1` 标签对象为 `cf3aa454...`，解引用后指向发布提交 `b542d01`。发布说明见 `docs/V1_5_1_RELEASE_NOTES.md`，`v1.5.0` 标签保持不变。

## 数据与安全状态

- `.env` 已被 Git 忽略；`.env.example` 仅包含占位配置。
- 严禁把真实 Key、数据库、上传件和导出报告提交到版本库。
- 百度 OCR 默认关闭；只有显式设置 `OCR_PROVIDER=baidu` 且配置本机凭据后，缺少文字层的扫描页才会发送到百度。
- CORS 已支持 `CORS_ORIGINS` 英文逗号分隔白名单；未配置时仅允许本地 Vite 地址，生产部署仍需填写实际 HTTPS 来源。
- 数据库、上传目录和报告目录已支持环境变量覆盖；合成数据的备份与安全恢复已验证，生产持久化挂载和真实主机恢复演练仍需部署时在隔离目录执行。
- DeepSeek 模型和超时已支持环境变量覆盖；上传默认50 MiB应用层限制已生效，反向代理限制仍待P3-03部署验证。
- 上传已形成大小、扩展名、声明MIME、文件签名、解析器结构和存储路径边界防线；P3-02已完成。
- 标准生产入口、探针、服务守护/反代/轮转模板及隔离持久化验证已完成；模板尚未安装到真实服务器，目标主机路径、证书和权限仍需部署时审核。
- 访问日志脱敏、单进程速率限制和方案A后端鉴权边界已完成；限流重启清空且多实例不共享。
- 鉴权本地默认关闭；方案A前后端、类生产HTTP和浏览器交互链路均已通过。生产启用仍需使用至少32位随机密钥、HTTPS和受控网络入口。
- 生产入口已关闭Uvicorn原始访问日志，Nginx模板关闭默认访问日志；请求审计统一使用应用层安全归一化JSON日志，避免客户文件名进入代理或服务器访问日志。
- 2026-09-03隔离浏览器回归时，空 `DEEPSEEK_API_KEY` 因配置优先级未覆盖本机 `.env`，意外产生1次Dashboard AI调用；未暴露密钥或业务数据，后续隔离回归必须设置非空测试Key及不可达测试Base URL双重断网边界。
- 依赖审计工具作为开发工具单独记录于 `backend/requirements-audit.txt`，不加入生产运行依赖；Python首次扫描的9条记录已通过升级修复，后续每次发布前必须重新联网审计。
