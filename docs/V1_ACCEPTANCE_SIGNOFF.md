# V1候选版本验收签署单

候选版本：`1.0.0-rc.1`

候选基线提交：`25e4e96b0330e6c5239987a1e63f2beb8b25e185`

最终待签署提交：本签署单所在提交（使用 `git rev-parse HEAD` 获取并在签署报告中登记）

复核日期：2026-09-03

签署状态：技术复核通过，等待项目负责人确认

## 本次签署确认什么

签署表示项目负责人确认：当前候选提交可以作为V1功能验收基线；已有功能、自动回归、真实授权样本结果、已知限制和回滚依据已经明确。签署不代表生产服务器已经部署，也不代表系统零风险。

本次签署不会自动执行以下操作：

- 不把版本提升为正式 `1.0.0`；
- 不创建 `v1.0.0` Git标签；
- 不推送本地提交或标签；
- 不覆盖数据库、上传文件或报告；
- 不调用DeepSeek、百度OCR或其他付费服务。

## 已复核证据

- [x] 候选基线提交存在，父提交为 `82f3383ea75674da139e58793d5b02cd3422bdb7`，提交说明与候选版本一致。
- [x] 签署前发现的生产默认路径修复及本签署材料已纳入同一个本地候选提交，最终提交号由Git命令核验。
- [x] 版本文件为 `1.0.0-rc.1`，当前提交没有标签。
- [x] Git基线包含前后端、测试、部署模板、依赖锁和项目文档；真实配置、数据库、上传件、报告及授权样本未入库。
- [x] 生产启动预检通过：数据库、上传目录、报告目录和鉴权配置可用。
- [x] 发布门禁最近一次通过：后端138项通过、1项因Windows符号链接权限跳过；前端lint/build、秘密扫描和Python/npm依赖审计通过。
- [x] PDF、扫描PDF、DOCX/DOC、复杂表格和异常文件已完成授权或合成回归，证据见 `docs/regression/V1_REAL_DOCUMENT_REGRESSION_REPORT.md`。
- [x] Dashboard、项目中心、风险检查器、风险定位、项目对比和报告导出已完成冻结回归，证据见 `docs/V1_FREEZE_REGRESSION.md`。
- [x] 回滚代码基点明确：候选提交为当前可用基线，其父提交仅是旧最小示例，不能作为业务可用版本。

## 接受后仍保留的限制

- 纯扫描样本有1项风险因缺少可靠OCR原文匹配而不绘制坐标框；低风险OCR框尚无真实样本覆盖。
- 当前Word版式回归使用WPS，不能保证与Microsoft Word分页完全一致。
- 复杂表格回归不覆盖所有多页、嵌套和跨页表格形态。
- 前端生产主包约2.44 MB，构建有大chunk提醒，但不阻塞运行。
- Ant Design `List` 存在未来主版本弃用提醒，当前功能正常。
- 单进程内存限流不适用于多实例共享计数，V1部署约束为单worker。

## 部署时必须另行完成

- 在真实服务器配置HTTPS、受控CORS、至少32位随机共享密钥、单worker及受限目录权限。
- 部署前创建数据库、上传目录和报告目录的新快照，在隔离新目录验证恢复，不原位覆盖用户数据。
- 部署后重新验证健康探针、鉴权、项目只读接口、PDF/Word预览和报告下载。
- 正式版本号、`v1.0.0`标签和远程推送均需单独授权。

## 项目负责人确认语句

确认本签署单所在提交号并复核本页后，如接受上述范围和限制，请明确回复：

> 确认签署 1.0.0-rc.1 候选版本验收，不创建标签、不推送。

收到确认后，只会把签署结果和确认日期写入项目文档并建立一个本地签署记录提交；正式版本、标签和推送仍需再次授权。
