# V1.5.0-rc.1 候选检查清单

候选版本：`1.5.0-rc.1`

状态：候选门禁完成并建立本地候选基线；不包含标签、推送或部署授权。

## 自动门禁

- [x] 根版本、FastAPI和前端包/锁文件版本一致。
- [x] Python编译通过。
- [x] 完整后端pytest通过，跳过和警告有明确记录。
- [x] 前端ESLint和生产构建通过。
- [x] 秘密扫描与差异格式检查通过。
- [x] 工作台迁移、API、修订冲突和最小闭环测试通过。

## 数据与范围

- [x] 暂存区不包含 `.env`、数据库、上传文件、报告、真实样本、依赖或构建产物。
- [x] 两版SQLite迁移保持增量、幂等并拒绝残缺同名表。
- [x] V1受保护功能未被移除或替换。
- [x] 不包含一键整本标书、自动正文、自动分派或自动投标。
- [x] 浏览器闭环证据与候选审计材料已纳入。

## Git边界

- [x] 仅创建一个本地原子候选提交。
- [x] 不创建或移动任何Git标签。
- [x] 不执行推送或部署。

候选提交哈希以Git历史和本批最终报告为准；候选验收签署和正式版本提升必须另行授权。
