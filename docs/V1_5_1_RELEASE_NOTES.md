# AI标书助手 1.5.1 发布说明

发布日期：2026-09-05

状态：已由签署候选提升为正式版本；`main` 和 `v1.5.1` 已推送到GitHub，未部署。

## 发布身份

- 正式版本：`1.5.1`
- 签署候选提交：`814c4bf8be0198d0e48b1d93ee3bb035bfb93700`
- 候选签署记录提交：`03411ec9e739d1907d30562474716440542e7efd`
- 正式发布提交：`b542d010d2679c0265fcd4cd50cd420fcd39954e`（短哈希 `b542d01`）。
- 本地annotated标签：`v1.5.1`，标签对象 `cf3aa454939812714bb377391a960459f90269b3`，解引用后指向正式发布提交。
- 既有 `v1.5.0` 标签保持不变。

## 主要变更

- 修复Dashboard多项目聚合只使用最后一个项目的数据一致性问题。
- 将智能编标工作台、Dashboard图表和文档预览改为按需加载，保持既有业务状态和交互契约。
- ECharts改用模块化入口，Dashboard图表异步块由1136.66 kB降至585.56 kB。
- 增加Dashboard多项目、前端拆包、PDF/Word预览与风险定位边界回归。

## 保持能力

- PDF/Word响应式预览、风险原文定位和高亮。
- Dashboard周期切换、预警穿透和AI摘要缓存。
- 项目中心、风险检查器、项目对比与报告导出。
- 智能编标工作台目录、评分点、人工审核、映射和响应材料最小闭环。

## 已知限制

- Vite仍提示585.56 kB Dashboard图表异步块超过500 kB；不通过提高阈值隐藏提醒。
- 当前Windows环境无符号链接权限，发布门禁对应备份用例保持1项跳过。
- 保留1个既有Starlette/httpx弃用警告。
- 扫描件无可靠OCR匹配时保守不画框；WPS版式验证不代表Microsoft Word分页完全一致。

## 发布边界

- 已建立本地正式版本、发布提交和经单独授权的 `v1.5.1` 标签。
- `main` 和 `v1.5.1` 已经项目负责人授权推送到已配置的GitHub仓库，并完成远程哈希复核；未部署。
- 未调用真实DeepSeek或百度OCR，未修改用户数据库、上传文件和报告。
- 后续若需部署，必须另行明确授权并提供目标环境。
