# AI标书助手 1.5.2 正式发布执行清单

日期：2026-09-09

状态：正式版本提升与本地 annotated 标签已完成；`v1.5.2` 固定解引用到正式发布提交 `4f01024`，尚未推送或部署。

## 已确认基线

- 已签署候选提交：`e4cf9dd10bad4ef257fd92206dcdce582b2edc74`。
- 候选提交是当前本地 HEAD `3636cd483414dc2414d2b28a03ee2d2cadcd63f4` 的祖先；当前 HEAD 包含后续签署和状态文档，不改变候选代码身份。
- 候选版本为 `1.5.2-rc.1`，当前不存在指向候选的标签，也不存在 `v1.5.2*` 标签。
- 腾讯云候选隔离门禁已通过；正式云端目录仍为 `1.5.1`，生产数据目录尚未创建。

## 授权分段

以下动作必须分段授权，不能从“继续自动开发”推定：

1. 正式版本提升：把根版本、前端包及锁文件统一改为 `1.5.2`，复跑完整发布门禁并创建本地正式发布提交。
2. 本地标签：为最终正式发布提交创建 annotated 标签 `v1.5.2`。
3. 远程推送：推送 `main` 和 `v1.5.2`，随后复核远程提交和标签解引用哈希。
4. 腾讯云部署：备份既有正式检出状态，将正式目录切换到固定 `v1.5.2` 提交，配置生产数据、服务和探针；部署授权不自动包含域名、证书或公网开放。

## 正式版本提升门禁

- 只修改版本文件和必要的发布状态文档，不夹带功能改动。
- 运行版本一致性测试、Python 编译、完整 pytest、秘密扫描、Python 依赖审计、前端 lint/build 和 npm 生产依赖审计。
- 检查候选修复仍在提交历史中，工作区不包含 `.env`、数据库、上传件、报告、真实样本、依赖目录或构建产物。
- 任一门禁失败即停止，不创建正式提交或标签。

## 部署与回滚边界

- 云端只能部署固定正式提交，不能直接跟随浮动 `main`。
- 在切换前记录 `C:\AI-Bid-Assistant` 的 HEAD、版本和工作区状态；用户数据必须位于独立持久化目录，禁止删除或覆盖。
- 先在本机环回地址完成后端就绪、前端静态资源和受保护接口检查，再决定是否开放 80/443。
- 回滚只切回已验证的 `v1.5.1` 提交和原配置；数据库若发生不可逆迁移必须另行制定恢复步骤。本次 `1.5.2` 修复不引入数据库迁移。

## 当前结论

正式 `1.5.2` 本地发布提交与 `v1.5.2` annotated 标签已建立。推送和腾讯云部署仍属于后续独立写操作，须分别获得明确授权。
