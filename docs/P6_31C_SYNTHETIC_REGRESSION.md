# P6-31C 受控入口纯合成回归记录

日期：2026-09-11。正式代码基线保持 `4f01024 / 1.5.2`；未热改、提交、推送或创建标签。

## 授权与数据状态

### 2026-09-11 腾讯云安全组只读核查

- 实例 `ins-goda9pyq` 仅绑定安全组 `sg-hftoto2k`（自定义模板），关联实例数量为1，未发现其他云资源共用该安全组。
- 入站规则共6条：已有 TCP 3389（一个特定 IPv6 来源、一个 `::/0`）、TCP 80/443 的 IPv4 与 IPv6 全来源；没有 UDP 41641，未执行任何添加、编辑或删除。
- 既有 IPv6 全来源 TCP 3389 暂只记录风险，不在本批擅自收紧，避免影响远程维护；是否整改需负责人另行决定。
- 由于办公电脑 Tailscale UDP 实际公网来源无法确认，直连试验保持停止；本批不改变安全组、不开放端口、不修改正式代码或数据。

### 单目标TUN排除回滚完成

### 2026-09-12 浏览器工作台入口复核

- Chrome重新连接受控HTTPS入口后，点击“智能编标”成功加载工作台。
- 未从左侧项目中心选择项目时，页面显示“请先从左侧项目中心选择一个项目”，目录建议和评分点提取按钮均为禁用状态；同时显示人工审核边界，不自动分派责任人、生成正文或整本标书。
- 本次仅读取页面并点击工作台入口，未上传文件、未提交表单、未调用DeepSeek/OCR、未修改业务数据。

### 2026-09-12 合成项目工作台只读回归

- 从项目中心选择 `Auth PDF Project` 后，智能编标工作台加载当前修订1、已覆盖评分点0、评分点缺口1、阻塞材料0。
- 目录显示“技术响应方案/已确认”和“项目实施计划/待审核”；评分点显示来源页码、缺口及待审核状态；响应材料显示空状态“尚未登记响应材料”。
- 仅观察页面状态，未点击接受、拒绝、添加映射、新增材料或生成建议，未改变合成数据。

### 2026-09-12 PDF风险定位补验

- 在 `Auth PDF Project` 分析页点击现有风险的“点击定位原文”入口，预览从第1页跳转到第2页。
- 页面显示 `AUTH REGRESSION PDF RISK PAGE 2`，风险详情同步显示第2页、风险扣分20分及原文依据；截图确认红色半透明高亮框覆盖风险文字。
- 本批仅触发已有风险定位读取，不上传文件、不提交表单、不调用DeepSeek/OCR、不修改业务数据。

### 2026-09-12 Word预览与页码映射补验

- 从项目中心打开 `Auth Word Project`，页面显示“Word版式预览”，并标注已使用本机办公软件转换、共2页；本批仅观察现有预生成夹具的渲染结果，不能替代真实办公文件转换验收。
- 点击已有中风险定位入口后，预览跳转到第2页，显示 `AUTH WORD RISK PAGE 2`；风险详情同步为第2页、扣分8分，截图确认风险文本可见。
- 未上传文件、未提交表单、未调用DeepSeek/OCR或新增WPS任务，未修改业务数据。

### 2026-09-12 侧栏折叠布局抽查

- 在 Word 第2页风险状态下收起项目中心侧栏，中央 Word 预览、顶部导航和右侧风险检查器仍可见，未出现横向溢出或内容消失。
- 随后恢复项目中心侧栏，项目列表和当前选中项目状态恢复；该操作只改变浏览器界面状态，不改变业务数据。
- 本次属于单一桌面视口的侧栏组合抽查，不替代完整窄屏/宽屏响应式矩阵。

### 2026-09-12 风险检查器折叠布局抽查

- 在 Word 第2页风险状态下收起右侧风险检查器，中央 Word 预览扩展到可用区域，顶部导航和项目中心仍保持可见。
- 恢复风险检查器后，右侧风险信息回到原布局；该操作只改变浏览器界面状态，不修改项目或风险数据。
- 本次仍属于单一桌面视口抽查，不替代完整响应式矩阵。

### 2026-09-12 报告页码一致性修复与云端差异

- 只读检查已下载的合成 Word 风险报告发现：报告表格仍显示原始第1页，而浏览器渲染预览已映射到第2页。
- 本地代码已修复前端报告请求携带 `report_page` 映射字段，后端报告表格优先使用该字段、无映射时继续回退原始 `page`；PDF和旧调用契约保持兼容。
- 报告契约测试4项通过，前端lint/build通过；本机LibreOffice实际渲染新导出报告2页，标题、评分、风险表格、原文依据和AI建议均可读，未见裁切或重叠。
- 浏览器当前连接的腾讯云正式 `1.5.2` 尚未包含本地修复；云端新导出报告仍显示原始第1页，说明修复待另行授权部署，不能把本地测试结果宣称为云端已生效。

### 2026-09-12 项目对比报告视觉复核

- 已使用本机LibreOffice逐页渲染现有合成项目对比报告，共5页；标题页、管理层决策摘要、项目核心信息表、综合指数表、共同风险、最终建议和免责声明均可读，未见文字裁切、表格重叠或页脚错位。
- 第5页仅保留报告说明段落，留白较多，登记为后续版式优化项；本批不为压缩留白进行无关重排。

- 用户确认恢复后，只读比较config.yaml、clash-verge.yaml、verge.yaml、profiles.yaml的SHA-256，4/4匹配试验前备份。
- 目标公网路由恢复Mihomo，tailnet /32路由保持Tailscale；单次pong经DERP(lax)，946毫秒，未建立直连。
- ChatGPT HTTPS返回200，TLS验证结果0；仅证明HTTPS可达，不扩大为完整对话功能验收。
- 本次试验回滚完成，不再等待用户操作。原加载慢故障仍未解决，未改云端安全组、正式代码或数据。初次只读校验命令发生PowerShell管道语法错误，修正后上述验证完整执行。

### 单目标TUN排除保存后验证（回滚待确认）

- 用户手动保存后，两份配置均包含目标/32；将该项还原为空列表后与备份逐字一致，确认没有其他配置变化。
- 目标公网有效路由WLAN，tailnet /32有效路由Tailscale。ChatGPT HTTPS 200、TLS验证结果0、耗时6.493797秒；这只证明HTTPS可达，不代表完整登录/对话功能验收。
- Tailscale三次pong仍DERP(lax)，耗时2125/388/867毫秒，未建立直连。
- worker HTTP 200但40.014899秒超时，只接收246272/1369825字节，端到端下载门禁失败。
- 已按失败恢复要求立即请求用户删除本次单条排除并保存；自动窗口工具仍不可用，尚未完成回滚核验，不宣称已恢复。不新增安全组规则、不开放端口、不修改正式代码或数据。

### 2026-09-11 单目标TUN排除授权后预检（尚未应用）

- 用户授权仅当前云服务器公网IPv4/32的TUN排除，保留Clash及其他代理规则；需备份、验证目标路由/Tailscale/ChatGPT，失败恢复。本批禁止云端安全组、新端口、正式代码或数据改动。
- 实测安装内核Mihomo Meta v1.19.29；官方TUN文档提供auto-route下的route-exclude-address。相关配置的auto-route为true，route-exclude-address为空，运行生成配置TUN启用。没有输出订阅、密钥或完整配置。
- 4份相关配置config.yaml、clash-verge.yaml、verge.yaml、profiles.yaml已复制到办公电脑用户目录独立备份（不在仓库），随后逐一比较SHA-256，4/4一致。备份包含敏感配置，不上传或提交。
- 目标公网路由复核仍为Mihomo。窗口操作遇到coordinate input geometry unavailable与SetIsBorderRequired不支持接口，恢复时提示窗口最小化及用户输入保护；无法可靠完成客户端保存和回退，停在修改前。
- 排除项未应用，其他代理规则未改，未进行改后联合验证，无网络变更需要回滚。客户端操作恢复后可按已有授权继续；不能把备份成功当成排除试验成功。

- 负责人授权：执行前备份空数据库；仅使用纯合成PDF/Word回归夹具，不调用DeepSeek、百度OCR或外部分析服务；验收后保留夹具，未经另行确认不得删除。
- 备份目录：`C:/AI-Bid-Data/backups/pre-p6-31c-synthetic-20260911-001256`。备份清单包含原数据库，原上传/报告目录为空；秘密环境配置另存于备份目录，不展示其内容。
- 使用仓库已有 `backend/scripts/prepare_auth_regression.py` 生成独立数据根 `C:/AI-Bid-Data/p6-31c-synthetic`。运行配置的数据库、上传和报告路径均切到该根；原数据库没有被合成记录覆盖。
- 两条项目：Auth PDF Project（80分，高风险1项）与Auth Word Project（92分，中风险1项）。Word源为占位文件，预览为预先生成的两页PDF，不能当成真实DOCX或WPS转换样本。
- 初次路径切换失败后出现旧进程数据库连接与恢复后的文件目录不一致；当时未达到完整回滚验证条件。最终停止旧API进程、统一三条数据路径并重启后通过门禁，不将中途失败记录为成功。
- 腾讯云命令 `cmd-kl0cjj9t` 最终执行 `inv-s8gt2jg5ie`：ExitCode 0，正式提交、外部提供方禁用、备份存在、合成路径一致、PDF下载与Word两页预览检查通过；未修改公网安全组。

## 本轮办公电脑Chrome结果

| 检查 | 结果与证据 |
| --- | --- |
| 项目中心与Dashboard | 显示2个合成项目、2项风险、平均分86；Dashboard“查看分析”进入PDF项目 |
| PDF正文 | 实际渲染两页，第一页含AUTH REGRESSION PDF PAGE 1 |
| PDF风险定位 | 点击风险跳到第2页，显示AUTH REGRESSION PDF RISK PAGE 2；上一页可用、下一页禁用 |
| PDF风险高亮 | DOM存在可见OCR高亮框，背景rgba(255,77,79,0.32)，测得约261.6×38.5像素；不代替完整响应式矩阵 |
| Word预生成版式预览 | 第1页显示AUTH WORD PREVIEW PAGE 1；点击原记录第1页风险后跳到渲染第2页，正文AUTH WORD RISK PAGE 2，详情页码同步为2 |
| 非AI项目对比 | 80/92分、风险1/1、高风险1/0、中风险0/1、扣分20/8一致；AI决策摘要未生成 |
| 对比报告下载 | 办公电脑Downloads中新增AI投标项目对比分析报告 (8).docx，39,224字节，10:27:27 |
| 风险报告下载 | 办公电脑Downloads中新增AI投标风险分析报告 (4).docx，36,052字节，10:27:53 |
| 控制台 | 本轮抽查error/warn为空；不宣称整个历史会话无错误 |

## 未完成及下一步

### 2026-09-11 办公电脑路由与TUN只读检查

- Tailscale BackendState=Running，ExitNodeStatus不存在，当前未报告活动出口节点。
- 活动接口包括WLAN、Mihomo（Meta Tunnel）和Tailscale；Clash Verge及Mihomo内核进程正在运行。
- 到云服务器公网IPv4的Windows有效路由选择Mihomo接口（默认路由）；到该服务器tailnet IPv4的路由选择Tailscale接口（/32）。不在仓库记录公网出口或私有DNS。
- Windows系统HTTP代理开关关闭，残留代理地址配置存在但未输出；PAC未配置，本进程HTTP_PROXY/HTTPS_PROXY/ALL_PROXY/NO_PROXY均不存在。以上不代表TUN已关闭。
- 修正诊断解释：此前curl --noproxy仅排除HTTP代理，不绕过操作系统TUN路由；其公网回显不能称为“物理网络直连出口”。目标公网路由经过Mihomo是已验证事实，但不证明Mihomo最终选择哪个上游，也不能据此确认Tailscale进程实际绑定路径或目标UDP源地址。
- 未读取完整Clash配置、订阅或凭据，未修改路由、代理、安全组或Windows防火墙。受限UDP试验继续暂停于来源确认条件。
- 最小后续候选：另行授权后备份相关配置，仅对该云服务器公网IPv4/32设置TUN路由排除，保持其他流量/ChatGPT代理不变，失败恢复；执行前先核对实际客户端支持和配置合并方式。不能只加普通DIRECT规则并假定全局模式会遵守。此为待授权方案，本批未执行，不保证单独消除中继。
- 依据：[Mihomo TUN配置](https://wiki.metacubex.one/en/config/inbound/tun/)说明route-exclude-address用于自动路由下的指定网段排除；须结合本机版本和生效配置核验。

### 2026-09-11 受限直连授权后的来源预检停止

- 负责人已明确授权：仅办公电脑当前公网IPv4访问云端UDP 41641；先核实实际监听/规则；端口不同、来源无法确认或影响其他实例则停止；失败撤销新增规则，不改代理/业务端口/正式代码/数据。
- 同一批次读取本机Tailscale netcheck的GlobalV4，并使用IPv4 HTTPS公网回显（curl明确绕过HTTP系统/环境代理）进行交叉检查，二者均取得结果，但IPv4地址不同。地址仅供当次诊断，不写入仓库。
- 不能把HTTPS出口直接用于UDP白名单，也不能把对STUN目的地的UDP探测地址当成访问指定云服务器的已确认来源；本机此前MappingVariesByDestIP=true。尚未证明是Clash、TUN、出口节点或运营商分流造成。
- 根据“来源无法确认则停止”条件，本批在来源预检处停止。未执行云端端口/关联安全组检查，未创建或修改任何安全组或Windows防火墙规则，无新增规则需要回滚；未继续反复测速。
- 下一步需要负责人选择继续方式：在不改代理/不开放新端口条件下，另行授权只读核对办公电脑到目标服务器的有效路由、Tailscale出口节点选择及代理/TUN状态，尽可能确认分流原因。只输出必要状态，不打印订阅、凭据或完整配置。如果仍无法确认指定目标UDP来源，继续停止，不扩大为多地址或0.0.0.0/0放行。

### 2026-09-11 两端连通性汇总与待决策方案

- 云端命令 `cmd-4cvbhrkn` 首次 `inv-s8hn4x0gfn` 因Windows PowerShell将原生命令stderr中的JSON格式警告当成终止错误而退出。仅在原生命令调用期间改用Continue，随后恢复Stop并校验退出码/JSON，复跑 `inv-r8hn60g17h`，15:47:13～15:47:17，ExitCode 0，`NETCHECK_SUMMARY_OK`。
- 云端：UDP=true、IPv4=true、IPv6=false、MappingVariesByDestIP=false、UPnP/PMP/PCP=false、PreferredDERP=17。办公电脑此前摘要：UDP/IPv4/IPv6=true、MappingVariesByDestIP=true、UPnP/PMP/PCP=false、PreferredDERP=2。PreferredDERP不是实际业务路径证明，实际走中继的证据来自此前ping。
- 两端UDP可用不等于指定UDP入站端口可达；云端自身HTTPS通过也不代表跨节点直连成功。尚不能判定Clash为唯一原因。
- 待负责人决定的最小试验：先核实服务器Tailscale实际UDP监听端口、绑定安全组及Windows有效防火墙规则；若符合默认41641，考虑仅允许办公电脑当时公网出口IPv4/32访问云端UDP 41641，必要时增加对应Windows规则。不得放行其他来源、端口或应用8000/4173，不启用Funnel。若源地址无法确认或规则影响其他实例，停止并重新确认。
- 这会新增公网UDP入站许可，超出此前禁止新增公网端口的边界，尚未执行，必须另行明确授权。记录原规则和新增规则ID；试验后未直连或下载仍失败则移除仅本次新增规则并核验恢复，不触碰既有规则。成功也须用完整资源下载、PDF预览及鉴权边界验证，不能以netcheck通过代替。
- 无新增权限路径：保留现有受控中继入口与失败记录；不再重复相同测速，也不将冷加载门禁标为通过。代理分流对照是另一选择，但需先确认具体Clash客户端与模式、备份规则并授权，不能贸然停用影响用户联网的代理。
- 官方依据：[防火墙端口说明](https://tailscale.com/docs/reference/faq/firewall-ports)、[设备连通性](https://tailscale.com/docs/reference/device-connectivity)。官方默认端口不能代替本机实际监听核验，试验不保证直连成功。

### 2026-09-11 下午自身HTTPS对照

- 自动化助手只读命令 `cmd-8a94fnrr`（ai-bid-worker-self-https-readonly），执行 `inv-r8hn000u9d`，15:40:49～15:40:55，ExitCode 0。通过Tailscale状态取得自身DNS，仅用于请求，不输出DNS/令牌，不读取秘密配置。
- 服务器通过自身HTTPS读取同一worker三次均200、完整1,369,825字节，1999/1744/1709毫秒，返回 `SELF_HTTPS_WORKER_OK`。包含PowerShell客户端开销，不等同于纯服务响应时间，不覆盖跨节点传输。
- 随后办公电脑同一资源GET返回200，但20.011秒仅收到115,200/1,369,825字节并超时。下午仍可复现端到端传输慢，不能以服务器自身通过宣布故障修复。
- 办公电脑Tailscale netcheck摘要：UDP/IPv4/IPv6=true，MappingVariesByDestIP=true，UPnP/PMP/PCP=false，PreferredDERP=2。仅保留能力摘要，不输出地址。云服务器侧netcheck尚未执行；本机结果不能代表两端，也不能据此归因Clash。
- 当前主要差异在跨节点访问；下一批先补云端连通性摘要并形成最小修复选择，不再反复重复相同下载测速。任何代理、路由、安全组或生产配置调整先说明影响并取得相应授权。端到端门禁仍失败，不推进其他验收。

### 2026-09-11 云端回环对照完成

- 腾讯云自动化助手创建独立诊断命令 `ai-bid-worker-loopback-readonly`，命令ID `cmd-bda7elnd`；执行 `inv-r8hd5g0stv`，10:39:29～10:39:33，ExitCode 0。
- 仅读取部署目录worker文件长度并GET `http://127.0.0.1:4173/pdf.worker.min.mjs` 三次，不读秘密配置，不调用业务接口，不改服务、网络或数据。自动化助手保留诊断命令/执行记录。
- 三次均200、完整1,369,825字节，耗时1539/1462/1406毫秒，返回 `LOOPBACK_WORKER_OK`。耗时包含PowerShell客户端处理，不等同于纯服务器响应时间；字节长度一致不是内容哈希校验。
- 与办公电脑40秒仍未完整下载相比，当前优先排查回环以外的Tailscale Serve HTTPS、网络中继与办公电脑路径；没有证据需要修复PDF解析代码。不能仅靠本对照将Clash或DERP认定为唯一原因。
- 下一最小批次：只读检查两端Tailscale连通能力及服务器自身HTTPS资源访问，进一步区分Serve与跨节点路径。不自动停用用户代理，不开放新端口、不更改安全组。端到端冷加载仍未通过，暂不推进其他验收。

### 2026-09-11 冷加载只读复测

- Tailscale三次探测均经DERP(lax)中继，延迟471/906/398毫秒，未建立直连。探测有响应；退出1表示未建立直连，不代表节点离线。首次受限会话无法访问服务管道，经批准提升权限后取得结果。
- worker普通GET：200、首字节4.252秒，40.005秒收到983,552/1,369,825字节并超时。
- 支持压缩的GET对照：200、首字节2.086秒，40.004秒收到1,212,928/1,369,825字节并超时。未记录实际Content-Encoding，不能宣称压缩生效或提速；两次完整下载检查均失败。
- HEAD通过：200、text/javascript、长度1,369,825，具有ETag、Last-Modified和Accept-Ranges，未出现Cache-Control；不能仅据此认定浏览器完全不缓存。
- 证据支持优先检查中继路径与大资源传输，不足以证明中继是唯一原因。CLI不使用浏览器缓存，本轮不是浏览器冷/热缓存对照。
- 下一步先取得云服务器回环同一资源测速，区分服务器与传输路径影响。未修改代理、防火墙、安全组、Tailscale配置或正式代码；问题未修复，不继续其他验收项。

1. 首次PDF曾停留Loading PDF，随后正文成功显示。独立worker下载测速HTTP 200，但45秒仅收到754,176/1,369,825字节并超时；这是传输慢的证据，不足以判定服务或应用代码根因，也不能将该测速记为通过。下一批先只读核对受控网络链路与冷/热加载差异。
2. 本批未完成窄屏/侧栏组合矩阵，不能宣称完整响应式验收。
3. 报告确认实际文件下载，尚未逐项核对DOCX正文与渲染版式。
4. Word夹具不覆盖真实Office/WPS转换，也不覆盖真实扫描PDF/OCR。不得据此宣称这些云端能力通过。
5. 保留原数据、备份、独立合成根及生成报告；当前生产入口展示合成项目，不能误称已恢复原空库。清理或运行数据切回须先明确边界与权限。

不在此文档写入共享令牌、API Key、具体tailnet DNS或认证链接。
