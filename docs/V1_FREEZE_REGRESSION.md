# V1候选版本冻结回归记录

日期：2026-09-03
候选版本：`1.0.0-rc.1`
环境：纯合成隔离SQLite、上传目录、报告目录，后端 `127.0.0.1:8011`，前端 `127.0.0.1:5174`。

## 隔离边界

- 使用 `prepare_auth_regression.py` 新建两条合成项目：PDF和Word各一条。
- Word预览PDF预生成，未调用Microsoft Word或WPS。
- 使用非空测试Key和 `http://127.0.0.1:9` 不可达DeepSeek地址；Dashboard摘要与项目对比AI请求均安全返回502，没有连接真实模型。
- 未连接或修改用户 `bid.db`、`uploads`、`reports`，未读取真实 `.env`。

## HTTP与启动证据

- 生产启动预检通过：数据库、上传、报告、鉴权配置均正常。
- 匿名根接口200、匿名存活探针200、授权就绪探针200。
- 共享模式文档入口授权访问404，确认生产文档关闭。
- 初始无凭据项目列表和Dashboard均401；正确凭据后两者均200。
- PDF原件、Word预览元数据和Word预览PDF均200。
- 风险报告与项目对比报告POST均200。
- Dashboard AI摘要和项目对比AI只访问不可达测试地址并返回502，页面保留非AI分析结果。
- 回归完成后前后端进程停止，8011和5174监听端口均关闭。

## 浏览器交互证据

- 无凭据时自动显示访问凭据弹窗；凭据只保存在当前页面内存。
- 首次检查发现Dashboard恢复但项目中心未重试；改为由凭据启用状态统一触发两个请求后复验通过，两条合成项目均出现。
- Dashboard 7天切换30天后，周期说明和统计文案同步更新。
- PDF项目加载1页响应式预览；Word项目显示预生成的1页“Word版式预览”和页码。
- 两个项目均可加入对比，对比结果显示双方100分、0项风险及持平推荐；AI不可达不影响本地对比结果。
- 风险检查器正确显示100分、0项风险和空状态。
- 两类报告导出操作均触发带鉴权POST并返回200。
- 控制台只观察到既有Ant Design `List`及`Alert message`弃用提示，未发现凭据或业务响应输出。

## 结论与剩余门禁

候选版本的隔离生产启动、鉴权和V1核心浏览器链路通过。本记录不代表正式发布；工作区旧入口删除和大量未跟踪成果仍需形成正式Git基线，之后才能进入用户签署和 `v1.0.0` 标签步骤。
