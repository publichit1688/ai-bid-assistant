# AI标书助手 1.5.0-rc.1 候选验收签署单

日期：2026-09-04

状态：项目负责人已签署；候选未提升正式版本，未创建标签、推送或部署。

## 候选身份

- 候选版本：`1.5.0-rc.1`
- 最终候选提交：`33ad2bcee8752475caa53feecb2a336c6777cef7`（短哈希 `33ad2bc`）
- 提交说明：`feat(workbench): establish v1.5.0-rc.1 candidate`
- 当前候选提交未附加Git标签；既有本地 `v1.0.0` 标签仍指向 `c0bfd6304a5c8f92d2708c0042d32744e3dce9dc`。

## 验收范围

- V1.5智能编标工作台的目录建议、评分点提取、逐项人工审核、评分点—章节人工映射、响应材料及修订快照。
- 独立前端工作台入口、项目隔离、覆盖/缺口/材料状态展示和显式AI调用确认。
- 两版SQLite增量迁移、工作台API、自动化测试、纯合成浏览器回归及发布门禁材料。
- 保持V1的PDF预览与风险高亮、Dashboard预警穿透、项目中心、风险检查器、项目对比、报告导出和AI缓存能力。

明确不包含：一键生成整本标书、自动生成正文、无来源补写、自动分派责任人、自动投标和复杂目录树拖拽编辑。

## 复核证据

- 候选提交共35个文件，变更为4945行新增、36行删除；文件清单与候选说明一致。
- 提交文件不包含 `.env`、SQLite数据库、上传文件、报告、真实样本、`node_modules` 或 `dist`。
- 根版本、FastAPI版本及前端包/锁文件均为 `1.5.0-rc.1`。
- 建立候选时完整后端回归为167 passed、1 skipped；前端lint/build、秘密扫描、Python依赖审计及npm缓存离线审计通过。
- 本次签署前版本与工作台边界聚焦回归为4 passed；详细复核记录见 `docs/ACTIVE_TASK.md`，门禁和浏览器证据分别见 `docs/V1_5_CANDIDATE_AUDIT.md` 与 `docs/V1_5_WORKBENCH_REGRESSION.md`。

## 已知限制

- 前端主包约2.46 MB，存在拆包提醒；不阻塞当前最小闭环。
- Ant Design `List` 有下一主版本弃用提醒，当前功能正常。
- 当前Windows环境无符号链接权限，对应用例保持跳过。
- 工作台AI结果依赖文件文字质量，必须逐项人工审核；当前迁移只支持SQLite。

## 数据、安全与回滚边界

- 签署不触碰用户数据库、上传文件或报告，也不授权真实模型调用。
- 本地 `v1.0.0` 标签继续提供既有正式版定位；任何版本切换或回滚操作仍需另行确认并先保护当前工作区。
- 本签署只确认上述候选可进入后续正式版本决策，不授权提升为 `1.5.0`、创建或移动标签、推送、部署。

## 项目负责人确认

项目负责人于2026-09-04确认：

> 确认签署 1.5.0-rc.1 候选版本验收，最终候选提交为 33ad2bc，不创建标签、不推送、不提升正式版本。

签署状态：已确认。

签署确认的最终候选提交为 `33ad2bc`。本次确认不创建标签、不推送、不提升正式版本，也不授权部署。
