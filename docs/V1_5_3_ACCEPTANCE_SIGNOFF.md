# AI标书助手 1.5.3-rc.1 候选验收签署单

日期：2026-09-24

状态：项目负责人已签署；腾讯云隔离候选门禁已通过。未创建标签、未推送候选、未提升正式版本、未部署。

## 候选身份

- 候选版本：`1.5.3-rc.1`
- 修复基线：`b3c6ada4e7e662624ac44a0a2c628f32ad767a3a`
- 最终候选提交：`c9285cf8bad159e451364f04fe4b9e91b84ead4d`
- 候选不创建或移动标签；已发布 `v1.5.2` 保持不变

## 验收范围

- 修复风险报告可选采购清单导致的章节重复或跳号。
- 覆盖缺字段、空列表、非法类型和非空列表四类章节顺序。
- 保持报告正文、评分、页码映射、样式和数据格式不变。
- 将根版本、FastAPI动态版本和前端包/锁文件统一为 `1.5.3-rc.1`。

## 签署前证据

- 完整发布门禁通过：182 passed、1 skipped；秘密扫描、Python依赖审计、前端lint/build和npm生产依赖审计通过。
- 版本一致性通过，前端构建标识为 `frontend@1.5.3-rc.1`。
- 两份纯合成报告已完成4页视觉检查，章节连续且未见裁切、重叠或乱码。
- 候选文件边界、父提交、版本、标签和数据排除项已完成只读复核。
- 腾讯云隔离候选门禁执行 `inv-r92h1j0q6h` 成功：System账户符号链接测试1 passed，完整后端183 passed；秘密扫描、Python依赖审计、前端lint/build和npm生产依赖审计通过。
- 云端隔离候选保持 `c9285cf / 1.5.3-rc.1`，正式目录保持 `a4d837a / 1.5.2` 且已跟踪工作区干净。

## 签署边界

签署只确认候选可进入后续腾讯云候选门禁和正式版本决策，不自动授权提升正式 `1.5.3`、创建标签、推送候选或部署。回滚使用可审计的 `git revert`，不使用危险重置，不删除用户数据。

## 项目负责人确认

项目负责人于2026-09-24确认：

> 确认签署 1.5.3-rc.1 候选版本验收，最终候选提交为 c9285cf8bad159e451364f04fe4b9e91b84ead4d；签署只授权后续腾讯云候选门禁，不创建标签、不推送、不提升正式版本、不部署。
