# P2 格式回归记录（脱敏）

日期：2026-08-29
授权：项目所有者明确授权使用 `backend/uploads/` 全部现有文件，并允许调用 DeepSeek。
环境：系统临时目录、隔离 SQLite、隔离 uploads/reports；原文件只读并复制到 Git 忽略目录。

## 已执行

| 样本编号 | 格式特征 | 页数 | HTTP | 评分 | 风险总数 | 高/中/低 | 总扣分 | 格式链路 |
| --- | --- | ---: | ---: | ---: | ---: | --- | ---: | --- |
| RB-PDF-TEXT-SHORT | 可复制文本短PDF | 16 | 200 | 0 | 6 | 4/2/0 | 116 | 通过 |
| RB-PDF-TEXT-LONG | 可复制文本长PDF | 70 | 200 | 0 | 7 | 6/1/0 | 153 | 通过 |
| RB-PDF-SCAN | 每页含图像且有可提取文本 | 49 | 200 | 64 | 3 | 1/2/0 | 36 | 通过 |

以上只证明保存、解析、真实 AI 调用、统一评分和隔离数据库写入链路成功。项目名称、风险语义、页码、高亮和引用准确性尚未人工核对，保留给 P2-03。

## 未覆盖

- DOCX：授权目录中没有 DOCX 样本。
- 纯扫描 PDF：现有图文混合样本每页含图像，但同时存在可提取文本，不能冒充纯扫描件。
- 复杂表格：未确认现有样本具备足以建立固定预期的复杂表格。
- 异常文件：本批未构造，留作后续无敏感数据测试。

## 数据安全

- 原文件未修改、未删除。
- 完整 AI 结果只保存为 `regression_samples/<编号>/actual.local.json`，该目录已被 Git 忽略。
- 终端只输出样本编号、HTTP状态和统计汇总，没有输出正文、真实项目名或模型原文。
- 第一次运行完成后，Windows 因 SQLAlchemy 引擎句柄未释放而无法自动删除隔离临时目录；运行器已增加显式 `engine.dispose()`。遗留目录仅含本轮临时副本和隔离数据库，不属于项目数据。
