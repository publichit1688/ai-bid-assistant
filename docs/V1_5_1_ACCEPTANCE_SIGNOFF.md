# AI标书助手 1.5.1-rc.1 候选验收签署单

日期：2026-09-05

状态：项目负责人已签署；候选随后经单独授权提升为本地正式 `1.5.1`，未创建标签、推送或部署。

## 候选身份

- 候选版本：`1.5.1-rc.1`
- 最终候选提交：`814c4bf8be0198d0e48b1d93ee3bb035bfb93700`（短哈希 `814c4bf`）
- 父提交：正式 `1.5.0` 发布提交 `a4295e60cc5dd8a9e4f2a45a8c362db5867e15f6`
- 提交说明：`perf(maintenance): establish v1.5.1-rc.1 candidate`
- 候选提交没有附加Git标签；既有本地 `v1.5.0` 标签仍指向父提交。

## 验收范围

- 修复Dashboard多项目聚合只使用最后一个项目的数据一致性问题。
- 将智能编标工作台、Dashboard图表和文档预览拆为按需加载模块，同时保持原有状态与操作契约。
- Dashboard使用ECharts模块化入口，保留折线图、柱状图、饼图及现有交互。
- 保持PDF/Word响应式预览、风险跳页与高亮、Dashboard预警穿透、项目中心、项目对比、报告导出和AI缓存能力。
- 增加合成回归夹具、前端拆包/预览边界契约测试及维护审计材料。

本候选不增加一键整本标书、自动正文、自动分派或自动投标功能。

## 复核证据

- 候选提交共22个文件，变更为1562行新增、789行删除；文件清单与候选审计一致。
- 提交文件不包含 `.env`、SQLite数据库、上传文件、报告、真实样本、`node_modules` 或 `dist`。
- 根版本、FastAPI版本及前端包/锁文件均为 `1.5.1-rc.1`。
- 建立候选时完整门禁为177 passed、1 skipped；秘密扫描、Python依赖审计、前端lint/build及npm生产依赖审计通过。
- 签署前只读身份、标签、文件边界和差异格式复核通过；版本、品牌/拆包与文档预览边界聚焦回归为12 passed。

## 已知限制

- Dashboard图表异步块为585.56 kB，Vite仍提示单块超过500 kB；相比模块化前已下降48.5%，当前不通过提高阈值隐藏提醒。
- 当前Windows环境无符号链接权限，完整门禁中对应备份安全用例保持1项跳过。
- 保留1个既有Starlette/httpx弃用警告，未影响本候选接口测试。
- 纯扫描样本仍有1项风险因无可靠OCR匹配而保守不画框；当前Word版式验证使用WPS，不代表Microsoft Word分页完全一致。

## 数据、安全与后续动作边界

- 签署不触碰用户数据库、上传文件、报告或真实样本，也不授权真实模型调用。
- 本签署只确认该候选可作为后续正式版本决策基线。
- 签署不授权提升为正式 `1.5.1`、创建或移动标签、推送或部署；这些动作必须另行明确授权。
- 若候选需要撤销，使用 `git revert 814c4bf` 形成可审计反向提交，不使用 `git reset --hard`，不删除用户数据。

## 项目负责人确认

项目负责人于2026-09-05确认：

> 确认签署 1.5.1-rc.1 候选版本验收，最终候选提交为 814c4bf，不创建标签、不推送、不提升正式版本、不部署。

签署状态：已确认。

签署确认的最终候选提交为 `814c4bf`。后续正式版本提升由项目负责人另行授权；截至正式提升批次，仍未创建标签、推送或部署。
