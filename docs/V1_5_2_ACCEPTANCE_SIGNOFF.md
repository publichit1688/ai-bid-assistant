# AI标书助手 1.5.2-rc.1 候选验收签署单

日期：2026-09-09

状态：项目负责人已于 2026-09-09 确认签署；仅授权后续腾讯云候选门禁。

## 候选身份

- 候选版本：`1.5.2-rc.1`
- 最终候选提交：`e4cf9dd10bad4ef257fd92206dcdce582b2edc74`（短哈希 `e4cf9dd`）
- 父提交：`5cca7ad6fc44f51ed293042c8d61de85a3fe4e77`
- 提交说明：`fix(backup): establish v1.5.2-rc.1 candidate`
- 候选提交没有附加 Git 标签；已发布 `v1.5.1` 仍解引用到 `b542d010d2679c0265fcd4cd50cd420fcd39954e`。

## 验收范围

- 修复 Windows System 账户下拒绝符号链接备份时可残留快照目标的原子性问题。
- 在创建快照目标前检查上传和报告源目录，复制阶段继续二次检查。
- 保持 SQLite 在线备份、不覆盖、SHA-256 清单和安全恢复契约不变。
- 将根版本、FastAPI 运行版本及前端包/锁文件统一为 `1.5.2-rc.1`。

本候选不增加 Dashboard 功能、不改变 PDF/Word 预览、风险高亮、项目对比、报告导出、AI 缓存或智能编标工作台。

## 复核证据

- 候选提交共 11 个文件，123 行新增、13 行删除；代码改动集中于备份源预校验。
- 提交文件不包含 `.env`、SQLite 数据库、上传文件、报告、真实标书、`node_modules` 或 `dist`。
- 根版本与前端包/锁文件根版本均为 `1.5.2-rc.1`，第三方 `base64-js@1.5.1` 没有被误改。
- 建立候选时的统一门禁：177 passed、1 skipped；秘密扫描、Python 依赖审计、前端 lint/build 和 npm 生产依赖审计通过。
- 签署前身份、父提交、标签、文件边界和差异格式复核通过；版本与备份聚焦回归为 2 passed、1 skipped。

## 已知限制与签署条件

- 本机普通 Windows 账户无创建符号链接权限，完整门禁的对应用例保持 1 项跳过。
- 正式提升或继续生产配置前，必须在腾讯云 System 账户环境对本候选复跑符号链接用例和前端门禁。
- 保留 1 个已知 Starlette/httpx 弃用提示；Dashboard 图表块保持 585.56 kB 的已审计构建提示。

## 数据、安全与后续动作边界

- 签署不触碰用户数据库、上传文件、报告或真实标书，也不授权真实模型调用。
- 签署只确认该候选可用于后续云端门禁和正式版本决策。
- 签署不授权提升为正式 `1.5.2`、创建或移动标签、推送或部署；这些动作必须另行明确授权。
- 如需撤销候选，使用 `git revert e4cf9dd` 形成可审计反向提交，不使用 `git reset --hard`，不删除用户数据。

## 项目负责人确认

项目负责人确认原文：

> 确认签署 1.5.2-rc.1 候选版本验收，最终候选提交为 e4cf9dd；签署只授权后续腾讯云候选门禁，不创建标签、不推送、不提升正式版本、不部署。

签署状态：已确认。该签署不授权创建标签、推送、提升正式版本或部署。
