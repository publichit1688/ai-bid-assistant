# 当前执行入口（2026-09-24）

- 正式发布提交 `3c7aebec1901f34e4578da4268d47e3872c27864` 与 annotated标签 `v1.5.3` 已建立；标签对象 `f859d426ba829670b4a0d1520b2ec03b9d4f66e8`，解引用指向正式提交。
- `main` 与 `v1.5.3` 已推送并完成远程身份复核；标签保持固定在正式发布提交 `3c7aebe`，不会随本次状态文档提交移动。
- 当前停止。腾讯云正式备份、部署及回滚门禁继续等待单独授权，不自动执行。

- 当前授权：将已签署且通过腾讯云候选门禁的 `1.5.3-rc.1` 提升为正式 `1.5.3`，完整发布门禁通过后创建正式提交和 annotated 标签 `v1.5.3`，再推送 `main` 与标签。
- 腾讯云正式备份、服务切换、部署和回滚演练不在本批授权内；推送完成后停止并等待单独部署授权。
- 正式 `1.5.3` 完整发布门禁已通过：182 passed、1 skipped，秘密扫描、Python/npm依赖审计、前端lint/build通过；下一动作是创建正式发布提交和 annotated 标签。

- 最新：项目负责人已签署最终候选 `c9285cf8bad159e451364f04fe4b9e91b84ead4d`，且腾讯云隔离候选门禁 `inv-r92h1j0q6h` 通过：System符号链接1 passed、完整后端183 passed、秘密扫描、Python/npm依赖审计、前端lint/build通过。
- 正式腾讯云目录仍为 `a4d837a / 1.5.2`，服务、数据和配置未切换。当前停止在候选门禁完成状态；下一步如提升正式 `1.5.3`、创建标签、推送或部署，必须取得新的明确授权。以下为历史。

- 最新：固定修复提交 `b3c6ada` 已推送到指定GitHub远程并复核一致；随后版本统一为 `1.5.3-rc.1`，完整本地门禁通过（182 passed、1 skipped，秘密扫描、Python/npm依赖审计、前端lint/build通过）。
- 当前按负责人授权建立一个本地候选提交，并保留待签署状态；不创建标签、不再次推送、不执行腾讯云部署。部署与回滚方案已准备，正式云端动作须另行授权。以下为历史。

- 最新：负责人已明确授权按当前交付清单将报告章节编号补丁的7个已跟踪文件创建为一个本地Git提交；最终身份以Git回执为准。
- 本次只逐文件暂存并提交该清单；不改版本、不打标签、不推送、不部署。末页留白和引用跨页继续列为排版优化项，不扩大本补丁范围。以下为历史。

- 最新：获准使用本机LibreOffice后，两份纯合成报告以独立临时配置成功渲染，各2页且全部查看；编号连续、文字/表格无裁切重叠。配套渲染限制已解除，不重复请求该授权。
- 下一批审阅本地编号补丁和交付清单。保留末页留白及有采购清单样本原文引用跨页的排版优化项，不宣称完整排版验收通过；不改原报告，不处理真实业务文件，不提交或部署。以下阻塞为历史。

- 最新有效状态：报告证据核查发现章节编号重复/跳号，report.py已本地最小修复；新增4项回归先红后绿，报告契约8 passed/1 warning。未提交、未部署，线上与留存原报告未改变。
- 当前优先：完成修复后报告视觉复验。documents技能要求配套LibreOffice且禁用桌面安装版，当前运行时没有配套soffice，渲染尚未执行；先解决合规可用渲染路径，不把XML/pytest当作视觉验收，不重复跑无关全量测试。通过后再审阅交付，提交/部署需另行授权。以下为历史。

- 最新有效状态：线上合成PDF四侧栏组合缩放高亮、2→1→2清除/恢复及PDF↔Word跨文件清理均通过；1280×800无横向溢出。视口及侧栏恢复，未改线上代码或数据。
- 下一最小批次：读取P6-31C验收记录，核对云端既有合成报告内容/版式证据，选择尚未覆盖的最小项；不重新部署、不调用AI、不删除夹具。不把本轮高亮验收当作全部尺寸或真实Office转换通过。以下为历史批次。

- 最新有效状态：线上合成Word高亮补验通过；本日完成第1页清除/第2页恢复、双折叠/仅右折叠/恢复双展开，无横向溢出；左折叠使用09-22实测证据。临时视口已恢复，保留已有页面。
- 下一最小批次：线上合成PDF缩放、切页及跨文件高亮清理验收；不重新部署、不调用AI、不改数据。Word预生成预览不等于真实Office转换，其他验收缺口保持未完成。以下为历史批次状态。

- 2026-09-22当前有效状态：a4d837a已部署，成功执行inv-s8xv1kg3ct（91秒），备份及29项Node/lint/build/回环门禁通过，办公HTTPS首页+新index-vANbzg-p.js+项目2条复验通过。下方上传阻塞和未部署为历史。
- 下一最小批次：仅用保留合成项目验收线上PDF/Word缩放后高亮、切页和侧栏交互；不调用真实AI，不上传/删除业务文件，不重复部署。版本1.5.2，部署记录仅本地文档未提交；备份、旧dist及bundle保留。

- 2026-09-22最新授权：部署固定a4d837a，先备份/预检、失败回滚，其他发布/网络/真实AI边界不变。本地23KB增量bundle已通过Git校验，腾讯云直接上传文件选择器超时，尚未传输或改变线上。
- 下一步恢复Chrome扩展文件上传能力，使用目标专属部署目录校验SHA-256与Git身份，再准备并验证正确12文件白名单、失败终止、备份和回滚脚本；不执行旧4cc85ed草稿，不把历史预检当作本次通过。

- 2026-09-22已获当前12文件本地补丁提交授权；29项Node、秘密扫描和格式复验通过。按精确清单提交后停止，最终提交以Git回执为准；不改版本、不打标签、不推送、不部署。下方等待授权不再有效，线上仍未包含新高亮修复；同步或部署需另行授权。

- 2026-09-22当前有效状态：12文件交付审阅完成；修复入口诊断清理异常掩盖原超时，29项Node及lint通过。HEAD4cc85ed、暂存为空；等待负责人明确授权当前清单12文件本地提交，不沿用历史授权，不重复审阅或云端部署。下方“下一批审阅”为已完成历史。

- 2026-09-22最新：云端只读检查ExitCode 0；当前HEAD4cc85ed、已跟踪文件无差异，首页/入口JS与frontend/dist文件一致，详见PROJECT_STATE。下方“当前HEAD未知”阻塞已解除；未覆盖所有拆分资源/依赖或后端进程身份，不标记全系统审计完成。
- 下一最小批次：审阅新增入口脱敏诊断和累计12文件清单，确认提交边界。高亮修复未提交/部署，不沿用历史授权；不重复部署或修改网络。

- 最新覆盖：脱敏阶段诊断落地，7项入口测试与lint通过；沙箱外完整入口复验退出0，固定index-CDm_oRGG.js、项目列表2条通过。此前入口失败保留为历史，根因未确认，不再作为当前持续阻塞。
- 下一批恢复云端只读核查，核对当前HEAD、Git差异与托管目录；入口成功不能替代逐文件一致性。累计候选清单现为12文件，需审阅新增诊断并另行取得提交授权，下方10文件条目为历史。不部署或变更网络。

- 最新优先项：部署一致性审计尚未完成。历史云端日志确认4cc85ed和index-CDm_oRGG.js，但不能替代服务器当前源码/托管目录逐文件核对。日志首行Continue=Stop错误需在下次部署前修正并验证失败终止策略，不复用未经审查的旧脚本。
- 本批Tailscale只读查询确认本机Running及唯一目标Online/Active；沙箱外完整入口检查仍退出1，原因未细分。下一批先恢复云端只读检查通道，核对当前HEAD、Git差异和实际静态托管文件；不反复跑全量测试或盲目部署。
- 本批只更新审计文档。下方10文件补丁等待新提交授权的边界不变；其中累计文档包括本次核查记录。所有历史成功/失败均按当时证据理解，不代表持续状态。

- 当前状态：PDF/Word高亮切页、跨文件和侧栏补验通过，代码审阅及LOCAL_PATCH_HANDOFF.md当前10文件清单完成。等待本次本地补丁提交授权；不重复已完成回归或沿用历史清单授权。HEAD4cc85ed、版本1.5.2，线上尚未部署高亮修复。下方定位/补验指令为历史记录。

- 最新：缩放高亮已本地修复，26项Node、lint/build及6项后端契约通过；隔离Chrome确认Word左栏折叠后第2页橙色高亮保持。下一批补PDF/切页/侧栏组合回归，随后审阅交付；线上尚未部署此修复。下方“先定位”已被本记录取代。

- 最高优先：本地复现正式Word风险高亮在侧栏折叠/画布缩放后消失。1280×800四组合布局和第2页保持正常，但高亮持续性未通过；暂停其他尺寸验收，先定位并修复该问题。

- 最新完成：正式PDF 1280×800四侧栏组合布局检查，无页面横向溢出，画布适配容器；已恢复视口并关闭临时页。
- 下一最小批次调整为正式Word 1280×800同组验收，再补其他尺寸；下方泛化“检查Chrome连接”已有成功恢复证据。

- 本地全量回归已完成：178 passed、1 skipped、1 warning，93.56秒；符号链接测试受Windows权限限制。下方历史“继续稳定观察”不作为重复测试任务。
- 下一批：检查Chrome连接，按P6-31C补验正式合成PDF/Word的响应式尺寸与侧栏组合，记录具体视口和结果；不调用真实AI或删除夹具。
- 后续缺口：冷加载测量、云端报告内容/版式、真实Word转换。预生成Word预览不能替代真实转换。
- 首页HEAD 200仅证明当次首页可达；完整资源/API自动检查仍待补验。当前执行环境若遇网络沙箱限制，应按权限机制重试获准的只读检查，不能直接判定线上故障。

# 历史批次记录

# 2026-09-20 预览边界回归复跑

- [x] 从 `backend` 目录使用 `.venv\Scripts\python.exe` 复跑 `tests/test_frontend_preview_boundary.py`：6 passed、1 warning。
- [x] 根目录启动导致的 `app` 导入错误已通过修正工作目录排除，不作为业务阻塞。
- [ ] 继续线上稳定观察，保持版本1.5.2功能冻结。

# 2026-09-20 终端 HTTPS 验证完成

- [x] 负责人使用 PowerShell `curl.exe -I` 验证正式 Tailscale HTTPS 入口，收到 `HTTP/1.1 200 OK`、HTML 类型和 Caddy 响应头。
- [x] 清除“终端 HTTPS 检查暂记为环境阻塞”的当前状态；未修改网络、代理、服务器、代码或数据。
- [ ] 继续线上稳定观察，保持版本1.5.2功能冻结。

## 2026-09-20 稳定观察复核

- [x] 自动只读复核未改变线上结论；当前执行环境的 Schannel `SEC_E_NO_CREDENTIALS` 仅记录为本机复核限制。
- [ ] 继续使用已验证的办公电脑入口做只读观察；不关闭证书校验，不调整代理，不重复部署。

# 当前活动任务

## 2026-09-20 正式 Word 风险页码复验完成

- Auth Word 线上历史分析只读回归通过：风险定位后预览与详情均为第2页，原文高亮文本一致。
- 下一步继续稳定观察，暂不增加新功能或版本变更。

## 2026-09-20 线上入口稳定观察

- 浏览器只读验证通过；终端 `check-deployed-entry.mjs` 受本机 TLS/代理链限制失败，已记录为环境阻塞。
- 下一步保持线上稳定观察，不调整代理/证书，不重复部署。

## 2026-09-20 前端生产构建复跑完成

- Vite 生产构建成功；大 chunk 提示仍为已知非阻塞项。
- 下一步继续线上稳定观察，不扩展 Dashboard，不自动发布新版本。

## 2026-09-20 全量后端回归完成

- 全量 pytest 178 passed、1 skipped、1 warning；秘密扫描通过。
- 当前没有新增阻塞，继续保持1.5.2稳定观察与功能冻结。

## 2026-09-20 后端回归契约修复完成

- 更新过时测试断言以匹配当前统一风险预览页解析器；20项后端相关回归通过，安全扫描通过。
- 下一步继续线上稳定观察；本批不新增功能、不部署、不推送。

## 2026-09-20 部署后门禁复跑完成

- 23 项前端 Node 回归与 `npm run lint` 均通过；本批没有代码、数据、版本或部署变更。
- 下一步继续线上稳定观察；若无阻塞，再处理文档收尾，不扩展 Dashboard 功能。

## 2026-09-20 线上页码修复已部署

- `4cc85ed` 已部署成功，腾讯云执行 ID：`inv-r8vt4c0rnc`，ExitCode 0；线上 Word 页码列表/定位回归通过。
- 保持版本1.5.2、无新增公网端口、无真实AI调用；旧命令内容已恢复。下一步仅做线上稳定观察和文档状态收尾。

## 2026-09-20 页码补丁本地提交授权

负责人已明确授权LOCAL_PATCH_HANDOFF.md当前清单10文件的本地补丁提交，版本保持1.5.2。本记录随补丁保存，最终提交身份及成功状态以Git日志和执行回执为准；下方等待授权为历史记录。提交后停止，不创建标签、不推送、不部署。保留全部数据、夹具、旧部署脚本及bundle，不纳入本次提交；线上6427d83尚未包含本修复，后续同步或部署需对应明确授权。

## 2026-09-20 页码补丁审阅完成，等待本地提交授权

- 审阅页码解析、App展示/导航接线、预览映射清空与测试边界，本补丁范围未发现新增阻断项。23项Node测试、lint/build复跑通过，既有大chunk警告保留。本批未复跑浏览器，沿用上一批隔离UI证据。
- docs/LOCAL_PATCH_HANDOFF.md明确当前10文件候选，含累计部署/入口恢复/UI验收记录；数据库、上传/报告、合成夹具、dist、bundle和固定旧提交部署脚本排除并保留。
- HEAD6427d83、版本1.5.2、暂存区为空；未创建提交、标签、推送或部署。线上页码问题尚未修复。下一步等待负责人明确授权这10文件本地补丁提交，不重复审阅，不沿用旧11文件授权；后续部署需另行授权。

## 2026-09-20 页码修复隔离浏览器验收通过

- 新增frontend/scripts/preview-risk-pages.mjs，仅回环5176、合成API、读取既有合成PDF；所有非GET请求拒绝，无后端/数据库/AI或外部代理。
- Chrome实测Word原页1映射2：点击前列表第2页，点击后预览/详情第2页；切换PDF无Word映射仍定位原始第2页。使用已有预生成文件，不代表真实Office转换。
- 23项Node测试、lint/build通过（既有大chunk警告保留）。临时服务停止，5176无监听，临时标签页关闭，夹具保留。
- 下一批审阅页码修复及累计记录，整理逐文件交付清单；未授权创建本批新提交、推送或部署，线上6427d83不变。

## 2026-09-20 Word风险列表页码本地修复

- 确认右侧风险列表直接显示item.page，而点击定位使用wordPreviewRiskPages。新增riskPreviewPage.js统一渲染页码解析，列表和点击导航共用；原始risk.page及报告逻辑不改动。
- 新增4项回归：UI接线、Word原页1映射2且数据不变、PDF/缺映射回退、非法值与页数边界。接线回归修复前失败，修复后合计23项Node测试通过；lint/build通过，既有大chunk警告保留。
- 本批仅本地代码和文档，未提交/部署/推送，不改版本或正式数据。线上6427d83仍存在列表页码问题，不能声称线上已修复。
- 下一批使用隔离合成环境补验列表第2页、点击详情第2页和PDF回归，再审阅交付；本批未做修复后的浏览器验收，真实Word转换仍未验收。

## 2026-09-20 正式合成Word预览部分通过，页码显示待修复

- Auth Word Project已有版式预览显示2页；评分92分、中风险1项、扣8分。点击风险定位至第2页，截图确认AUTH WORD RISK PAGE 2文字高亮、详情页码第2页及原文一致。
- 新发现一致性问题：风险列表仍显示第1页，而定位后的详情/渲染页为第2页。本批不标记全部通过。初步源码检索显示定位使用wordPreviewRiskPages映射，另有直接risk.page展示，尚需定位具体组件和最小回归修复。
- 下一批优先本地复现并修复Word风险列表与渲染页码不一致，测试通过后再整理交付；不直接修改或重新部署正式环境。暂停推进其他验收任务。
- 本地19项既有Node测试通过，但不覆盖本次页码问题；本批仅浏览器验证和文档，无AI调用、重新分析、删除、提交、推送或部署。预生成夹具不等于真实Office转换通过。页面及数据保留。

## 2026-09-20 正式合成PDF预览与风险定位通过

- 复用Chrome正式页面，打开Auth PDF Project历史分析，预览显示2页；评分80分、高风险1项、扣20分，与历史列表一致。
- 点击风险定位从第1页跳至第2页；截图确认红色风险框覆盖AUTH REGRESSION PDF RISK PAGE 2，右侧风险详情页码与原文一致。手动上一页回第1页、下一页回第2页均成功，边界按钮禁用正确。
- 本地19项Node测试通过；本批未重新分析、调用AI、上传/导出/删除、部署或修改服务。只做当前窗口PDF小阶段，不替代正式响应式全矩阵、真实Word转换和报告版式验收。
- 下一批验收正式合成Word已有预览与风险定位（预生成PDF不能视为真实Office转换）；保留当前浏览器页面、全部数据与备份。本次只更新文档，不创建提交或标签、不推送。

## 2026-09-20 正式Dashboard冷启动小批验收

- Chrome正式新页最终正常渲染：Auth PDF Project 80分/1风险、Auth Word Project 92分/1风险；Dashboard项目2、风险2、平均86，历史读取未报错。
- 手动摘要按钮加载阶段禁用，统计完成后可用；摘要保持空状态并显示显式触发提示。本批未点击生成，未上传/删除/导出或修改服务。未采集正式网络请求计数，不以空摘要证明零POST；本地实际处理函数测试覆盖自动加载零POST。
- 新页导航等待曾超时，随后经历空页面与统计加载才完成；未取得可靠加载耗时指标，冷启动性能仍待专项测量。一次控制台错误/警告采样为空，不代表全部会话无错误。
- 本地19项Node回归通过；本批无业务代码改动，仅更新状态记录。下一批继续正式合成PDF风险定位小阶段，真实Word转换、报告版式及完整矩阵仍待验收。版本/提交/部署保持不变。

## 2026-09-20 负责人恢复后入口复验通过

- 负责人反馈已经恢复后，本机实时检查BackendState=Running，目标云端节点Online=true、Active=true。
- 办公电脑只读check-deployed-entry.mjs退出码0：正式HTTPS首页、固定/assets/index-wLcyXqAL.js和/api/files均通过状态/类型/内容校验，projectCount=2。入口连通性阻塞解除；未读取业务正文或调用AI。
- 本批未进行云端配置修改、服务重启或重复部署；具体离线根因与负责人恢复操作未核实，不猜测。6427d83部署记录保持，下一批继续正式页面安全冷启动与合成UI验收，不能将HTTP验证替代完整交互/真实Word/报告版式验收。
- 本次只更新本地状态文档，不提交、不推送、不打标签，保留全部数据与备份。下方离线记录为历史状态。

## 2026-09-20 受控链路定位：目标Tailscale节点离线

- 本机BackendState=Running；按正式域名精确筛选目标节点100.83.20.96，Online=false、Active=false、CurAddr为空，最后在线2026-09-19 13:26:44 UTC（北京时间21:26:44）。这是办公客户端报告，尚未由云端核实原因。
- 三次Tailscale ping（每次5秒）均超时，退出码1。当前证据指向受控节点连通性，不能据此断言云主机停机、应用损坏或代理是根因；不重复部署。
- 浏览器实时列表仅有空的内置浏览器，无Chrome连接。下一步需恢复Chrome扩展连接并打开腾讯云，再只读检查ins-goda9pyq运行状态、云端Tailscale服务/认证/出口及本机4173、8000健康。不要仅反复重试办公HTTPS。
- 本批仅诊断与文档，无网络设置、服务重启、代码、数据、版本、提交、标签或推送变更。6427d83部署成功记录保持，当前入口验收仍阻塞；所有备份/夹具保留。

## 2026-09-20 核实6427d83部署成功，办公入口补验超时

- 已获负责人部署6427d83授权；本次只读取既有执行inv-s8p002g7tq，未重复执行。目标ins-goda9pyq，命令cmd-bkf9vmwh；2026-09-15 16:23:33至16:24:55，82秒，命令成功/ExitCode 0。
- 云端日志确认固定提交6427d83b0660279303e888cff68638c644482597；bundle/Git及运行数据备份通过，19项Node测试、lint/build、部署后健康与回环监听检查通过。保留既有大chunk等构建提示。
- 备份路径C:/AI-Bid-Backups/pre-6427d83-02e56e5610f04692b14b30a2861bd3c6；入口资源/assets/index-wLcyXqAL.js。以上为部署时回执，不等于今日实时服务器检查。
- 今日办公电脑只读HTTPS检查在沙箱内外均失败；独立首页请求10秒超时。Tailscale服务及BackendState均为Running，不能据此认定服务器离线或部署失败。未关闭证书验证、未修改代理/安全组/网络设置。
- 下一小批先只读定位受控入口连通性，再验证固定资源和历史JSON，随后安全冷启动UI。正式完整UI、真实Word转换和报告版式仍待验收；不点击AI生成、不删除夹具，不重复部署。
- 版本仍1.5.2；本次仅更新本地记录，未提交、推送、创建标签或调用外部分析服务。下方为历史状态。

## 当前授权：11文件本地补丁提交（2026-09-15）

负责人已明确授权手动摘要交付清单内11文件创建本地提交，版本1.5.2；授权记录随补丁保存，提交ID及完成状态以Git日志/执行回执为准。提交后停止，不创建标签、不推送、不部署。下一阶段需对应远程同步或部署授权；正式环境尚未获得此补丁，不重复请求已经授予的本地提交授权。下方为历史记录。

## 当前状态：补丁审阅完成，等待本地提交授权（2026-09-15）

审阅发现统计请求乱序问题并完成测试驱动修复；19项Node测试、lint/build通过。11文件候选及排除范围见LOCAL_PATCH_HANDOFF.md，保留所有数据、bundle和旧部署脚本。HEAD68888a8，版本1.5.2，不提交、不推送、不部署；下一步需明确本地提交授权，普通继续不重复同项审阅。正式环境仍有自动摘要行为。

## 当前状态：手动摘要本地UI验收通过（2026-09-15）

Chrome隔离合成服务验证冷启动零请求、手动加载、失败提示/重试、成功展示、相同数据缓存、30/7天切换均通过；最终仅2次合成摘要请求。17项测试、lint/build通过。下一批仅审阅补丁与交付范围，不自动提交/部署；正式环境自动摘要行为尚未改变。下方为历史记录。

## 当前状态：手动摘要本地实现及测试完成（2026-09-15）

负责人已同意本地修改不部署；App统计加载不再自动请求摘要，按钮触发并保留缓存，含重复点击/迟到响应保护。实际处理函数模拟接口5项测试通过，前端合计17项、lint/build通过。下一批本地隔离合成UI验收按钮和冷启动，不调用真实AI；正式68888a8未变，不能直接视为线上问题已解决。未提交或部署，下方为历史记录。

## 当前阻塞：正式页面冷启动可能自动调用AI（2026-09-15）

浏览器已连接但旧应用页已关闭。代码确认首次加载自动POST Dashboard摘要，缓存未命中会调用真实模型并写缓存，故没有直接打开正式页。下一步先确认冷启动方案，不要求用户反复重连浏览器、不重复部署、不将HTTP通过标记为UI验收。建议本地改为手动触发摘要并保留缓存；上线需单独明确授权。

## 2026-09-15 最新状态：终端验收工具完成

已完成只读首页/固定JS/历史JSON脚本和5项测试，正式入口实测通过（index-DtzCARX0.js，2项目），Node总计12项、语法检查、lint/build通过。按用户“终端开发”本批未操作浏览器；不将接口检查替代正式合成UI验收，后续继续该待办，避免自动AI摘要。没有重复部署或创建提交；下方为历史记录。

## 当前状态：68888a8已部署，后续只读验收

腾讯云inv-s8n00m0mqh成功，固定提交68888a8、版本1.5.2；云端备份、7项测试、lint/build、健康和回环监听通过。办公电脑HTTPS新资源index-DtzCARX0.js及历史JSON接口200/2项目通过。备份位置见PROJECT_STATE.md；未使用COS、不推送、不打标签、不改秘密或数据配置。下一小批仅做不触发真实AI的正式合成UI验收；当前部署已完成，不重复执行部署脚本。部署记录未另行提交。下方为历史记录。

## 当前任务：部署68888a8，等待控制台登录

负责人已明确授权部署腾讯云。目标提交68888a825fff708133d323936faecdb2fa136284，保留版本与既有网络安全边界，不推送、不打标签。Chrome控制台已实际跳转微信登录页；需用户完成登录后继续预检/备份/部署/失败回滚。尚未执行任何云端部署步骤，也未授权或执行本次COS对象公有读。后续如需扩大文件公开范围，单独确认；不要重复请求本次部署授权。下方为历史记录。

## 当前授权边界（2026-09-14）

已获11文件本地补丁提交授权，本记录随补丁保存；实际提交ID以Git日志及执行回执为准。本轮完成本地提交核验后停止，不修改版本、不创建标签、不推送、不部署。下一步如需远程同步或云端验收，应明确授权新的提交；此前正式环境、真实Word转换及报告完整版式限制不因本地提交而关闭。下方为历史记录。

## 当前状态：等待本地补丁提交授权（2026-09-14）

本地补丁交付审阅完成，11文件白名单见LOCAL_PATCH_HANDOFF.md；前端7项测试、lint/build及安全扫描通过。未创建提交，HEAD仍8f130f9、版本1.5.2。下一步须明确授权后逐文件暂存并创建本地提交，不打标签、不推送、不部署；执行前重新核对变化。普通“继续自动开发”不视为部署授权，不重复安排同项审阅。下方为历史记录。

## 最新状态：本地预览组合已补齐（2026-09-14）

PDF1024四种侧栏状态及恢复已通过，第2页风险框与详情保留。Node7项、lint/build通过。交付边界见LOCAL_PATCH_HANDOFF.md；下一小批只读审阅本地补丁与逐文件提交候选，不创建提交或部署。正式环境验证、真实Word转换及完整版式限制继续保留。下方为历史任务记录。

## 当前优先：补齐PDF侧栏组合及补丁交付检查（2026-09-14）

隔离合成Word/PDF已完成1024/1280/1440/1920宽度双侧栏展开回归，第2页定位、预览边界正常；Word另完成1024宽度的4种侧栏组合。pytest14项及Node7项通过。下一小批补齐PDF在1024宽度的单/双侧折叠与恢复，随后汇总本地补丁交付检查；尚未授权将当前未提交补丁部署，不自动推送或部署。真实Word转换及正式完整内容矩阵保持待验收，保留所有样本。

## 2026-09-14 本批结果

- 合成Word报告页码补验完成：正式页面风险定位第2页，实际导出报告(7)的风险表第2页、引用PAGE 2、评分92分一致；已核查落地DOCX正文并在合成回归记录保存文件大小和SHA-256。
- 此前报告页码不一致问题关闭；不把预生成夹具补验视为真实Word转换或完整版式验收。应用代码与正式部署未改，样本和报告保留。

## 2026-09-13 当前批次结果

- 同源API修复 `8f130f9` 已部署；执行 `inv-r8kp2q08r0` ExitCode 0。bundle校验、运行数据备份、独立目录构建、首页/历史接口和回环监听验证通过。
- 备份为 `C:/AI-Bid-Backups/pre-8f130f9-10fc74c970a848e7919f41952ced7ec5`；旧资源、数据及原有合成夹具保留。
- 办公电脑HTTPS首页200且入口资源更新为 `index-oM_1rAB-.js`，历史接口200、2个项目；Chrome新页较长白屏后已加载两个历史项目和Dashboard（项目2、风险2、平均分86），无读取失败提示。首次加载慢与报告页码补验继续待办。
- 临时COS对象及空桶均已删除；负责人完成微信验证后，2026-09-13控制台复核存储桶列表“共0项 / 暂无数据”。部署中转清理完成。
- 新增 `deploy/windows/deploy-8f130f9.ps1`；修复预检失败误清理风险和混合分隔符比较。前两次运行均已验证回滚；确认控制台实际保存更新后第三次成功。本机3项API基址测试、PowerShell语法/路径检查及秘密扫描通过。
- 本批无新提交、标签或推送；未修改公网安全组、代理、密钥或业务数据，未调用外部分析服务。P6-31C仍进行中。

## 2026-09-12 问题记录（修复已部署）

- 实际下载报告（6）已找到，36,051字节，正文风险表仍为第1页而引用为第2页；上次仅点击导出并不能证明页码修复端到端通过。
- 新开正式入口显示项目/Dashboard读取失败；同源历史接口独立探测HTTP 200。线上入口资源 `index-BkCPqOEh.js` 的实际API常量为 `http://127.0.0.1:8000`，同时包含 `report_page`。确认生产构建遗漏API配置后回退到访问者电脑地址；旧标签页数据不能代表新部署状态。
- 本地 `frontend/src/api.js` 已改为生产默认同源、开发保留回环地址、显式配置优先；提交 `8f130f9` 已于2026-09-13部署。
- 当前最高优先为修复正式前端构建配置并核对浏览器实际接口请求，随后重新导出合成Word报告验证第2页。P6-31C保持进行中；本批未改生产配置、数据或网络。
- 上传阻塞已解除，bundle已完成云端校验与部署，中转对象及空桶清理完成；后续只处理剩余验收。

更新时间：2026-09-12

## 正式部署收尾

已完成正式环境部署收尾：固定提交 `ad5acb5` 已通过云端 bundle 校验、前端构建、健康检查和回环监听门禁；部署前备份路径为 `C:\AI-Bid-Backups\pre-ad5acb5-r3-20260912-015103`。临时 COS 对象及空桶已删除。本批不再重复部署或删除数据。

2026-09-12正式报告补验完成：`Auth Word Project` 页面点击“导出报告”后界面无错误；由于浏览器扩展不捕获程序化 Blob 下载，未将点击动作当作文件落地证明。后端 `tests/test_report_contract.py` 在 `backend` 工作目录下通过 `4 passed、1 warning`。

## 当前批次

2026-09-12浏览器连接已恢复：Chrome受控入口可接管，已只读打开 `Auth PDF Project` 分析页。当前合成项目显示PDF两页预览、评分80分、1项风险及第2页风险定位；本批未上传文件、未提交表单、未调用AI或修改数据。响应式矩阵、报告内容/版式和真实Word转换仍未完成。

2026-09-12 PDF风险定位补验完成：点击现有风险入口后预览跳转第2页，风险详情页码同步，截图确认红色半透明高亮覆盖合成风险文字；仅执行只读交互，未修改数据。响应式矩阵、报告内容/版式和真实Word转换仍未完成。

2026-09-12 Word预览补验完成：打开 `Auth Word Project` 后显示2页Word版式预览和本机办公软件转换标识；风险入口跳转第2页，风险详情页码同步、风险文本可见。该夹具不替代真实办公文件转换验收，未创建新的WPS任务。

2026-09-12侧栏布局抽查完成：收起并恢复项目中心侧栏后，中央Word预览、顶部导航和右侧风险检查器保持可见；仅为单一桌面视口抽查，完整响应式矩阵仍未完成。

2026-09-12风险检查器布局抽查完成：收起并恢复右侧风险检查器后，中央Word预览扩展且顶部导航、项目中心保持可见；仅覆盖单一桌面视口，未宣称完整响应式矩阵通过。

2026-09-12报告页码一致性修复已部署：Word报告导出优先使用渲染页码映射，后端无映射时回退原始页码；报告契约4 passed，前端lint/build通过，本机LibreOffice渲染新报告2页未见裁切/重叠。腾讯云固定提交 `ad5acb5` 已部署，云端报告内容待合成回归复验。

2026-09-12项目对比报告视觉复核完成：本机LibreOffice逐页检查5页对比报告，关键表格和建议内容可读，无裁切/重叠；第5页留白较多，作为后续非阻塞版式优化项记录。

合成项目工作台只读回归完成：选中 `Auth PDF Project` 后，工作台显示当前修订1、已覆盖评分点0、评分点缺口1、阻塞材料0；目录含已确认/待审核章节，评分点显示来源页码与缺口，响应材料空状态正常。本批未执行接受/拒绝/新增/生成建议操作。

浏览器工作台入口复核完成：重新连接Chrome后进入正式受控入口，智能编标工作台正常加载；未选择项目时“生成目录建议/提取评分点”正确禁用，并显示人工审核边界提示。本批未上传文件、未调用AI、未修改业务数据。

测试文档维护完成：补充Windows pytest临时目录锁定时的独立 `--basetemp` 用法和路径安全边界，避免重复使用被锁目录；未修改运行代码。

任务状态标记修复：将唯一未完成的P6-31C从 `[ ]` 更新为 `[>]`，明确其已完成合成核心回归和本地门禁，剩余正式环境/网络路径验收阻塞；未修改代码或部署。

AI隔离与响应契约复核完成：提示词、异常响应和Dashboard缓存测试共20项通过、1条既有警告；测试使用不可达/空配置隔离，不调用真实DeepSeek。

OCR与扫描页降级复核完成：OCR provider选择、扫描页文字提取降级及文档预览边界共10项通过、1条既有警告；本批未启用百度OCR、未上传真实文件。

项目中心与版本元数据复核完成：工作区 API、配置契约和版本一致性测试共14项通过、1条既有警告；历史项目接口与1.5.2版本约束保持通过。

核心分析契约复核完成：核心 API、风险评分、风险定位和回归模板测试共33项通过、1条既有警告；未调用真实AI，未修改分析逻辑。

生产启动与部署资源复核完成：生产入口、部署资源和运行时健康检查共10项通过、1条既有警告；测试未启动云端服务、未调用真实AI。

完整发布门禁复跑完成：Python 编译、后端177 passed/1 skipped、秘密扫描、Python依赖审计、前端 lint/build、npm生产依赖审计全部通过；构建保留既有大 chunk 非阻塞提示，未执行发布写操作。

鉴权与安全边界复核完成：鉴权、鉴权决策、速率限制、请求日志脱敏共17项通过、1条既有警告；测试未读取或输出真实密钥，未操作云端。

上传安全边界复核完成：上传类型/MIME/大小校验、事务失败清理和存储路径安全共28项通过、1条既有警告；仓库没有独立 `test_doc_parser.py`，实际测试文件已按清单执行。本批未接触真实文件。

备份与路径安全复核完成：备份、恢复、路径约束测试共13项通过、1项因Windows当前不支持符号链接权限而跳过，1条既有警告；测试未接触真实数据。

稳定性边界复核完成：Dashboard缓存、AI错误结构化响应、上传事务与失败清理测试共26项通过、1条既有警告；测试均使用隔离环境，未调用真实AI。

V1.5工作台后端闭环复核完成：目录设计、评分点、映射、响应材料、迁移和闭环测试共17项通过、1条既有警告；未调用真实AI或修改业务代码。

前端边界契约复核完成：预览边界、品牌契约、鉴权契约共13项测试通过、1条既有警告；确认PDF worker、工作台/页面入口和鉴权状态边界未回归。本批未修改运行代码。

报告导出契约复核完成：仓库实际仅有 `tests/test_report_contract.py`，共3项测试通过、1条既有警告；首次尝试的不存在文件名已纠正，没有产生业务变更或测试残留。

DOC/DOCX预览回归复跑完成：`tests/test_word_preview.py` 与 `tests/test_document_preview.py` 共7项通过、1条既有警告；Word主引擎失败回退WPS、缓存PDF、真实预览接口边界均未回归。本批未调用外部OCR/DeepSeek，未修改业务代码。

后端回归门禁复跑完成：首次默认临时目录因Windows权限阻塞，改用独立 `backend/.pytest-auto-20260911-2` 后通过，结果为177 passed、1 skipped、1 warning。跳过项为当前Windows环境不支持的符号链接权限场景，非业务失败。

后端门禁复跑完成：首次使用默认 `backend/.pytest-tmp` 因 Windows 权限占用导致 setup 错误；改用隔离临时目录后，完整 pytest 为 **177 passed, 1 skipped**（1项为当前 Windows 环境能力限制），无业务断言失败。

前端门禁复跑完成：在 `frontend` 目录执行 `npm run lint` 与 `npm run build` 均通过。构建仍提示既有大 chunk 警告（DashboardChart 585.56 kB、card 460.94 kB），本批不继续拆包，避免扩大P6-31C范围。

2026-09-11只读云端核查完成：实例 `ins-goda9pyq`（公网地址已在既有记录中）仅绑定安全组 `sg-hftoto2k`，该安全组仅关联此一台云服务器。入站规则为既有 TCP 3389/80/443 规则，没有 UDP 41641 放行，也未修改规则；另发现已有 IPv6 全来源 TCP 3389 规则，列为安全风险待负责人单独决定。Tailscale直连试验继续保持停止，不扩大安全组范围。

回滚复核完成：用户删除排除并保存后，4份配置SHA-256均与试验前备份一致；目标公网路由恢复Mihomo，tailnet /32仍走Tailscale。Tailscale中继pong 946毫秒，ChatGPT HTTPS 200且证书验证通过。本次单地址排除试验已恢复并结束；加载慢/直连问题仍未解决，不进入新增网络规则操作。

最新覆盖：用户已手动保存单目标TUN排除。两份配置核验仅该排除项变化，目标公网路由转WLAN，tailnet路由保持Tailscale；ChatGPT HTTPS 200且证书验证通过。但Tailscale三次仍走DERP(lax)，worker下载40秒超时，仅246272/1369825字节。试验未解决故障，已请求用户立即删除本次单条排除并保存；因窗口操作工具故障，回滚尚待用户操作及复核，不标记完成。

2026-09-11最新：已获仅云服务器公网IPv4/32的TUN排除授权。Mihomo 1.19.29及现有auto-route/空排除列表已核对，4份相关配置已在办公电脑用户目录单独备份并逐一校验哈希一致。电脑操作窗口捕获/定位失败，恢复时检测到用户输入；本批停在应用前，未写入排除项，未修改安全组、端口、正式代码或数据。授权仍有效，不重复索取；等待客户端可可靠操作后继续单地址试验与失败回退。详见回归记录。

最新结论：受限UDP直连试验已获授权，但来源预检发现Tailscale UDP公网出口与绕过HTTP代理的HTTPS公网出口不同，无法确认指定云服务器UDP 41641的实际来源。已按“来源无法确认则停止”条件停止，未新增规则，无需回滚。端到端问题未修复，不再重复请求同一试验授权。

P6-31C：配置Tailscale受控内网应用入口与共享鉴权。

状态：已获纯合成夹具授权并完成核心浏览器回归，尚未整体关闭P6-31C。正式运行配置目前指向独立合成数据目录，原空数据库及备份保留。Chrome已验证PDF正文/风险跳页/可见高亮、Word预生成预览页映射、Dashboard查看分析穿透、基础项目对比及两类报告实际下载。首次PDF worker传输慢仍待排查；真实Word转换、报告内容/版式及窄屏矩阵尚未在本批验收。详见 `docs/P6_31C_SYNTHETIC_REGRESSION.md`。

本机只读检查已完成：到目标公网地址的系统路由经过Mihomo TUN，到tailnet地址经过Tailscale；没有活动Tailscale出口节点，系统HTTP代理关闭但TUN仍活动。此前--noproxy不等于绕过TUN，目标UDP来源仍未确认。后续若试验仅目标公网IPv4/32的TUN排除，需要新授权，因为原授权禁止改代理；未授权不修改路由/代理，不放行规则，不扩大来源范围。详见回归记录。

## 已完成

冷加载排查进展：办公电脑三次Tailscale探测走DERP(lax)，两次worker下载40秒超时；云端 `inv-r8hd5g0stv` 三次回环完整下载通过，ExitCode 0。排查范围收窄到回环以外路径，端到端问题尚未解决。未修改网络或正式代码。

- 新增可重复的纯合成隔离夹具脚本，创建临时SQLite、PDF、Word占位源文件及预生成Word预览PDF，不调用WPS或DeepSeek。
- 共享模式启动检查通过；根接口和存活探针匿名200，项目列表无凭据/错误凭据401、正确凭据200且响应携带请求ID。
- 授权Dashboard、PDF原件、Word预览接口、Word预览PDF和风险报告均返回200；PDF原件匿名访问返回401。
- 共享模式下文档入口匿名返回401、授权后返回404，确认FastAPI文档已关闭。
- 日志审计发现并修复Uvicorn默认访问日志输出原始文件名：生产入口关闭Uvicorn访问日志，Nginx模板关闭默认访问日志，保留应用安全归一化JSON日志。
- 修复后日志仅显示 `/uploads/<file>`，不包含测试密钥和原始文件名；前后端隔离服务均已正常停止。
- 浏览器确认无凭据时项目与Dashboard返回错误并自动打开凭据弹窗；刷新后凭据清除并再次进入未授权状态。
- 首次浏览器实测发现保存正确凭据后不会自动重试旧请求；已修复为立即重新加载项目列表与Dashboard。
- 修复后项目列表恢复2个隔离项目，Dashboard恢复统计，PDF和Word Blob均渲染1页且显示合成正文。
- 导出报告按钮触发带凭据的 `POST /api/report` 并返回200；浏览器扩展未捕获程序化Blob下载事件，因此以请求证据和既有报告内容测试共同验收。
- 浏览器控制台未出现测试凭据或Axios业务响应；记录既有Ant Design List/Alert弃用提示。
- P3-05备份第一小批代码已完成验证：SQLite一致性快照、上传/报告复制、SHA-256清单、不覆盖和符号链接拒绝。
- P3-05恢复第二小批代码已完成验证：拒绝已有目标、路径穿越、清单外文件、符号链接、哈希篡改、复制期变化及损坏SQLite，并通过同级临时目录原子落位。
- 新增高置信度秘密扫描脚本，只扫描源码、测试、部署模板和文档，不读取本地 `.env`、数据库、上传件或报告；命中时只输出文件、行号和规则，不输出疑似凭据内容。
- Python审计首次发现当前虚拟环境4个包共9条记录；已升级到明确修复版本并复扫为0。前端116个生产依赖审计为0个已知漏洞。
- 建立统一候选版本 `1.0.0-rc.1`，根版本文件、FastAPI元数据、前端包和锁文件由自动测试锁定一致。
- 新增一键发布检查入口，实际串联Python编译、完整pytest、秘密扫描、Python依赖审计、前端lint/build及npm生产依赖审计。
- 新增V1发布清单、回滚门禁和发布说明草案；明确候选版本不等于正式发布，未经用户确认不创建 `v1.0.0` 标签。
- 使用两条纯合成项目完成隔离生产和浏览器冻结回归；根/探针、401/200、文档关闭、PDF/Word预览、Dashboard周期、项目对比与报告接口通过。
- 修复凭据启用后项目中心可能不重试首次401的问题：由凭据状态变化统一重试项目与Dashboard，避免事件时序漏请求。
- AI摘要与AI对比均被非空测试Key加不可达本机Base URL隔离并返回安全502，未连接真实DeepSeek；非AI结果保持可用。
- 确认Git历史只有2个提交和4个跟踪文件；旧入口仅1个路由，现入口装配7组路由且所有启动链路指向 `app.main:app`，旧入口删除应进入候选基线而非恢复。
- 127个未跟踪候选按根目录、后端、前端、部署和文档完成分类；`.env`、数据库、上传、报告、真实样本、依赖和构建目录均经忽略规则核验，不在候选列表。
- 发现5个未引用的Vite模板文件及页面标题/模板favicon遗留；已登记为提交前清理项，没有在审计批次擅自删除。
- 形成单一原子候选基线提交建议和回滚边界，详见 `docs/V1_GIT_BASELINE_AUDIT.md`；本批未暂存、提交、推送或打标签。
- HTML语言改为 `zh-CN`，页面标题、描述、主题色和自有SVG favicon统一为AI标书助手品牌。
- Vite模板README改为项目实际启动/验证说明；删除未引用的 `icons.svg`、`hero.png`、`react.svg`、`vite.svg`，PDF worker保持不变。
- 新增前端品牌契约测试，防止模板标题/资源回归并确认PDF worker继续存在。

## 下一批唯一目标

报告页码补验已完成。下一小批进行正式入口的窄屏响应式抽查，覆盖两侧栏折叠、Word预览和风险检查器；不修改合成夹具或调用真实AI。首次传输慢、真实Word转换及完整报告版式仍为已知限制。

## 已知限制

- 纯扫描样本1项风险无可靠OCR匹配时不画框，本样本未产生低风险项。
- 表格样本不覆盖多页密集、嵌套或大量跨页表格。
- 当前Word验证使用WPS，不代表Microsoft Word分页完全一致。
- 上传并发和解析超时仍需后续部署安全批次保护。
- Git工作区存在旧入口删除及大量未跟踪成果，发布前必须建立正式版本基线。

## 本批验证

- 腾讯云 COS 已开通，临时桶位于 `ap-chongqing`，访问权限为私有读写、单 AZ，当前文件数为 1；唯一对象为已验证 Git 官方安装包，未开启 CDN、自定义域名、版本控制、日志存储或其他付费附加功能。
- 用户开启 Chrome 本地文件访问权限后，文件选择器成功上传安装包；COS 页面显示“任务已完成（已成功 1 个）”、共 1 个文件，大小 62.32 MB，访问权限继承桶的私有读写策略。
- 项目负责人已明确确认执行 COS 中转的 Git 安装；自动化助手执行 ID `inv-k8e34g0636` 状态为命令成功，开始时间 22:20:17、结束时间 22:20:50、耗时 33 秒、ExitCode 0。
- 输出依次包含 `DOWNLOAD_GIT_FROM_COS`、`GIT_SHA256_OK`、`GIT_SIGNATURE_OK`、`git version 2.55.0.windows.5` 和 `GIT_INSTALL_OK`，证明下载、哈希、签名、安装和版本检查完整通过。
- 安装参数包含 `/NORESTART`，本批未主动重启。
- 项目负责人另行确认删除临时 COS 对象和空桶；对象删除后文件列表显示“共 0 个文件 / 暂无数据”，空桶删除并刷新后存储桶列表显示“共 0 项”，原桶名称不再出现。
- 直接打开已删除 `.exe` 对象地址被浏览器客户端下载策略拦截，未作为 404 证据；清理完成以腾讯云 COS 控制台的资源列表为权威证据。
- 云端只读预检首次执行 `inv-m8e3g9ggm6` 因 GitHub 连接重置以 ExitCode 1 结束；失败前确认 Git 2.55.0 可用、应用目录和数据目录均不存在、C 盘可用 76.68 GB，且未创建任何目录。
- 修复命令仅增加 OpenSSL、HTTP/1.1 与 4 次有限重试；复跑 `inv-k8e3mtg1f9` 用时 3 秒、ExitCode 0，第一轮远程读取即成功并输出 `DEPLOY_PREFLIGHT_OK`。
- 远程 `v1.5.1` annotated 标签对象为 `cf3aa454939812714bb377391a960459f90269b3`，解引用提交为 `b542d010d2679c0265fcd4cd50cd420fcd39954e`，与正式发布记录一致；远程 `main` 已前进到 `5cca7ad6...`，后续部署只固定标签，不跟随 `main`。
- 固定版本检出执行 `inv-k8e3t3gkdd` 状态为命令成功，开始时间 22:44:23、结束时间 22:44:47、耗时 24 秒、ExitCode 0。
- 输出确认 `CHECKOUT_HEAD b542d010...`、`CHECKOUT_TAG v1.5.1`、`CHECKOUT_VERSION 1.5.1`、`CHECKOUT_WORKTREE_CLEAN`、`DATA_ROOT_NOT_CREATED` 与 `CHECKOUT_V151_OK`；未创建生产数据目录或启动进程。
- Git 的 detached HEAD 提示来自按 annotated 发布标签检出，属于不可漂移的预期部署状态；后续更新必须显式选择版本标签，不在服务器上直接提交。
- 云端依赖命令固定应用 HEAD 为 `b542d010...`，并以 Git 工作树及两份依赖清单相对 HEAD 无差异作为 Windows 兼容的防篡改门禁；原始字节 SHA-256 因 Git CRLF 换行转换已停用。
- 依赖安装首次执行 `inv-k8e4580ep2` 在下载前因 `requirements.txt hash mismatch` 以 ExitCode 1 安全退出；修正后执行 `inv-m8e4ahgquk` 完成后端依赖安装和秘密扫描，云端 pytest 为 177 passed、1 failed、2 warnings。
- 云端唯一失败为 `test_backup_refuses_overwrite_and_symlinks`：符号链接拒绝生效，但目标目录仍存在。本地已将两个文件源的校验前置到目标创建前；聚焦回归 1 passed、1 skipped，完整回归 177 passed、1 skipped、1 warning。
- 后端清单目前是带上下界的兼容版本区间而非完全哈希锁定；前端由 `npm ci` 严格使用锁文件。命令不会创建 `.env` 或 `C:\AI-Bid-Data`，不会启动应用或调用 DeepSeek/OCR。
- 本机官方安装包 `Git-2.55.0.5-64-bit.exe` 大小65,343,712字节，SHA-256为 `D065A4E23C3D9A6B5073D609B5BE0830227EC3CA053C083BA385061DDFAF94C6`；Authenticode状态为Valid，签名证书指纹为 `2A1E97CBF0DFCDA15B0DA0AC9745014F989D4AD0`。
- 腾讯云 Git 单组件命令首次执行 `inv-k8de5mgbc2` 因 `CRYPT_E_REVOCATION_OFFLINE` 退出；加入 `--ssl-no-revoke` 后 `inv-k8de8v0pr2` 因连接重置退出；加入HTTP/1.1和全错误重试后 `inv-k8debe0tii` 已进入官方CDN但约15–20 KB/s，最终按1200秒命令上限标记为超时。三次均未到达哈希、签名或安装阶段。
- 超时后只读预检于2026-09-08 10:53:35以ExitCode 0结束：Git为 `NOT_INSTALLED`，WPS已知路径不存在；随后路径诊断再次确认 `C:\Windows\py.exe` 可定位Python 3.14.6，`C:\Program Files\nodejs\node.exe` 为v24.18.0，`npm.cmd` 为11.16.0。
- 本批未安装 Git/WPS、未重启服务器、未上传应用或用户数据、未创建生产密钥，也未调用 DeepSeek/OCR；仅按授权创建临时 COS 桶并上传已验证的 Git 官方安装包。
- 腾讯云自动化助手首次运行环境命令 `inv-m8cwbw04rf` 在下载 Git for Windows 官方 GitHub 发布包时30分钟超时，安装阶段未开始。
- 分步命令 `inv-m8dd3a009e` 成功下载 Python/Node 官方安装包，两个 Authenticode 签名有效且 Python SHA-256 与官方值一致；Python和Node安装器均返回成功，脚本因预期 Python 路径不存在而以 ExitCode 1结束。
- 安装后只读预检 `inv-m8dd63gqra` 与路径诊断 `inv-m8dd7tggs7` 均 ExitCode 0：`py` 注册 Python 3.14.6，实际解释器为 `C:\Program\python.exe`；Node为 `v24.18.0`，npm为 `11.16.0`。
- 本批未安装Git/WPS、未重启服务器、未创建生产密钥、未调用DeepSeek/OCR，也未上传数据库、标书或报告。

- P6-16从 `backend` 使用既有 `.venv` 运行部署模板、生产启动、运行检查、备份和恢复聚焦回归：17 passed、1 skipped；跳过项仍为当前Windows无符号链接权限，保留1个既有Starlette/httpx弃用提示。
- 前端 `npm run build` 通过，正式版本为 `1.5.1`；Dashboard图表块仍为585.56 kB，保留已审计提醒。
- 秘密扫描和差异格式检查通过；测试只使用隔离临时目录并已清理，未读取真实 `.env`、用户数据库、上传文件或报告。
- 本批没有连接真实服务器、注册Windows/Linux服务、修改Nginx、调用DeepSeek/OCR或执行部署。

- P6-15推送前确认远程 `main=82f3383` 是本地 `main=f30d618` 的历史祖先，远程不存在 `v1.5.1`，满足安全快进和无标签冲突条件。
- Windows Schannel首次在读取GitHub回包时出现 `SEC_E_MESSAGE_ALTERED`；改为单次命令使用OpenSSL后推送成功，没有永久修改Git SSL配置。
- GitHub远程复核：`main=f30d61833cb719b4658ceb28497f31cb0ae0afe6`；`v1.5.1` 标签对象为 `cf3aa454939812714bb377391a960459f90269b3`，解引用到 `b542d010d2679c0265fcd4cd50cd420fcd39954e`，与本地一致。
- 推送范围仅为 `main` 和 `v1.5.1`；未部署，真实配置、数据库、上传文件、报告和回归原件均未进入Git。

- P6-14仅暂存 `CHANGELOG.md`、`TASKS.md`、`docs/ACTIVE_TASK.md`、`docs/PROJECT_STATE.md` 和 `docs/V1_5_1_RELEASE_NOTES.md` 5份标签状态文档。
- 提交前标签 `v1.5.1` 仍解引用到 `b542d01`，版本保持 `1.5.1`；差异格式和秘密扫描通过。
- 本批只创建一个本地文档提交；未移动标签、推送或部署。

- P6-13创建本地annotated标签对象 `cf3aa454...`，标签 `v1.5.1` 解引用后精确指向正式发布提交 `b542d010d2679c0265fcd4cd50cd420fcd39954e`。
- 标签说明为 `AI Bid Assistant v1.5.1`；创建前确认同名标签不存在，创建后HEAD保持不变。
- 本批未推送或部署，未调用外部模型，也未访问或修改用户数据。

- P6-12将根版本、FastAPI版本契约和前端包/锁文件由 `1.5.1-rc.1` 统一提升为正式 `1.5.1`。
- 从仓库根目录复跑统一正式发布门禁：177 passed、1 skipped，秘密扫描、Python依赖审计、前端lint/build通过；最后的npm审计首次因受限网络TLS连接中断，按相同 `npm audit --omit=dev` 在允许联网环境重跑后为0个已知漏洞，正式门禁全部组成项通过。
- 新增 `docs/V1_5_1_RELEASE_NOTES.md`，记录维护范围、保持能力、已知限制和未授权动作边界。
- 本批只创建本地正式发布提交；未创建或移动标签，未推送、部署或调用真实外部模型。

- P6-11仅暂存6份签署与状态文档，候选代码提交 `814c4bf` 保持为父提交；提交前差异格式与秘密扫描通过。
- 本批按授权创建一个独立本地文档提交；未修改 `VERSION` 或前端包版本，未创建标签、推送或部署。

- 项目负责人于2026-09-05明确确认签署 `1.5.1-rc.1` 候选版本验收，最终候选提交为 `814c4bf`。
- 签署范围明确排除标签、推送、正式版本提升和部署；本批未执行这些动作，也未调用DeepSeek或访问用户数据。
- 验收签署单、候选审计、项目状态、任务清单和变更日志已同步；签署记录尚未提交，等待另行授权。

- P6-10签署前确认HEAD为 `814c4bf8be0198d0e48b1d93ee3bb035bfb93700`，父提交为正式 `1.5.0` 提交 `a4295e60...`；候选提交没有标签，`v1.5.0` 仍指向父提交。
- `git show` 和 `git diff-tree` 确认候选仅含22个审计文件，1562行新增、789行删除；提交差异格式检查通过。
- 从 `backend` 使用既有 `.venv` 复跑版本、品牌/懒加载和文档预览边界聚焦回归：12 passed、1个既有Starlette/httpx弃用警告。
- 首次从仓库根目录直接运行聚焦测试因模块搜索路径不符合项目入口而出现12个导入错误；已按 `TESTING.md` 改从 `backend` 运行并全部通过，候选代码无需修改。
- 新增 `docs/V1_5_1_ACCEPTANCE_SIGNOFF.md`，明确候选范围、门禁证据、已知限制、数据/回滚边界和待负责人确认文本。

- P6-09将根版本、FastAPI版本契约和前端包/锁文件统一为 `1.5.1-rc.1`，并按授权复跑统一候选门禁。
- 候选提交仅包含P6维护源码、测试、审计文档和版本同步文件；真实配置、数据库、上传、报告、真实样本、依赖和构建产物继续排除。
- 本批只创建一个本地候选提交；没有创建或移动标签，没有推送或部署。最终提交哈希以Git历史及本次交付报告为准。

- P6-08从仓库根目录运行统一发布门禁：Python编译、完整pytest、秘密扫描、Python依赖审计、前端lint/build及npm生产依赖审计全部通过。
- 完整后端回归177 passed、1 skipped、1个既有Starlette/httpx弃用警告；跳过项仍为当前Windows无符号链接权限。
- Python与前端生产依赖均为0个已知漏洞；秘密扫描通过；前端构建保留585.56 kB图表块提醒，没有提高警告阈值。
- 暂存区为空；审计时11个已跟踪修改、6个未跟踪源码/文档，加上新审计文档后形成18个未来候选文件。
- Git忽略边界确认真实 `.env`、`bid.db`、上传、报告、真实样本、`node_modules` 和 `dist` 被排除；跟踪范围只存在安全模板 `.env.example`。
- `v1.5.0` 为annotated tag：标签对象 `8095a315...` 解引用后仍指向HEAD `a4295e60...`；根/后端/前端版本契约保持 `1.5.0`。
- 原子提交与 `git revert` 回滚建议已记录在 `docs/P6_MAINTENANCE_CANDIDATE_AUDIT.md`；本批未暂存、提交、推送、打标签或部署。

- P6-07将 `DashboardChart` 从 `echarts-for-react` 完整入口改为其ESM core，并通过 `echarts/core` 注册Bar、Line、Pie、Grid、Legend、Tooltip、LabelLayout和CanvasRenderer；四处option及Dashboard状态仍留在 `App`。
- 首次使用CommonJS `lib/core` 时，生产构建通过但Vite开发页出现“Element type is invalid”空白页；浏览器回归捕获后立即改为包内ESM core并重新验证，没有带错进入后续任务。
- 最终Dashboard图表块由1136.66 kB（gzip377.43 kB）降至585.56 kB（gzip198.59 kB），分别减少551.10 kB（48.5%）和178.84 kB（47.4%）。
- 默认Dashboard正常首次JS由约2062.78 kB（gzip673.61 kB）降至约1511.68 kB（gzip494.77 kB）；入口、工作台和文档预览块保持原体积。
- 隔离浏览器确认4个ECharts实例/Canvas正常，1280px页面无横向溢出；切换最近30天后周期比较和两张趋势文案同步更新，4个图表实例仍存在。
- 重点关注项目可穿透到两页PDF分析页并显示1项风险；页面切换后Dashboard图表正常卸载，浏览器控制台0错误0警告。
- 最终前端lint与manifest构建通过；品牌/拆包和核心接口聚焦回归13 passed、1个既有Starlette/httpx弃用警告。隔离AI使用非真实Key与不可达本机地址，未连接真实DeepSeek。

- P6-06使用 `vite build --manifest` 核对真实构建图：入口明确动态引用 `WorkbenchPage`、`DashboardChart` 和 `DocumentPreview`，没有退化为静态导入。
- 首屏静态JS约926.12 kB（gzip296.18 kB）；默认Dashboard立即增加1136.66 kB（gzip377.43 kB）的图表块，正常首次进入合计约2062.78 kB（gzip673.61 kB，不含CSS）。
- 智能编标工作台仅增加9.32 kB（gzip3.23 kB），共享Ant Design块已在入口加载；文档预览只在打开PDF/Word后增加约347.78 kB（gzip103.99 kB，含极小浏览器兼容动态块）。
- 依赖源码确认 `echarts-for-react` 默认入口执行 `import * as echarts from 'echarts'`，当前图表实际只使用pie、line、bar以及tooltip、legend、grid、xAxis、yAxis，完整ECharts入口是剩余大块的明确来源。
- 审计结论：继续拆工作台或预览收益低且增加回归风险；把Dashboard再包一层也不会减少默认首屏下载。下一批只评估ECharts模块化引入，不提高警告阈值、不改Dashboard功能。
- 前端生产manifest构建通过；聚焦静态契约与前端lint在本批收尾门禁中复验。

- P6-05第三小批使用独立SQLite、上传和报告目录，以及两页合成PDF和两页预生成Word预览PDF；未访问用户数据库/上传文件，未调用WPS、百度OCR或DeepSeek。
- PDF异步预览显示2页，上一页/下一页边界正常；风险点击定位第2页，文字层出现高风险高亮，合成OCR框按10%/8%/48%/5%归一化坐标覆盖。
- Word版式预览显示2页及本机办公软件来源；风险原始页为第1页，`risk_page_map`正确跳转渲染第2页并出现中风险文字高亮。
- 首次浏览器测量发现懒加载竞态导致495px容器内画布仍按700px渲染；将 `ResizeObserver` 与宽度状态移入实际挂载的 `DocumentPreview` 后，复测画布宽度等于495px且页面无横向溢出。
- 快速切换文档时 `react-pdf` 记录一条 `AbortException: TextLayer task cancelled`；依赖源码确认这是卸载进行中文字层任务的取消提示，最终Word页码与高亮正常。未修改第三方依赖、未关闭文字层，作为已知非功能性告警保留。
- 完整后端回归177 passed、1 skipped、1个既有Starlette/httpx弃用警告；跳过项仍为当前Windows无符号链接权限。前端lint/build通过。
- 最新构建主入口345.92 kB（gzip107.59 kB），文档预览347.68 kB（gzip103.88 kB），Card共享块460.94 kB（gzip150.10 kB），Dashboard/ECharts块1136.66 kB（gzip377.43 kB）；保留大chunk提醒供P6-06审计。

- P6-05第二小批新增懒加载 `DocumentPreview`，入口仅在存在URL、兼容页或错误时请求预览组件；`react-pdf`和worker初始化已移入组件。
- Blob获取与对象URL回收、Word rendered/text分支、`risk_page_map`、页码/宽度状态、风险点击和文字高亮调度继续留在 `App`，没有扩大拆分范围。
- 前端lint/build通过；主入口由1269.76 kB（gzip393.76 kB）降至346.23 kB（gzip107.71 kB），预览chunk为347.44 kB（gzip103.79 kB），Card共享chunk为460.94 kB（gzip150.10 kB）。
- 预览边界、Word渲染页映射和风险定位聚焦回归16 passed、1个既有Starlette/httpx弃用警告。
- 本批未启动业务服务或浏览器，未调用WPS、百度OCR、DeepSeek，也未访问用户数据库和上传文件。

- P6-05第一小批新增 `test_frontend_preview_boundary.py`，6项契约锁定PDF worker/Blob生命周期、700px响应式上限、文字与注释层、翻页边界、Word rendered/text分支、风险映射和OCR叠框。
- 前端边界、后端Word预览及风险定位聚焦回归16 passed、1个既有Starlette/httpx弃用警告。
- 前端lint/build通过；构建产物保持主入口1269.76 kB、Dashboard/ECharts异步块1136.65 kB、工作台块9.25 kB，仍有大chunk提醒。
- 本批只新增测试和审计文档，未启动浏览器或业务服务，未调用WPS、百度OCR、DeepSeek，也未访问用户数据库和上传文件。

- P6-04第二小批在独立SQLite、上传和报告目录运行：Dashboard创建4个ECharts实例，累计2个项目/1项高风险，7天切换30天后周期文案和统计同步更新。
- 重点关注列表的 `Auth PDF Project` 可穿透到标书分析页，PDF显示第1/1页，风险检查器展开并显示1项合成高风险、20分扣分及第1页定位；浏览器控制台0错误0警告。
- 首轮隔离页面暴露Dashboard只统计最后一条项目：项目中心为1项风险而Dashboard为0。已修复聚合块缩进并新增双项目回归，修复后接口和页面均为1项。
- Python编译通过；完整后端回归171 passed、1 skipped、1个既有Starlette/httpx弃用警告，跳过项仍为当前Windows无符号链接权限。
- 前端lint/build通过；主入口1269.76 kB、Dashboard/ECharts异步块1136.65 kB、工作台块9.25 kB，保留大chunk提醒。
- AI使用非空测试Key并固定到不可达 `127.0.0.1:9`，未连接真实DeepSeek；测试页面及8015/5185服务已关闭，隔离数据待最终安全清理。

- P6-04第一小批前端lint/build通过；主入口由约2412.47 kB（gzip 773.02 kB）降至1269.76 kB（gzip 393.76 kB），ECharts生成1136.65 kB（gzip 377.43 kB）异步chunk，工作台chunk保持9.25 kB。
- Dashboard图表懒加载契约聚焦回归5 passed、1个既有Starlette/httpx弃用警告；锁定入口不再静态导入ECharts、4处option仍由原Dashboard状态生成。
- 本批没有启动业务服务、调用DeepSeek、访问用户数据库或移动 `v1.5.0` 标签；Dashboard交互浏览器回归留待P6-04第二小批。

- P6-03第二小批使用全新合成环境完成浏览器回归：工作台异步入口及未选项目空状态正常；PDF项目显示修订、目录、评分点和操作入口；Word项目形成隔离空工作台。
- AI目录确认提示展示“发送当前文件并消耗额度”，测试点击取消；新增章节、评分点映射和新增材料弹窗均正常打开并取消，没有执行真实业务写入。
- 浏览器控制台0错误0警告；Dashboard初始化AI使用非真实测试Key并固定到不可达 `127.0.0.1:9`，安全返回502，未连接真实DeepSeek。
- 临时浏览器页面、后端8012和前端5182服务均已关闭；合成数据目录清理后复核端口与工作区。

- P6-03第一小批前端lint/build通过；生成独立 `WorkbenchPage` chunk 9.25 kB（gzip 3.20 kB），主包由2421.34 kB降至2412.47 kB（gzip 773.02 kB）。
- 工作台懒加载与props契约聚焦回归4 passed、1个既有Starlette/httpx弃用警告；首次lint发现搬迁后残留的 `ReloadOutlined` 导入，移除后复跑通过。
- 浏览器交互回归留待P6-03第二小批，未以构建通过代替最终页面验收。

- P6-02只读审计确认 `App.jsx` 9733行、约232 kB，生产主包约2421.34 kB、gzip约774.99 kB；入口静态依赖Ant Design、ECharts和react-pdf。
- 拆包顺序与禁止项已写入 `docs/FRONTEND_BUNDLE_AUDIT.md`；本批不改Vite配置或运行代码，使用现有P6-01 lint/build结果作为构建基线。

- P6-01前端 `npm run lint` 和 `npm run build` 通过；项目中心弃用列表已不再进入生产包，构建主包由约2.46 MB降至约2.42 MB，仍保留大包提醒。
- 新增项目中心列表契约测试，锁定不再导入/渲染Ant Design `List`，并保留列表、列表项及空状态语义。

- 正式 `1.5.0` 完整发布门禁通过：Python编译通过；后端167 passed、1 skipped、1个既有弃用警告；秘密扫描通过；Python依赖审计0个已知漏洞；前端lint/build通过；npm生产依赖审计0个已知漏洞。
- 前端构建保留既有约2.46 MB主包拆分提醒；跳过项仍为当前Windows无符号链接权限，均不阻塞正式版本。
- 版本一致性契约确认根版本、FastAPI及前端包/锁文件均为 `1.5.0`；本地正式发布提交和 `v1.5.0` 标签按本批Git结果建立，不推送、不部署。

- 签署记录提交前仅暂存6个指定文档，秘密扫描与暂存差异格式检查通过；候选提交 `33ad2bc` 保持在父提交历史中，未创建标签、推送、提升正式版本或部署。

- 签署内容与候选身份复核：用户确认版本 `1.5.0-rc.1`、最终候选 `33ad2bc` 及“不创建标签、不推送、不提升正式版本”边界；Git HEAD仍为完整哈希 `33ad2bcee8752475caa53feecb2a336c6777cef7`，HEAD无标签。
- 验收记录、项目状态、活动任务、任务清单和变更日志已同步；秘密扫描与差异格式检查通过。

- 候选身份复核：`HEAD` 为 `33ad2bcee8752475caa53feecb2a336c6777cef7`，HEAD无标签，既有 `v1.0.0` 标签仍指向 `c0bfd6304a5c8f92d2708c0042d32744e3dce9dc`；35个候选文件未命中配置、数据库、上传、报告、真实样本、依赖或构建产物边界。
- 版本与工作台边界聚焦回归：4 passed、1个既有Starlette/httpx弃用警告。首次从仓库根目录执行因 `app` 导入路径错误在夹具阶段失败，改为按项目约定从 `backend` 使用现有 `.venv` 后全部通过。
- 秘密扫描与差异格式检查通过；本批未调用DeepSeek，未触碰用户数据库、上传文件及报告。

- 统一候选门禁：后端167 passed、1 skipped、1个既有弃用警告；Python依赖审计0个已知漏洞；前端lint/build通过。
- npm在线安全端点连续两次连接重置；使用本机最新缓存执行 `npm audit --offline --omit=dev --audit-level=low`，结果0个已知漏洞。
- 版本一致性契约通过；秘密扫描通过。候选暂存前再次核对忽略边界和差异格式。

- Python编译通过；完整后端回归167 passed、1 skipped、1个既有弃用警告，跳过项为当前Windows无符号链接权限。
- V1.5迁移聚焦回归5 passed、1个既有弃用警告；覆盖旧数据保留、两版迁移幂等和残缺同名表拒绝。
- 前端 `npm run lint` 和 `npm run build` 通过；保留既有约2.46 MB主包提醒。
- 候选忽略边界确认 `.env`、数据库、上传、报告、真实样本、依赖和构建目录不进入Git；本批未调用DeepSeek或触碰用户数据。

- 纯合成浏览器闭环通过：修订1→6、目录/评分点审核、人工映射、材料新增/阻塞编辑、项目隔离和空状态均符合预期；1024px文档宽度无溢出。
- AI确认提示通过且点击取消；Dashboard初始化AI仅命中不可达 `127.0.0.1:9` 测试地址并安全502，未连接真实DeepSeek。
- 浏览器控制台仅有既有Ant Design `List`弃用提醒；前后端停止后8000/5173端口均关闭。
- 前端 `npm run lint` 和 `npm run build` 通过；工作台最小闭环与材料聚焦回归3 passed，1个既有弃用警告。

- 前端 `npm run lint`：通过，0错误、0警告；`npm run build`：通过，保留既有约2.46 MB主包提醒。
- 响应材料后端聚焦回归：2 passed，1个既有弃用警告；覆盖人工新增、已确认目标约束、四态汇总、修订快照和过期写入保护。
- 本批未启动业务服务或调用DeepSeek，未触碰用户数据库、上传文件及报告。

- 前端 `npm run lint`：通过，0错误、0警告；`npm run build`：通过，保留既有约2.46 MB主包提醒。
- AI目录建议与评分点后端聚焦回归：13 passed，1个既有弃用警告；首次固定pytest临时目录因Windows权限未进入用例，改用唯一 `--basetemp` 后全部通过。
- 本批没有启动业务服务、点击确认入口或调用真实DeepSeek；未触碰用户数据库、上传文件及报告。

- 前端 `npm run lint`：通过，0错误、0警告；`npm run build`：通过，保留既有约2.45 MB主包提醒。
- 映射及最小闭环后端聚焦回归：3 passed，1个既有弃用警告；覆盖同工作台确认对象、重复关系更新、覆盖统计、修订快照和旧修订冲突。

- 前端 `npm run lint`：通过，0错误、0警告；`npm run build`：通过，保留既有约2.45 MB主包提醒。
- 工作台章节接口聚焦回归：9 passed，1个既有弃用警告；覆盖人工新增、标题清理、排序、跨工作台父级和修订冲突。

- 前端 `npm run lint`：通过，0错误、0警告；`npm run build`：通过，保留既有约2.45 MB主包提醒。
- 响应材料接口聚焦回归：2 passed，1个既有弃用警告；覆盖责任人清空、说明和状态更新、修订快照及冲突保护。

- 前端 `npm run lint`：通过，0错误、0警告；`npm run build`：通过，保留既有约2.45 MB主包提醒。
- 目录/评分点审核后端聚焦回归：13 passed，1个既有弃用警告。
- 临时Vite浏览器只读验收：智能编标导航、工作台标题、未选项目空状态和风险检查器隐藏通过；后端刻意未启动，未执行真实审核写入或DeepSeek调用，临时5173服务已停止。

- 前端 `npm run lint`：通过，0错误、0警告。
- 前端 `npm run build`：通过；保留既有约2.45 MB主包拆分提醒。

- 工作台最小闭环聚焦回归：1 passed；覆盖目录、来源评分点、审核、映射、材料、四态汇总、连续7个修订、最终快照和旧修订冲突。
- Python编译与秘密扫描通过；完整后端回归：167 passed、1 skipped、1个既有弃用警告。跳过项仍为当前Windows无符号链接权限。

- 响应材料API聚焦回归：2 passed；覆盖新增、列表、人工更新、四态汇总、修订快照、跨工作台/未确认目标拒绝、空更新和并发冲突。
- Python编译检查通过；完整后端回归：166 passed、1 skipped、1个既有弃用警告。跳过项仍为当前Windows无符号链接权限。

- 工作台迁移与机器契约聚焦回归：8 passed；覆盖基础库升级、第二版本幂等、旧工作台数据保留、响应材料字段和状态契约，以及残缺材料表拒绝且不登记成功。
- Python编译检查通过；完整后端回归：164 passed、1 skipped、1个既有弃用警告。跳过项仍为当前Windows无符号链接权限。

- 工作台机器可读契约与设计边界测试：3 passed；JSON结构校验、秘密扫描和新增文件行尾检查通过。
- 完整后端回归：141 passed、1 skipped、1个既有弃用警告；本批未修改前端运行代码或数据库。

- 隔离共享模式HTTP回归：根/存活探针、401、错误凭据、项目列表、Dashboard、PDF、Word预览、报告和文档关闭契约通过。
- `npm run lint`：通过，0错误、0警告；`npm run build`：通过，保留既有约2.44 MB主包警告。
- Python编译检查通过；完整后端回归：125 passed，1个已知弃用警告。
- 修复后运行日志确认只有安全归一化路径，不再出现原始上传文件名。
- Edge扩展浏览器回归通过：401弹窗、正确凭据自动恢复项目/Dashboard、PDF/Word页面、报告请求、刷新清除凭据及控制台秘密检查完成。
- `npm run lint`与`npm run build`通过；完整后端回归：126 passed、1 skipped、1个已知弃用警告。
- Windows当前权限不允许创建符号链接，备份符号链接拒绝用例跳过；实现仍包含显式拒绝逻辑。
- 安全恢复聚焦测试：7 passed、1 skipped、1个已知弃用警告；全部使用合成SQLite、上传件和报告。
- Python编译与差异格式检查通过；完整后端回归：132 passed、1 skipped、1个已知弃用警告。
- 秘密扫描通过；秘密扫描聚焦测试2 passed。Python当前环境与前端生产依赖复扫均为0个已知漏洞。
- Python编译和完整后端回归通过：134 passed、1 skipped、1个既有弃用警告；前端lint/build通过，保留既有约2.44 MB主包警告。
- `scripts/verify_release.ps1`端到端通过：135 passed、1 skipped、Python/npm均0个已知漏洞、秘密扫描及前端lint/build通过；保留既有弃用和约2.44 MB主包提醒。
- 隔离生产HTTP与浏览器冻结回归通过，详细证据见 `docs/V1_FREEZE_REGRESSION.md`；两类AI请求只命中不可达测试地址，服务结束后隔离端口已关闭。
- 凭据重试修复后重新执行统一发布入口：135 passed、1 skipped，秘密扫描、Python/npm依赖审计、前端lint/build全部通过。
- Git只读审计：2个提交、4个历史跟踪文件、127个未跟踪候选、1个超过1 MiB的必要PDF worker；历史高置信度秘密扫描0命中，用户数据和本机配置忽略规则通过。
- 品牌聚焦测试2 passed；前端lint/build与秘密扫描通过，保留既有约2.44 MB主包提醒。
- 第四小批完成后统一发布入口再次通过：137 passed、1 skipped，Python/npm均无已知漏洞，秘密扫描和前端lint/build通过；保留既有弃用和约2.44 MB主包提醒。
- 候选提交前复验发现固定pytest临时目录受Windows残留权限影响，固定pip-audit缓存也产生权限降级警告；发布门禁已改用每次唯一测试目录和审计缓存并增加契约测试，避免旧缓存阻塞或干扰后续验证。
- 最终发布入口通过：138 passed、1 skipped，秘密扫描、Python/npm依赖审计和前端lint/build全部通过；候选暂存区不包含真实配置、数据库、上传件、报告、授权样本或构建依赖。
- 签署前从仓库根目录执行生产预检时发现相对默认路径随当前目录漂移；生产入口已固定切换到 `backend/`，避免误生成根目录数据库和存储目录，并新增启动契约断言。
- 正式 `1.0.0` 完整发布门禁通过；发布脚本成功提示已改为同时适用于候选版和正式版的中性文案。
- 本批未调用百度 OCR 或 WPS，未触碰用户数据库和上传文件。隔离启动时空的 `DEEPSEEK_API_KEY` 未覆盖本机 `.env`，意外产生1次真实Dashboard AI调用；未输出密钥，后续隔离启动必须同时使用非空测试Key和不可达测试Base URL。
