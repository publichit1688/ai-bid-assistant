# V1.5 智能编标工作台候选审计

日期：2026-09-04

状态：自动门禁与只读边界审计通过；最终候选提交 `33ad2bc` 已由项目负责人签署，不包含正式版本提升、标签、推送或部署。

## 交付范围

- 后端：版本化SQLite增量迁移、工作台模型和API、带来源的目录建议与评分点提取、人工审核、章节映射、响应材料及修订快照。
- 前端：独立智能编标入口、三栏总览、显式AI确认、建议审核、人工目录、人工映射、材料新增/编辑和覆盖/阻塞汇总。
- 质量：设计与机器契约、迁移/API/闭环自动测试、纯合成浏览器回归和测试夹具。
- 明确排除：一键生成整本标书、自动生成正文、无来源补写、自动分派责任人、自动投标。

## 自动门禁

- Python编译：通过。
- 完整后端回归：167 passed、1 skipped、1 warning。跳过项为当前Windows无符号链接权限；警告为既有Starlette/httpx弃用提醒。
- V1.5迁移聚焦回归：5 passed、1个既有弃用警告。
- 前端ESLint：通过，0错误、0警告。
- 前端生产构建：通过；保留约2.46 MB主包拆分提醒。
- Python当前环境依赖审计：0个已知漏洞；前端生产依赖使用本机最新缓存离线复核为0个已知漏洞（官方npm审计端点当批两次连接重置）。
- 纯合成浏览器回归：通过，见 `docs/V1_5_WORKBENCH_REGRESSION.md`。
- 秘密扫描与差异格式检查：通过。

## 迁移复核

- `20260903_01_workbench_foundation` 新增工作台、来源、目录、评分点、映射和修订表。
- `20260903_02_response_materials` 新增响应材料表。
- 迁移只做增量创建，不删除或覆盖V1表及数据；重复运行幂等。
- 同名表字段残缺时拒绝登记迁移成功；当前实现仅支持SQLite，其他数据库会明确中止。
- 启动时先执行迁移，再装配工作台路由。

## Git与数据边界

- 最终候选提交为 `33ad2bcee8752475caa53feecb2a336c6777cef7`（短哈希 `33ad2bc`）。
- 当前本地 `v1.0.0` 标签仍指向 `c0bfd6304a5c8f92d2708c0042d32744e3dce9dc`。
- 当前产品版本已统一提升为 `1.5.0-rc.1`。
- 候选提交包括35个代码、测试、契约和文档文件；测试临时目录不属于候选。
- `.env`、用户SQLite、上传文件、报告、真实回归样本、`node_modules`和`dist`继续由Git忽略。

## 已知限制

- 前端主包约2.46 MB，后续可按页面拆包；当前不阻塞工作台闭环。
- Ant Design `List` 存在下一主版本弃用提醒；当前功能正常。
- 工作台目前只支持人工新增顶级章节，不包含复杂树形拖拽编辑。
- 工作台AI结果必须逐项人工审核；来源无法验证的结果会被过滤。
- 当前迁移仅支持SQLite。

## 候选结论与授权边界

V1.5最小闭环已建立 `1.5.0-rc.1` 本地候选基线，单一原子提交包含代码、测试、契约和文档。

项目负责人已确认签署 `1.5.0-rc.1` 最终候选提交 `33ad2bc`；签署明确不创建标签、不推送、不提升正式版本。签署记录是否提交以及后续正式版本动作均需另行授权。
