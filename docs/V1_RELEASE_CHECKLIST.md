# V1 发布检查清单

正式版本：`1.0.0`
状态：候选验收已签署，用户已授权创建本地 `v1.0.0` 标签；尚未推送或部署到真实服务器。

## 自动检查

在仓库根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\verify_release.ps1
```

该入口依次执行Python编译、完整后端pytest、秘密扫描、Python依赖审计、前端lint/build和npm生产依赖审计。依赖审计需要联网，任何一步失败都必须停止发布。

## 人工发布门禁

- [x] 确认 `VERSION`、FastAPI元数据、`frontend/package.json` 和锁文件版本一致。
- [x] 确认工作区中的旧入口删除和未跟踪成果已逐项审计，并建立可回溯的Git候选基线 `25e4e96`。
- [x] 已完成旧入口、127个候选文件、历史秘密与用户数据忽略边界审计；真实配置和用户数据未进入候选提交。
- [x] 前端模板标题、favicon、README和无引用资源已完成品牌清理，并通过契约测试与生产构建。
- [x] 使用隔离配置完成生产启动预检、存活/就绪探针、鉴权401/200和服务停止验证。
- [x] 使用合成项目回归Dashboard周期、项目中心、风险检查器、PDF响应式、Word预览、项目对比和两类报告导出；真实风险跳页/高亮沿用P0/P2已存证据。
- [x] 使用已授权样本抽查电子PDF、扫描PDF、DOCX/DOC和复杂表格；原件和模型完整响应未提交Git。
- [ ] 确认真实服务器使用HTTPS、受控CORS、共享访问密钥、单worker、持久化数据库/上传/报告目录及受限文件权限。
- [ ] 创建全新备份，并在隔离新目录完成一次恢复和应用读取抽查；不得覆盖运行目录。
- [x] 复核发布说明、候选验收签署单和已知限制，没有把候选版本描述为“零风险”或“全自动编标”。
- [x] 项目负责人已于2026-09-03签署 `1.0.0-rc.1` 候选验收，最终候选提交为 `6844019`；确认明确排除标签和推送。
- [x] 用户已明确授权将候选版本提升为 `1.0.0` 并创建本地 `v1.0.0` 标签；明确不授权推送。

## 回滚门禁

- [x] 保留候选代码提交标识 `25e4e96`、依赖锁文件和环境配置项清单；真实部署前仍需生成当次备份清单哈希。
- [ ] 回滚仅切换到已验证代码和隔离恢复后的数据目录，不原位覆盖数据库或用户文件。
- [ ] 回滚后重新执行启动预检、健康探针、鉴权和核心只读接口检查。
