# AI标书助手 1.5.3-rc.1 候选审计

日期：2026-09-24

状态：最终候选 `c9285cf8bad159e451364f04fe4b9e91b84ead4d` 已签署，并通过腾讯云隔离候选门禁。未创建标签、未推送候选、未提升正式版本、未部署。

## 修复范围

- 当采购清单章节存在时，报告章节保持一至八连续编号。
- 当采购清单字段缺失、为空或类型非法时，不生成采购章节，后续章节保持一至七连续编号。
- 不改变报告正文、评分、风险页码映射、样式、数据库结构或用户数据。

## 基线与版本边界

- 修复基线提交：`b3c6ada4e7e662624ac44a0a2c628f32ad767a3a`，已推送至指定GitHub仓库的 `main`。
- 候选父提交固定为上述修复基线；最终候选提交为 `c9285cf8bad159e451364f04fe4b9e91b84ead4d`。
- `VERSION`、FastAPI动态版本、`frontend/package.json` 和锁文件根版本统一为 `1.5.3-rc.1`。
- 已发布 annotated 标签 `v1.5.2` 不移动；候选不创建标签。

## 验证要求

- 报告契约覆盖缺字段、空列表、非法类型和非空采购列表。
- 执行 `scripts/verify_release.ps1`：Python编译、完整pytest、秘密扫描、Python依赖审计、前端lint/build及npm生产依赖审计。
- 确认候选差异不包含 `.env`、数据库、上传件、报告、真实样本、依赖目录、构建产物、旧部署脚本或bundle。
- 任一门禁失败即停止，不创建候选提交。

## 门禁证据

从仓库根目录执行 `scripts/verify_release.ps1`：

- Python编译通过。
- 后端完整回归：182 passed、1 skipped、1条既有Starlette/httpx弃用提示。
- 秘密扫描通过，未读取或输出真实 `.env` 内容。
- Python当前环境依赖审计：0个已知漏洞。
- 前端lint和生产构建通过，构建标识为 `frontend@1.5.3-rc.1`。
- Dashboard图表chunk为585.56 kB（gzip 198.59 kB），保留既有大chunk提醒。
- npm生产依赖审计：0个已知漏洞。
- 版本一致性测试通过。

## 已知限制

- 末页留白和有采购清单样本的原文引用跨页属于既有排版优化项，不纳入本编号补丁。
- 本机普通Windows账户无法创建符号链接，对应用例按既有规则跳过；云端候选门禁仍需在System账户环境复跑。
- 合成报告视觉核查不等于真实业务文件或全部Office转换验收。

## 腾讯云隔离候选门禁

- bundle SHA-256：`7BE6BDBBE492D664801736BDF5BF49A583F4DEC7F79DCD00CD3002BD2FEBA7E8`；上传执行 `inv-r92gq90aki`，隔离环境准备执行 `inv-r92gskgbu3`。
- 完整门禁执行 `inv-r92h1j0q6h` 成功，ExitCode 0：System账户符号链接用例1 passed，完整后端183 passed、1条既有Starlette/httpx弃用提示。
- 秘密扫描通过；Python依赖审计与npm生产依赖审计均为0个已知漏洞；前端lint和生产构建通过。
- 候选位于独立目录 `C:\AI-Bid-Gate\1.5.3-rc.1-c9285cf`；正式目录保持 `a4d837a85ab14cbd560e378e5ee5f59e6d66fda3 / 1.5.2` 且已跟踪工作区干净。
- 隔离目录和上传bundle暂保留以便审计；未切换服务、未修改正式数据或配置、未调用真实AI。

## 后续授权边界

候选已完成签署与腾讯云隔离门禁。下一阶段如需提升正式 `1.5.3`、创建标签、推送或部署，仍须分别取得明确授权。
