# AI标书助手 1.5.2-rc.1 候选审计

日期：2026-09-09

状态：备份原子性修复已统一为 `1.5.2-rc.1` 并通过完整候选门禁；项目负责人已于 2026-09-09 签署候选提交 `e4cf9dd`，仅授权后续腾讯云候选门禁。本候选尚未打标签、推送、提升正式版本或部署。

## 修复范围

- 云端 Windows System 账户回归发现：备份源包含符号链接时会正确拒绝，但先创建的快照目标可因 Windows 异常清理受阻而残留。
- 将上传目录和报告目录的存在性与符号链接检查前置到快照目标创建之前，并在复制时保留二次检查。
- 不改变 SQLite 在线备份、不覆盖、逐文件 SHA-256 清单、恢复校验或用户数据格式。

## 版本与 Git 边界

- `VERSION`、FastAPI 运行版本、`frontend/package.json` 和锁文件根版本统一为 `1.5.2-rc.1`。
- 第三方依赖 `base64-js@1.5.1` 保持不变，没有被项目版本替换误改。
- 最终候选提交为 `e4cf9dd10bad4ef257fd92206dcdce582b2edc74`（短哈希 `e4cf9dd`），父提交为 `5cca7ad6fc44f51ed293042c8d61de85a3fe4e77`。
- 已发布 annotated 标签 `v1.5.1` 仍指向正式提交 `b542d010d2679c0265fcd4cd50cd420fcd39954e`，本候选不创建或移动标签。
- 本批不推送、不部署、不修改云端 `v1.5.1` 检出。

## 门禁证据

从仓库根目录执行 `scripts/verify_release.ps1`：

- Python 编译通过。
- 后端完整回归：177 passed、1 skipped、1 个已知 Starlette/httpx 弃用提示。
- 秘密扫描通过，未读取或输出真实 `.env` 内容。
- Python 当前环境依赖审计：0 个已知漏洞。
- 前端 lint 和生产构建通过，Dashboard 图表块保持 585.56 kB（gzip 198.59 kB）的已审计提示。
- npm 生产依赖审计：0 个已知漏洞。
- 版本一致性测试通过，前端构建标识为 `frontend@1.5.2-rc.1`。

本机普通 Windows 账户不允许创建符号链接，因此对应用例按既有规则跳过；修复的最终符号链接验证必须在腾讯云 System 账户环境复跑。

## 数据与安全边界

- 未读取、复制、删除或提交 SQLite 数据库、上传文件、报告、真实标书和 API Key。
- 未调用 DeepSeek、百度 OCR、WPS 或任何真实业务外部服务。
- 回滚使用可审计的 `git revert`；不使用 `git reset --hard`，不删除用户数据。

## 后续条件

候选提交需先完成只读身份与差异复核，再由项目负责人单独签署。未签署前不提升正式 `1.5.2`、不创建标签、不推送、不部署。
