# AI标书助手 1.5.3 腾讯云部署与回滚方案

日期：2026-09-24

状态：候选签署与腾讯云隔离门禁已完成；未提升正式版本、未创建标签、未推送、未备份或切换正式服务，未部署。

## 发布阶段

1. [已完成] 建立并签署 `1.5.3-rc.1` 本地候选 `c9285cf8bad159e451364f04fe4b9e91b84ead4d`。
2. [已完成] 在腾讯云独立隔离目录运行完整门禁（`inv-r92h1j0q6h`），正式目录保持 `a4d837a / 1.5.2` 不变。
3. [已完成] 经授权提升正式 `1.5.3`、运行完整门禁并创建正式提交 `3c7aebec1901f34e4578da4268d47e3872c27864`。
4. [已完成] 创建 annotated 标签 `v1.5.3`；标签对象 `f859d426ba829670b4a0d1520b2ec03b9d4f66e8` 解引用到正式提交。
5. 经单独授权推送 `main` 与 `v1.5.3`，复核远程提交、标签对象和解引用提交。
6. 经单独部署授权后，才允许备份、传输固定bundle并切换正式环境。

## 部署前硬门禁

- 固定目标必须是已签署并完成云端候选门禁的正式提交，不跟随浮动 `main`。
- 校验Git bundle SHA-256、bundle引用、提交父链、版本和标签解引用身份。
- 云端正式工作区必须无已跟踪修改；记录旧HEAD、版本、服务、计划任务、监听端口和磁盘空间。
- 对SQLite、上传目录、报告目录、生产配置及旧前端dist执行不覆盖备份，并生成逐文件哈希清单。
- 本补丁无数据库迁移；若检查发现迁移差异，立即停止并重新评估。
- 不读取或输出API Key、共享鉴权令牌和真实文档内容。

## 建议部署步骤

1. 在独立临时目录导入固定bundle，验证提交和标签身份。
2. 使用隔离依赖/缓存运行完整后端回归、秘密扫描、依赖审计和前端lint/build。
3. 对正式运行数据及旧dist创建可验证备份；备份失败立即停止，正式目录不变。
4. 只切换代码和前端构建产物，保留生产 `.env`、SQLite、上传、报告和Caddy/Tailscale配置。
5. 保持FastAPI和Caddy仅回环监听，禁止新增腾讯云公网端口或安全组规则。
6. 启动后验证版本、健康/就绪、匿名401、代理鉴权、首页、固定入口JS和项目列表契约。
7. 使用保留合成项目验证报告导出、连续章节编号、PDF/Word预览、高亮、Dashboard穿透及项目对比。

## 回滚方案

- 当前已知线上代码状态为 `a4d837a85ab14cbd560e378e5ee5f59e6d66fda3`；正式执行时必须以部署前服务器实测HEAD及备份清单作为唯一回滚目标，若不一致则停止并更新方案。
- 停止新服务后恢复旧代码、旧dist和原生产配置；本补丁不迁移数据库，原则上不回滚或覆盖用户运行数据。
- 若切换期间产生新报告或上传件，只保留并校验，不删除；必要时隔离新增输出后再恢复服务。
- 恢复后复核旧版本、健康/就绪、鉴权、回环监听、首页/API及项目数量。
- 任一恢复校验失败时保持服务不对外并人工审阅；禁止使用 `git reset --hard` 或删除数据目录。

## 成功标准

- 云端固定提交、版本和静态入口一致。
- 完整门禁及部署后烟雾检查通过。
- 报告章节连续编号在合成数据上复验通过。
- 8000/4173继续仅回环监听，正式受控HTTPS入口正常。
- 项目数、SQLite、上传件和报告在部署前后保持一致。

本文件不构成部署授权。正式部署必须另行明确确认固定提交、备份目录和执行窗口。
