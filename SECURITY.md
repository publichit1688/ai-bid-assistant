# 安全说明

- 真实 API Key 只存放在本机或部署平台的秘密管理中，禁止写入源码、文档、测试夹具、日志和截图。
- `.env` 已加入 `.gitignore`；只提交 `.env.example`，其中不得出现可用凭据。
- 部署时通过环境变量或未提交的 `.env` 设置 `DATABASE_URL`、`UPLOAD_DIR`、`REPORT_DIR` 和 `CORS_ORIGINS`。生产 CORS 只填写实际前端 HTTPS 来源，多个来源用英文逗号分隔，禁止为了省事使用 `*`。
- 数据库、上传目录和报告目录必须挂载到受控的持久化存储，并限制为应用服务账号可读写；修改目录前先备份现有数据，不能把目录切换当作数据迁移。
- 生产入口默认监听 `127.0.0.1` 且不启用 reload；对外服务应由 HTTPS 反向代理转发。启动前检查和就绪接口只返回检查项名称与状态，不得返回数据库URL、绝对目录或凭据。
- `deploy/` 内容仅为需人工审核的模板，不代表已安装系统服务。Nginx域名/证书、systemd用户/路径和Windows包装器路径必须按目标主机替换；不得把秘密直接写进服务单元或XML。
- 应用只向标准输出/错误写日志，轮转由journal、Windows服务包装器或系统logrotate负责。日志禁止包含客户正文、完整提示词、数据库URL、绝对业务文件路径和任何密钥。
- 每个HTTP响应包含 `X-Request-ID`。客户端请求ID只接受1至64位字母、数字、点、下划线和连字符；其他值由服务端替换，防止日志注入。
- 结构化访问日志只包含事件名、请求ID、方法、安全归一化路径、状态码和耗时；不记录查询字符串、请求/响应正文、Cookie、Authorization、客户端IP或原始异常。上传文件名统一记录为 `/uploads/<file>`，数字资源ID替换为占位符。
- 脱敏工具覆盖API Key、Authorization、Cookie、密码、Secret、Token、数据库URL、`sk-`令牌和Bearer令牌。不得绕过该工具把第三方响应或异常对象直接写入日志。
- 上传与AI接口采用进程内滑动窗口限流，超限返回 `429 / RATE_LIMITED` 和 `Retry-After`。默认上传30次/分钟，项目对比与Dashboard AI共享60次/分钟预算；健康、历史读取、报告和静态预览不受此限流影响。
- 限流身份默认只使用直接连接地址且只在内存中保存不可逆SHA-256摘要，不记录原始地址。默认不信任 `X-Forwarded-For`；只有明确启用 `TRUST_PROXY_HEADERS=true` 且直接连接地址属于 `TRUSTED_PROXY_IPS` 时才使用代理覆盖的首个合法地址。
- 当前限流器是单进程防护，重启会清空窗口，多worker/多实例之间不共享。当前生产默认保持 `APP_WORKERS=1`；对公网和多实例部署仍需在可信网关或共享存储层增加全局限流。
- 共享密钥鉴权由 `AUTH_MODE=shared_token` 显式开启；本地默认 `disabled`。生产密钥 `APP_ACCESS_TOKEN` 必须是至少32位的高熵随机值，只能存于秘密管理或未提交的 `.env`，不得放入源码、构建变量、URL、localStorage或日志。
- 共享模式仅公开 `/`、`/api/health` 和CORS预检，其余API、`/uploads/*`与就绪探针均要求Bearer凭据，FastAPI文档入口关闭。启动检查会拒绝缺失、过短或未知模式，但不会输出密钥内容。
- 前后端共享密钥链路已实现：前端只在当前页面内存保存凭据，401时重新提示；PDF、Word转换预览、报告和普通API均经统一客户端。隔离共享模式全链路回归完成前不得在真实部署启用。CORS和限流都不能替代鉴权，方案边界见 `docs/AUTH_DECISION.md`。
- 浏览器控制台禁止输出完整Axios错误对象或业务响应，因为其中可能包含Authorization Header或客户分析数据；前端统一客户端及页面不得记录凭据。
- 生产启动显式关闭Uvicorn默认访问日志，Nginx参考模板也关闭默认访问日志，因为两者会记录原始URL并泄露上传文件名。HTTP请求审计只使用应用层JSON访问日志，其静态文件路径归一化为 `/uploads/<file>`。
- 备份与恢复脚本只允许创建全新目标；备份使用SQLite在线备份API和逐文件SHA-256清单，恢复验证路径、文件集合、哈希及SQLite完整性后通过同级临时目录原子落位。遇到符号链接、篡改或已有目标时拒绝继续；禁止对运行中的生产数据库、上传或报告目录原位恢复。
- 发布前必须执行 Python、npm 生产依赖和仓库秘密扫描。秘密扫描不得读取本地 `.env` 或用户数据，发现疑似凭据时只报告位置和规则、不得打印内容；发现真实凭据应立即吊销轮换，而不是只删除源码。
- 禁止提交 `backend/bid.db`、`backend/uploads/`、`backend/reports/` 和生成的 DOCX/PDF，它们可能包含客户数据。
- 日志不得输出 Key 的全部或部分内容；只允许输出“已配置/未配置”。异常响应不得返回内部路径、完整提示词或第三方原始凭据错误。
- 上传文件必须视为不可信输入。部署前需完成大小、类型、路径、解析超时和恶意文档防护。
- `UPLOAD_MAX_BYTES` 默认限制为 50 MiB；后端以分块读取累计真实字节数，超限后在文档解析、Office 转换、OCR 和 DeepSeek 调用前返回 413 并清理本次临时文件。反向代理仍应设置不高于该值的请求体上限。
- 上传入口同时校验允许扩展名、明确声明的 MIME 和文件头签名：PDF 必须具有 `%PDF-`，DOCX 必须是 ZIP 容器签名，DOC 必须是 OLE 复合文档签名。空 MIME 与 `application/octet-stream` 仅作为客户端兼容输入，仍必须通过文件签名和后续结构解析。
- 扩展名与明确 MIME 不一致返回 `415 / FILE_TYPE_MISMATCH`，签名不一致返回 `415 / FILE_SIGNATURE_MISMATCH`；两者都必须在 Office、OCR 和 AI 调用前失败并清理临时文件。
- 数据库中的文件路径同样视为不可信输入。列表与详情仅对解析后仍位于 `UPLOAD_DIR` 内的路径生成公开地址；越界路径公开地址返回空，Word预览返回 `409 / UNSAFE_STORED_PATH`。
- 物理删除前必须再次解析并验证路径位于上传根目录；异常历史记录允许删除数据库记录，但不得删除、转换或读取上传目录之外的文件。路径校验使用解析后的路径，因此已存在且指向目录外的符号链接也会被拒绝。
- `DEEPSEEK_MODEL` 和 `DEEPSEEK_TIMEOUT_SECONDS` 可按部署环境覆盖。模型名不属于秘密，但 API Key 仍只能存放在秘密管理或未提交的 `.env` 中；超时必须为正数。
- 发现密钥进入 Git 历史时，不要只删除文件：立即吊销并轮换密钥，再按授权流程清理历史。
- 不在自动开发中删除用户数据库、上传件或报告；数据迁移和清理必须先备份并获得明确授权。
- 百度 OCR 属于第三方数据处理：默认关闭，只发送缺少文字层的扫描页；启用前必须确认客户授权、数据合规、账号额度和费用。百度凭据仅允许放入本机 `.env` 或部署平台秘密管理。
