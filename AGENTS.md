# AI 标书助手开发总指令

本仓库是面向中国工程建设招投标场景的 AI 分析 SaaS。技术基线保持为 FastAPI、React/Vite、Ant Design、ECharts、SQLite/SQLAlchemy 和 DeepSeek；除非有明确迁移任务，不替换主技术栈。

## 每次开始前

必须依次阅读：

1. `AGENTS.md`
2. `docs/PROJECT_STATE.md`
3. `docs/ROADMAP.md`
4. `docs/ACTIVE_TASK.md`
5. `TASKS.md` 中最高优先级且未完成的任务

先执行 `git status --short` 并检查相关实现。工作区可能包含用户数据和未提交成果，不覆盖、不清理、不假设未跟踪文件可删除。

## 开发边界

- 当前目标是 V1 收尾与稳定，不继续无限扩展 Dashboard。
- 默认保护：PDF 响应式预览、风险原文定位与高亮、Dashboard 预警穿透、项目中心、风险检查器、项目对比、Word 报告导出、Dashboard AI 摘要缓存。
- 每个自动开发批次只完成一个可验证的小阶段；避免无关重构。
- 修改前定位数据流和回归敏感点，修改后运行与变更相关的最小充分验证。
- 验证失败时先修复；不能修复则记录阻塞原因，不进入下一任务。
- 不删除 SQLite 数据库、上传文件、用户报告；不执行危险 Git 操作。
- 不输出、提交、打印 `.env` 或任何 API Key。日志只允许说明变量是否已配置。
- V1.5 仅做智能编标工作台的目录生成和评分点映射，不做一键生成整本标书。

## 完成定义

一个任务只有同时满足以下条件才可标记完成：

- 需求对应代码或文档已落地，范围没有扩张；
- 可用的 lint、build、编译、启动、接口或人工回归已执行；
- 新问题已修复，或作为已知阻塞写明证据；
- `docs/PROJECT_STATE.md`、`docs/ACTIVE_TASK.md`、`TASKS.md` 与 `CHANGELOG.md` 已同步；
- 最终报告使用中文，包含“本次完成、修改文件、测试结果、当前进度、下一步”。

## 自动选择任务规则

用户只说“继续自动开发”时：读取上述文件，从 `TASKS.md` 选择最高优先级未完成且无阻塞的最小任务；一次只做一个批次，完成验证和文档同步后停止并报告。若任务依赖真实密钥、真实标书或部署账号，只完成无需新增权限的部分，并明确请求所缺条件。
